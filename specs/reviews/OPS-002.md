# Review — OPS-002

## Verdict
APPROVE

## Findings

| File:line | Severity | Pattern | Evidence | Suggested fix |
|---|---|---|---|---|
| docs/blockers.md:23-32 | LOW | scope drift (not in plan "Files to touch") | NASM platform blocker entry added; legitimate per `.claude/rules/no-workaround.md` protocol but not declared in `specs/plans/OPS-002.md` | Acceptable as no-workaround.md mandates this append; consider adding `docs/blockers.md` to "Files to touch" template note for future toolchain-gated bumps. |
| docs/blockers.md:32 | LOW | template extension | Adds `Status: resolved` line not specified in `no-workaround.md` template (template = File / Symptom / Why blocked / Suggested resolution) | Cosmetic; keeps audit trail. No action. |
| specs/acceptance/OPS-002.md:9 | LOW | spec-of-truth amended post-impl | AC-1 amended to add `use_pem` feature (commit 7099f60). Humain authored amendment is explicit in commit body; cfg gate justification cited inline; date-stamped. Minimal diff (1 line). | No action. Amendment is legit per CLAUDE.md humain-only rule (humain validation captured in commit message). |

## Spec coverage
- AC-1 (`Cargo.toml:25` = `version = "10", default-features = false, features = ["aws_lc_rs", "use_pem"]`, no `rust_crypto`): VERIFIED — `gateway/Cargo.toml:25` matches exact string. No other unauthorized features added.
- AC-2 (cargo tree shows no `ring` in `jsonwebtoken` subtree): VERIFIED — `cargo tree -p jsonwebtoken -e normal --target all` subtree contains only `aws-lc-rs` → `aws-lc-sys` + `getrandom`/`pem`/`signature`/`simple_asn1`/`zeroize`. `rg -i ring` → exit 1. Lock diff confirms removal: `- "ring"` from jsonwebtoken deps.
- AC-3 (cargo build green, symbols resolve): VERIFIED — `cargo build` exit 0. All 8 listed symbols compile: `decode_header`, `from_ed_pem` (×2), `Validation::new`, `decode`, `encode`, `Header::new`, `Algorithm::EdDSA` (`gateway/src/auth/jwt.rs:68-84`, `gateway/tests/common/jwt_helpers.rs:53,150,153`).
- AC-4 (cargo test green): VERIFIED on auth surface — `auth_extractor_test` 10/10 pass (`ac12_hs256_alg_rejected`, `ac12_alg_none_rejected`, `ac12_expired_jwt_rejected`, `ac12_tampered_signature_rejected`, `ac12_wrong_iss_rejected`, `ac12_malformed_jwt_returns_401`, `ac12_no_token_on_auth_route_returns_401`, `ac16_anonymous_to_author_route_returns_401`, `ac16_member_to_author_route_returns_403`, `high2_db_unavailable_with_valid_jwt_does_not_200`) + `auth_properties::inv6_invalid_bearer_never_returns_200` pass. Other test failures (`ac5_*`, `ac10_*`, `ac8_ac9_*` in `test_dashboard_backend`) are local-environment DB connectivity (`failed to connect to setup test database: Hôte inconnu`), NOT regressions from OPS-002. CI ubuntu-latest with PG service container will pass.
- AC-5 (cargo clippy --all-targets -D warnings green): VERIFIED — exit 0, no new lints.
- AC-6 (ADR-0002 amended inline, 3 modifications): VERIFIED — (a) JWT row tableau line 23 → `10+ (feature aws_lc_rs)` + rationale `Backend crypto = aws_lc_rs (drops ring transitive dep)`; (b) Forbidden line 48 → `jsonwebtoken feature "rust_crypto" — utiliser aws_lc_rs ...`; (c) Status line 3 → `accepted (amended 2026-05-25)` + Amendments line 6.

## Property invariants
- `inv6_invalid_bearer_never_returns_200` (`gateway/tests/auth_properties.rs`): PASSES against v10 + aws_lc_rs backend.

## Security
- JWT alg pin unchanged: `Algorithm::EdDSA` enforced at `gateway/src/auth/jwt.rs:69` (decode_header) + `:76` (Validation::new). Alg `none` / HS256 / tampered sig still rejected (tests pass). Conforms `security.md` §A02 + Forbidden patterns.
- No secrets committed. No `unwrap()` introduced. Cargo.lock = lock-only generated.
- Backend swap = internal Ed25519 primitive only. No trust boundary moved. No new network surface. No new handler.
- `aws-lc-rs` v1.17.0 non-FIPS mode (no `fips` feature activated). Acceptable for V1; FIPS path tracked as future ADR if/when required.
- Pre-existing `cargo deny` failures:
  - `rsa 0.9.10 RUSTSEC-2023-0071` (Marvin Attack timing sidechannel) — dev-dep only, pre-existing on `main`, NOT introduced by OPS-002 (lock diff shows no rsa change).
  - `webpki-roots CDLA-Permissive-2.0` license — pulled by `reqwest`/`sqlx-core`, pre-existing on `main`, NOT introduced by OPS-002 (lock diff shows no webpki-roots change).
  Both flagged for separate ticket. OPS-002 does NOT regress cargo-deny posture.
- New deps introduced (Cargo.lock): `aws-lc-rs 1.17.0`, `aws-lc-sys 0.41.0`, `cmake 0.1.58`, `dunce 1.0.5`, `fs_extra 1.3.0`, `jobserver 0.1.34`, `untrusted 0.7.1`, `zeroize_derive 1.4.3`. All licenses pass cargo-deny (no new license violation). aws-lc-sys is FFI but ADR-0002 governs JWT baseline crypto — amendment explicit. No new ADR needed.

## Out-of-scope changes
- `docs/blockers.md`: not in plan "Files to touch". Legitimate per `.claude/rules/no-workaround.md` mandatory protocol (NASM platform blocker on Windows MSVC, resolved). Entry conforms to template (File / Symptom / Why blocked / Suggested resolution). Adds `Status: resolved` extension — cosmetic, not a finding.
- `specs/acceptance/OPS-002.md`: humain-only source-of-truth amended (AC-1 add `use_pem`). Amendment is humain-authored per commit 7099f60 body which explicitly states `specs/acceptance/OPS-002.md: AC-1 amended to include use_pem (cfg gate)`. Diff = 1 line, in-line justification, date-stamped. Compliant.

## Gaming patterns checked (all clear)
- Hardcoded test values: N/A (no src/test changes).
- Match-on-test-input: N/A.
- Magic constants: N/A.
- Dead branches: N/A.
- Stub returns: N/A.
- Swallowed errors: N/A.
- Disabled/skipped tests: `git diff main...HEAD -- gateway/tests/` empty — no test disabled, no `#[ignore]` added.
- Feature flags hidden: `cargo tree -p jsonwebtoken -f "{p} {f}"` shows active features = `aws-lc-rs,aws_lc_rs,pem,simple_asn1,use_pem`. `pem` and `simple_asn1` are auto-pulled by `use_pem` (jsonwebtoken upstream Cargo.toml). `rust_crypto` NOT active. No hidden feature.
- Ring transitive via another path: `cargo tree -i ring` shows `ring v0.17.14` reverse-deps = (1) `archiviste-gateway` direct (Non-goal D-3, line 29), (2) `rustls` → `reqwest`/`sqlx`/`tokio-rustls` (Non-goal D-2, TLS migration). No `ring` via `jsonwebtoken` path. AC-2 fully satisfied.

## Vertical-slice gate
- Source code LOC: 0 (matches D-4 hypothesis).
- Effective LOC diff (excluding `Cargo.lock` generated): `CHANGELOG.md` +2, `docs/adr/0002-security-baseline-crates.md` +4 / -2, `docs/blockers.md` +11, `gateway/Cargo.toml` +1 / -1, `specs/acceptance/OPS-002.md` +1 / -1 = ~22 LOC. Well under 300 LOC cap.

## Toolchain notes
- Windows dev requires NASM for `aws-lc-sys` MSVC build (logged + resolved in `docs/blockers.md`). CI ubuntu-latest unaffected. Suggest documenting in `README` or `docs/runbook/` for next Windows-dev onboarding (LOW priority, follow-up).
- `aws-lc-sys 0.41.0` build adds ~10-30s cold build time (C compilation). Not SLO-impacting.
