# Review — OBS-007

## Verdict
APPROVE

## Local gates (run in `eval/`)
| Gate | Result |
|---|---|
| `uv run ruff check .` | PASS — All checks passed |
| `uv run mypy --strict .` | PASS — no issues in 26 source files |
| `uv run pytest` | PASS — 104 passed, 1 skipped (pre-existing DB integration test, `DATABASE_URL` not set) |

## Findings

| File:line | Severity | Pattern | Evidence | Suggested fix |
|---|---|---|---|---|
| eval/tests/test_runner_oidc_cli.py:180-200 | MED | coverage theatre / tautology | `test_token_not_in_stdout_stderr_on_exit_5` uses `_FailingProvider`, which never produces a token. The sentinel `FAKE_TOKEN_SENTINEL` is asserted absent from stdout/stderr, but no token is ever created on this path, so the assertion cannot fail regardless of behaviour. The "token present but redacted" half of AC-11(b) is untested. | Add a success-path test: authed target + `_SpyProvider` (returns `SecretStr(sentinel)`) over a stubbed httpserver; after a full run, assert sentinel absent from run-file **and** stdout/stderr while clients DID send `Authorization: Bearer <sentinel>`. |
| eval/tests/* | MED | run-file leak unverified | AC-11 mandates "no token bytes in the run-file". No test reads the written run-file and asserts the sentinel is absent. (Code path is safe — `auth_header` SecretStr is never stored in `RunFile`/`EntryResult` — but the AC oracle is not exercised.) | Same success-path test above: load `output_path`, assert sentinel not in its bytes. |
| eval/pyproject.toml:71-73 | MED | type-system suppression beyond plan | `[[tool.mypy.overrides]] module=["eval.oidc"] disallow_untyped_calls=false` is load-bearing: removing it surfaces `oidc.py:86 error: Call to untyped function "fetch_id_token" [no-untyped-call]` (verified locally). Plan AC-9 authorised only the `ignore_missing_imports` mirror of psycopg2 for `google.auth`/`google.auth.*`; this extra per-module relaxation on a **production** module was not in the plan and weakens `mypy --strict` there. Borderline `no-workaround.md` (silencing the type checker). | Prefer scoping suppression to the single call site (`fetch_id_token(...)  # type: ignore[no-untyped-call]` with a WHY comment) rather than relaxing the whole module, OR document the override in AC-9 with human sign-off. Not a merge blocker — `cast(str, ...)` already pins the return type. |
| docs/vision.md (44 lines) | LOW | out-of-scope change | Modified but not in plan "Files to touch". Roadmap update (marks OBS-001..004 delivered, adds OBS-EVAL epic). Documentation-only, no behaviour. | Acceptable as roadmap bookkeeping; note for the record. |
| (whole diff) | LOW (accepted exception) | vertical-slice LOC overrun | ~591 added LOC counting prod+tests (`oidc.py`+`clients.py`+`ragas_runner.py`+`pyproject.toml`+tests+CHANGELOG), vs ≤300 cap. prod ~164, tests ~437. | Human has already accepted the overrun; recorded as accepted exception, not a blocker. |

## Spec coverage
- AC-1: ✓ `test_clients_auth.py::test_retrieve_client_sends_auth_header`, `::test_generate_client_sends_auth_header` (Authorization + X-Request-Id captured) + `test_oidc.py::test_is_authenticated_target` (https+remote=True)
- AC-2: ✓ `test_clients_auth.py::test_*_no_auth_header_by_default` (exact `{X-Request-Id}`, no Authorization key) + truth-table http/localhost/127.0.0.1/::1=False
- AC-3: ✓ no new arg in `_parse_args` (only OBS-003 `--persist` pre-existing); no env read added; `main(provider=...)` is a test seam, not a CLI flag. Decision driven solely by `is_authenticated_target(workers_url)`.
- AC-4: ✓ `test_oidc.py::test_derive_audience` (path/query/trailing-slash stripped, port preserved, `[::1]:9000` re-bracketed) + property `test_derive_audience_idempotent`. Verified `urlsplit/urlunsplit` round-trips IPv6 brackets and preserves port.
- AC-5: ✓ `IdTokenProvider` Protocol injected; tests use fakes only, no real metadata call. `test_metadata_provider_conforms_to_protocol`.
- AC-6: ✓ `test_runner_oidc_cli.py::test_exit_5_on_oidc_failure_before_any_entry` — exit 5 AND run-file not written (proves fail-fast before `_run_all_entries`). Token fetch precedes `_check_workers_reachable` and entry loop in `main()`.
- AC-7: ✓ `test_oidc_token_failed_log_has_no_token_bytes` — `oidc_token_failed` present, sentinel absent. `main()` logs `audience` only.
- AC-8: ✓ `test_exit_5_distinct_from_other_codes` + `test_runner_docstring_documents_exit_5`; module docstring line `5 — OIDC token fetch failure (authenticated target)`.
- AC-9: ✓ `google-auth>=2` in `[project].dependencies`; mypy `ignore_missing_imports` override for `google.auth`/`google.auth.*`/`google.oauth2`/`google.oauth2.*`. (See MED finding re: extra `eval.oidc` override.)
- AC-10: ✓ `test_provider_not_called_on_localhost_target` (spy.calls == []) + `test_clients_auth.py::test_retrieve_client_without_auth_never_calls_provider`.
- AC-11: PARTIAL — SecretStr type + repr/str redaction proven (`test_secret_str_redacts_in_repr`, `test_auth_header_secret_not_in_repr`); failure-path stdout/stderr check present but tautological (MED finding); success-path run-file/stdout leak check missing (MED finding). Code is correct (no leak path exists), oracle is under-tested.
- AC-12: ✓ `git diff main...HEAD -- specs/openapi/gateway-to-workers.yml` is empty.

## Property invariants
- AC-4 audience idempotence covered by hypothesis `test_derive_audience_idempotent` (local invariant; `specs/properties.md` correctly untouched — human-only, invariant is runner-local).

## Security
- No secrets in code/tests (sentinel is a fake non-credential literal).
- SecretStr: ✓ token wrapped at provider boundary (`oidc.py:89`), `.get_secret_value()` called only at httpx header construction (`clients.py:66,113`; `ragas_runner.py:99`). No plaintext token stored in `RunFile`/`EntryResult`.
- No unauthenticated fallback on authed target: ✓ `OidcTokenError` → `return 5` immediately; no `Authorization`-less retry (`ragas_runner.py:462-467`).
- A09 logging: ✓ `log.warning("oidc_token_failed", audience=audience)` — audience only, no token bytes.
- Token-fetch error handling: broad `except Exception` at `oidc.py:87` is re-raised as `OidcTokenError(...) from exc` (not swallowed) — compliant with no-workaround.
- `verify=False`: none. TLS via httpx defaults.
- SSRF: audience derived from operator-supplied `--workers-url`, not end-user input; metadata server is an intentional dependency. No new user SSRF surface.
- pip-audit (A06) on new `google-auth` dep: CI gate, not run locally; flag for CI.

## Out-of-scope changes
- `docs/vision.md` — 44 lines, not in plan "Files to touch". Roadmap bookkeeping, documentation-only (LOW).
- `eval/pyproject.toml:71-73` — `eval.oidc` mypy override not specified in plan AC-9 (MED, see findings).

## Notes
- Diff LOC ~591 (prod ~164 / tests ~437) over the ≤300 vertical-slice cap — accepted exception per human, not a blocker.
- `eval/uv.lock` (155 lines) regenerated for the new dep — generated file, excluded from LOC count, expected.
- Verdict APPROVE: all 3 MED findings are test-rigour/typing-hygiene gaps on code paths that are themselves correct; none is a HIGH security or spec-failure. Recommend addressing the AC-11 success-path/run-file test gap in this PR or a fast follow before relying on the leak guarantee.
