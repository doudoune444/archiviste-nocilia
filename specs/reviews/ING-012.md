# Review — ING-012

## Verdict
APPROVE

## Findings

| File:line | Severity | Pattern | Evidence | Suggested fix |
|---|---|---|---|---|
| .github/workflows/gdrive-sync.yml:72-89 | LOW | partial-failure observability | `extract summary` step has no `if: always()`. If `run sync` exits with a code != 0 and != 1 (hard crash, line 66-69 re-raises), the step fails, subsequent non-`always()` steps skip, and `/tmp/summary.json` never gets written. The `upload artifact` step (`if: always()`, line 150) then fails with "No files were found", contradicting the plan intent "always, even on sync error" (plan line 38). | Add `if: always()` to `extract summary` so the placeholder fallback (lines 77-80) is guaranteed to run before upload. Spec AC-9 doesn't strictly require artifact on hard crash, so not blocking. |
| .github/workflows/gdrive-sync.yml:10-12 | LOW | concurrency group not namespaced | `group: gdrive-sync` is a plain string, not `${{ github.workflow }}-...`. No collision today (no other workflow uses this group), but a future workflow re-using the name would serialise against this one. | Optional: rename to `gdrive-sync-${{ github.workflow }}` for forward-compat. Cosmetic. |

## Spec coverage

- AC-1 (triggers: dispatch + cron `0 3 * * 1`): PASS — `.github/workflows/gdrive-sync.yml:3-6`.
- AC-2 (`ubuntu-24.04`, single `sync` job, explicit minimal permissions): PASS — lines 15-23. GHA semantics: unspecified permissions default to `none` when `permissions:` is declared explicitly.
- AC-3 (`actions/checkout` pinned by 40-hex SHA + tag comment + `fetch-depth: 0`): PASS — line 38 `@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1` (SHA verified via `gh api repos/actions/checkout/git/refs/tags/v4.3.1` → matches); `fetch-depth: 0` line 40.
- AC-4 (`astral-sh/setup-uv` pinned + `uv sync`): PASS — line 43 `@caf0cab7a618c569241d31dcd442f54681755d39 # v3.2.4` (SHA verified); `uv sync` at line 47.
- AC-5 (script invocation with secret at step scope): PASS with documented deviation — spec literal `python scripts/gdrive_export.py` is replaced by `cd scripts && uv run python -m gdrive_export` (working-directory `scripts`, line 55). Deviation flagged in plan Risks (plan lines 51-53) and consistent with `scripts/gdrive_export/__main__.py` shipped entrypoint + runbook line 226. Secret injected at step env only (line 57), never written to filesystem.
- AC-6 (no-changes path, `no changes to sync` log): PASS — lines 93-101. `git status --porcelain lore/ scripts/.gdrive_state.json` includes both modified-tracked (state file) and untracked (new `lore/*.md`) entries by default.
- AC-7 (PR via peter-evans v6 SHA, unique branch, title with ISO date, body with `Summary:` line BEFORE fenced JSON BEFORE 2-item checklist, labels, commit-message): PASS — line 138 `@c5a7806660adbe173f04e3e038b0ccdcd758773c # v6.1.0` (SHA verified, v6.1.0 confirmed via action.yml at that ref); branch `chore/gdrive-sync-${{ github.run_id }}` line 140; date computed line 106; body construction lines 122-131 in correct order (Summary line → fenced ```json → 2 checklist items).
- AC-8 (`draft` toggle on errors): PASS — line 145 `draft: ${{ steps.summary.outputs.errors != '0' }}`. v6.1.0 `draft` input accepts the resulting `'true'`/`'false'` string (confirmed in action.yml input spec).
- AC-9 (artifact `gdrive-sync-summary.json`, retention 30, `if: always()`): PASS — lines 149-155. Caveat in LOW finding above.
- AC-10 (no `git push origin main`, no `--force`, no `gh pr merge`): PASS — grep `git push|gh pr merge|--force` in `.github/workflows/gdrive-sync.yml` returns only the AC-10 reminder comment line 135.
- AC-11 (concurrency, `cancel-in-progress: false`): PASS — lines 10-12.
- AC-12 (`timeout-minutes: 30`): PASS — line 24.
- AC-13 (fail-fast BEFORE checkout, exact message): PASS — step 1 lines 28-35, message `secret GDRIVE_SA_KEY missing — see docs/runbook.md` (em-dash U+2014, byte-identical to spec line 26 and runbook line 342, verified by `grep -c` returning 1 in each of the 3 files).
- AC-14 (actionlint in pre-commit AND ci.yml, same SHA): PASS — `.pre-commit-config.yaml:3` rev `a443f344ff32813837fa49f7aa6cbc478d770e62 # v1.7.9` ; `.github/workflows/ci.yml:31` `uses: rhysd/actionlint@a443f344ff32813837fa49f7aa6cbc478d770e62 # v1.7.9`. SHAs identical, verified against `rhysd/actionlint v1.7.9` ref via GitHub API.
- AC-15 (`vars.GDRIVE_ROOT_FOLDER_ID` documented as repo variable, non-secret): PASS — `docs/runbook.md:310-319` (section (d)), explicit "variable de repo (non-secret)".
- AC-16 (literal H2 `## Onboarding gdrive-sync` + 5 sub-procedures): PASS — `docs/runbook.md:278` literal title; sub-sections (a) line 283 SA + scopes (cross-ref existing section), (b) line 292 Drive share, (c) line 298 `gh secret set GDRIVE_SA_KEY`, (d) line 310 `gh variable set GDRIVE_ROOT_FOLDER_ID`, (e) line 321 `gh workflow run gdrive-sync.yml` + review PR.

## Property invariants

- `specs/properties.md`: no invariant applies — ticket is CI-config only, no production code, no embedding/SQL/auth path.

## Security

- Secrets in code: none detected. `GDRIVE_SA_KEY` reference appears only as `${{ secrets.GDRIVE_SA_KEY }}` template (lines 30, 57). Masked by GHA at runtime.
- Secret scope: `GDRIVE_SA_KEY_JSON` injected only as `env:` of `run sync` step (line 57), never job-level, never workflow-level. Never written to disk (no `echo "${GDRIVE_SA_KEY}" >` or equivalent). PASS.
- Step-1 fail-fast (line 32) reads `GDRIVE_SA_KEY` from env (not secrets context inline in shell), so `[ -z ... ]` runs on the masked value — no shell injection vector.
- Supply chain (A08): all 5 third-party action references pinned by 40-hex commit SHA with adjacent `# vX.Y.Z` comment. Each SHA verified against `repos/<owner>/<repo>/git/refs/tags/<tag>` via `gh api` (resolves to `type: commit` matching the pinned hex). No tag float, no `@main`, no `@master`. PASS.
- Permissions (A01): explicit `contents: write` + `pull-requests: write` + `actions: read` at workflow scope (lines 15-18). All others default to `none` per GHA semantics when block is declared. PASS.
- Concurrency (race condition on `scripts/.gdrive_state.json`): `cancel-in-progress: false` prevents mid-write corruption between manual + cron coincident runs. PASS.
- Shell-injection surface (A03): `${{ vars.GDRIVE_ROOT_FOLDER_ID }}` interpolated into `--root-folder-id "..."` (line 62) is repo-admin-controlled, not user/PR-supplied. `${{ github.run_id }}` numeric. `${{ steps.date.outputs.today }}` produced by `date -u +%Y-%m-%d`. `${{ steps.pr_body.outputs.body }}` passed as YAML value to peter-evans action input, not a shell. No `pull_request` or `pull_request_target` trigger — no user-controlled input enters the run.
- PR body markdown injection: summary JSON inserted between ` ```json ``` ` fences (lines 126-128). `gdrive_sync.summary` event (`scripts/gdrive_export/sync.py:808,814`) only carries numeric counters — no filename/URL strings — so no backtick / fence-escape vector.
- No SSRF, no SQL, no JWT, no LLM, no CORS, no CSP in this diff (CI-only).
- `gitleaks` already wired pre-commit (`.pre-commit-config.yaml:40-43`) and not bypassed.

## Out-of-scope changes

None. Touched files: `.github/workflows/gdrive-sync.yml` (new), `.github/workflows/ci.yml` (1 step added in `lint` job), `.pre-commit-config.yaml` (1 hook added), `docs/runbook.md` (1 section appended), `CHANGELOG.md` (1 bullet), `specs/plans/ING-012.md` (plan itself). All listed in plan `## Files to touch`.

## LOC budget

Code/config diff (excluding plan markdown and CHANGELOG): 231 LOC measured per task brief (workflow 155 + ci.yml +2 + pre-commit +6 + runbook +67 = 230, within 300 cap). Under budget. PASS.

## Notes for implementer

- The 5 pinned SHAs were re-verified live against `gh api repos/<owner>/<repo>/git/refs/tags/<tag>` during this review and all resolve to `type: commit` matching the pinned hex. No fabricated SHAs.
- The AC-5 entrypoint deviation (module form vs literal `scripts/gdrive_export.py` in spec) is acceptable because (a) the literal path doesn't exist in tree, (b) the deviation is logged in plan Risks line 52, (c) the runbook (line 226) and `__main__.py` doc string already use the module form. Spec amendment is not required; the spec's example invocation is loose-prose, not a contract clause.
- Optional polish (LOW-1): add `if: always()` to `extract summary` so the summary artifact is also uploaded on hard-crash exits. Strict reading of AC-9 doesn't require this; the upload step's current `if: always()` covers exit codes 0 and 1, which are the spec'd success and partial-error paths (AC-8). Suggest as a follow-up if observability of crash scenarios matters.
