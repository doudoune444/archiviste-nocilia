# Review — OBS-002 (re-review after remediation)

## Verdict
APPROVE

Re-review of `af45cf1` (remediation) on top of `4cec3be` (impl), diff base `origin/main` (merge-base `f40593f`).
All prior REQUEST_CHANGES blockers resolved. Endpoint correct, secure, lint-clean, 12/12 OBS-002 tests green.
LOC overage (~474 net prod) is accepted-by-human as one end-to-end slice (no widget split). No new bloat/dead code introduced by the remediation.

## Prior findings — disposition

| Prior finding | Status | Evidence |
|---|---|---|
| repository.py out-of-scope (MED) | FALSE POSITIVE / RESOLVED | Was PR #124, merged to origin/main. ABSENT from `git diff origin/main...HEAD`. Not re-raised. |
| test_conversation_integration.py out-of-scope (MED) | FALSE POSITIVE / RESOLVED | Same — #124, absent from diff vs origin/main. |
| styles.css + observability.css nav churn out-of-scope (MED) | FALSE POSITIVE / RESOLVED | styles.css is #124, absent from diff. observability.css now only adds `#health-widget` rules (in-scope). |
| docs/vision.md out-of-scope (LOW) | RESOLVED | Now in-scope: OBS-002 row reword drops stale "503/Mistral" clause + OBS-006 row added for the split. Roadmap accuracy, consistent with split decision. |
| LOC ceiling breach (MED) | ACCEPTED BY HUMAN | Explicit human decision to ship as one slice; do not split widget (OBS-002b). Noted, not blocking. |
| status.rs:88 dead `Utc::now()` (LOW) | RESOLVED | Replaced by `String::new()` + comment; cache overwrites checked_at at health_cache.rs:79. |
| probe_gcs/probe_workers DRY (LOW) | RESOLVED | Extracted `probe_http_endpoint` + `HttpProbeArgs`; no behavior change (verified vs health.rs). |
| AC-10 TTL-expiry + probe-counter under-tested (LOW) | RESOLVED | 2 new unit tests in health_cache.rs (counter==1; inject_expired TTL refresh + RFC3339 parse). |
| AC-4 checked_at never parsed RFC3339 (LOW) | RESOLVED | `ac10_cache_refreshes_after_ttl_expiry` parses fresh checked_at via `DateTime::parse_from_rfc3339`. |

## Remediation verification

| Concern | Verdict | Evidence |
|---|---|---|
| AC-10 cache tests tautological? | NO | `ac10_cache_prevents_second_probe_within_ttl`: two distinct closures (tags "first"/"second"), `AtomicUsize`==1 proves 2nd closure never ran, equal `checked_at` proves cache hit. `ac10_cache_refreshes_after_ttl_expiry`: `inject_expired` ages entry 11s>10s TTL, asserts distinct checked_at + RFC3339 parse. Both genuinely exercise the cache. |
| Empty checked_at ever served? (regression risk) | NO | `run_probes` sets `checked_at: String::new()` (status.rs:88) but `get_or_refresh` overwrites it on BOTH paths: slow path sets `snapshot.checked_at = checked_at.to_rfc3339()` (health_cache.rs:79) before storing AND serving; fast path returns the stored snapshot whose checked_at was already overwritten. Empty string never reaches a client. Confirmed by all integration tests asserting present/RFC3339 checked_at. |
| DRY refactor behavior change? | NO | `probe_http_endpoint` (status.rs:215) preserves: token-fail→down (caller), GET + `bearer_auth(expose_secret)`, `2xx`→ok else down, `warn` without host/message. Mirrors health.rs:51-64. AC-12 2xx→ok and non-2xx→down both green. |
| Secret hygiene preserved? | YES | `expose_secret` only at `.bearer_auth(...)` send site inside `probe_http_endpoint` (status.rs:219). `HttpProbeArgs.bearer: &SecretString` carries it unexposed. No token logged. |
| New bloat/dead code from remediation? | NO | af45cf1 touched only status.rs, health_cache.rs, docs/vision.md. `inject_expired` is `#[cfg(test)]` test seam, justified. No prod dead branch added. |

## Findings

| File:line | Severity | Pattern | Evidence | Suggested fix |
|---|---|---|---|---|
| gateway/static/assets/observability.css:46-48 | LOW (non-blocking, pre-existing) | dead CSS class | `.health-status--down` never matched: observability.js applies `health-status--<globalStatus>` only to the "Service" line (globalStatus ∈ {ok,degraded}); per-dep lines get no class. Class is from impl 4cec3be, NOT remediation. | Drop the rule, or apply per-dep status classes in JS in a future ticket. Not blocking. |

## Spec coverage
- AC-1: ✓ `ac1_ac2_ac5_status_shape_200_sanitised` (3 deps, no mistral, exactly 3 root keys, JSON content-type)
- AC-2: ✓ same test (exactly {status, latency_ms}, enum ok|down, negative leak assertions: no postgres://, host, SELECT, 127.0.0.1)
- AC-3: ✓ `ac3_aggregate_status_property_table` — all 8 combinations, real `ok ⟺ all ok` check
- AC-4: ✓ `checked_at` present + stable-in-cache + now PARSED RFC3339 in `ac10_cache_refreshes_after_ttl_expiry` (prior LOW closed)
- AC-5: ✓ `ac1_ac2_ac5...` + `ac6_all_down_returns_200_degraded`
- AC-6: ✓ `ac6_all_down_returns_200_degraded`; handler returns `Json<StatusResponse>` (infallible, no 5xx path)
- AC-7: ✓ `ac7_contract_status_no_auth_guard` (include_str! on public_api + no RequireAuthor/Member); mounted in `public_api` lib.rs:536
- AC-8: ✓ `ac8_ac9_all_probes_timeout_bounded_wall_clock` — real never-responding TCP listener, latency_ms ≤ 2200
- AC-9: ✓ same test — wall-clock < 3.5s (real `tokio::join!` status.rs:71)
- AC-10: ✓ `ac10_two_calls_within_ttl_same_checked_at` (integration) + `ac10_cache_prevents_second_probe_within_ttl` (counter==1) + `ac10_cache_refreshes_after_ttl_expiry` (TTL refresh) — within-TTL AND expiry AND probe-counter now all covered (prior LOW closed)
- AC-11: ✓ `ac11_probe_error_down_no_detail_in_body` (no host/error/`error` key in body); warns sanitised (dep + reason only)
- AC-12: ✓ `ac12_workers_2xx_is_ok` + `ac12_workers_non_2xx_is_down` (mockito); probe mirrors health.rs::probe_workers
- AC-13: ✓ (manual AC-17) — `observability.js` renders via `textContent` + `className`, no innerHTML, no inline style
- AC-14: ✓ `observability.js` non-200/network/parse → `'Santé du service indisponible.'`, no per-code branching
- AC-15: ✓ `ac15_status_security_headers` — 5 headers byte-for-byte; CSP/HSTS literals match lib.rs exactly (CSP unchanged)
- AC-16: ✓ `git diff origin/main...HEAD specs/openapi/gateway-to-workers.yml` = ZERO lines
- AC-17: pending human manual checklist (procedure documented in spec)

## Property invariants
- AC-3 aggregate property covered by table test (8 combos). Correctly kept OUT of `specs/properties.md` (human-only; no status invariant exists). No `specs/properties.md` change — correct.

## Security
- Secrets: ✓ `secrecy` used; `expose_secret()` only at `.bearer_auth(...)` send site (status.rs:219). `HttpProbeArgs.bearer: &SecretString` unexposed in transit. No tokens logged.
- SQL injection: ✓ postgres probe is static `sqlx::query("SELECT 1")`; no `format!` into SQL.
- SSRF: ✓ GCS host hardcoded `storage.googleapis.com`, bucket from config; workers URL from `state.config.workers_url`. No user-supplied URL.
- 5xx leak: ✓ handler returns `Json<StatusResponse>` (infallible). Failure detail confined to `tracing::warn` with dep+reason only (no host/message/SQL).
- Headers: ✓ 5 security headers byte-for-byte; CSP not modified.
- XSS: ✓ widget uses `textContent` + `className` only; never `innerHTML`; no inline `style=`.
- Trust boundary: ✓ public anon endpoint exposes only aggregate ok|down + latency; no per-user data, no IDOR surface.

## Out-of-scope changes
- None. All three prior false-positive out-of-scope files (repository.py, test_conversation_integration.py, styles.css) are PR #124, already merged to origin/main, ABSENT from `git diff origin/main...HEAD`. Confirmed absent, not re-raised.
- docs/vision.md roadmap reword + OBS-006 row: in-scope for the OBS-002→OBS-006 split (drops stale "503/Mistral" clause).

## Lints
- `cargo fmt --check`: clean (exit 0)
- `cargo clippy --all-targets -- -D warnings`: clean (exit 0, forced recompile of all 3 touched sources)
- `cargo test --test test_observability_status`: 10/10 pass
- `cargo test --lib health_cache`: 2/2 pass (new AC-10 cache tests)
- The 4 `#[sqlx::test]` DB tests fail locally for infra reasons only (Docker/Postgres off) — authoritative in CI, NOT findings.

## LOC
- Net prod diff ~474 LOC (status.rs 260, health_cache.rs 218 incl. tests, observability.js +75, css +33, html +3, state +10, lib/mod +6). Over the 300 vertical-slice ceiling. EXPLICIT HUMAN DECISION to ship as one end-to-end slice; widget not split into OBS-002b. Accepted-by-human, not a blocker. No new bloat introduced by remediation (af45cf1 = +161/-30 across 3 files, all justified: cache tests + DRY helper).
