# Review — UI-002b verdict APPROVE

Dashboard auteur frontend (PR2 du split UI-002). Reviewer adversarial pass 2026-05-26.

## Verdict

**APPROVE** (one MED suggestion non-blocking).

## Local gates

| Gate | Result |
|---|---|
| `cargo fmt --check` | PASS |
| `cargo clippy --all-targets -- -D warnings` | PASS |
| `cargo test --test test_dashboard_frontend` | 11/11 PASS |
| Pre-existing `test_dashboard_backend` failures | non-regression PR1 (`sqlx::test` sans Postgres local) |

## Spec coverage (PR2 scope)

| AC | Status | Evidence |
|---|---|---|
| AC-1  | OK       | `test_dashboard_frontend.rs:103` `ac1_dashboard_200_for_author` — 200 + `text/html` + sélecteur `<table id="tickets-table"` (`dashboard.html:15`). |
| AC-3  | OK       | `:185` + `:209` — `/assets/dashboard.{js,css}` 200 + CT correct (ServeDir UI-001 `lib.rs:394`). |
| AC-4  | PARTIAL  | `ac4_unknown_asset_returns_404` + `ac4_path_traversal_blocked` couvrent régression. Aucun test n'assert "exactement 5 assets" byte-for-byte ; inventaire manuel `static/assets/` = `app.js`, `styles.css`, `dashboard.js`, `dashboard.css` (favicon.ico 404 toléré). Acceptable phase 1. |
| AC-11 | OK       | `RequireAuthor` extractor `handlers/dashboard.rs:21` ; monté `dashboard_api` `lib.rs:415`. Member → 403 `author_required` (`ac11_dashboard_403_for_member`). Anonyme → 401 `invalid_token` (`ac11_dashboard_401_for_anonymous`, déviation L4). |
| AC-12 | OK       | `assert_security_headers` `:65` — 4 headers byte-for-byte sur `/dashboard`, `/assets/dashboard.{js,css}`. CSP littéral `:60` matche UI-001. |
| AC-13 | OK       | `ac13_no_inline_in_dashboard_html` `:308` grep `<script>`/`<style`/`style="`/`style='`/11 `on*=`. `dashboard.html` = `<script src=…>` + `<link rel="stylesheet" href=…>` uniquement. |
| AC-14 | OK (AC-24 manuel) | `dashboard.js:25-54` `renderRow` `textContent` exclusif. Ordre colonnes match spec. Aucun `innerHTML`. |
| AC-15 | OK (AC-24 manuel) | `dashboard.js:76` `window.open(body.signed_url, '_blank', 'noopener,noreferrer')` byte-for-byte. |
| AC-16 | OK (AC-24 manuel) | `dashboard.js:13-22` `showError` littéral `Erreur de chargement. Réessayez.` + `<small>req: <id></small>` optionnel. Trigger non-200 / JSON parse fail / catch network. |
| AC-17 | OK (AC-24 manuel) | `dashboard.js:107-113` littéral `Aucun ticket ouvert.` `colSpan = 5`. |
| AC-18 | OK (AC-24 manuel) | `dashboard.js:126-139` `<button id="load-more">Charger plus</button>` ; retrait quand `currentOffset >= totalTickets`. Pagination = `offset + body.items.length` (L1). |
| AC-24 | DEFERRED | Procédure 16 étapes humain avant `ready to ship`. |

## Security audit

| Check | Status | Evidence |
|---|---|---|
| Secrets in code         | CLEAN | Aucun. |
| `unwrap`/`expect` prod  | CLEAN | `handlers/dashboard.rs` zéro ; tests `#![allow(...)]` convention repo. |
| XSS / output sanit      | CLEAN | `textContent` only ; CSP `script-src 'self'`. |
| `window.opener` leak    | CLEAN | `noopener,noreferrer` byte-for-byte. |
| Auth gate `/dashboard`  | CLEAN | `RequireAuthor`, pas `#[public]`. |
| A09 log hygiene         | N/A   | Handler = `Html(DASHBOARD_HTML)` ; pas de log (AC-23 respecté). |
| A05 error envelope      | CLEAN | 403 `author_required` + 401 `invalid_token` byte-for-byte. |
| Path traversal `/assets/../` | CLEAN | `ac4_path_traversal_blocked` `:248`. |
| Forbidden patterns      | CLEAN | Aucun. |

## LOC accounting

| File | LOC |
|---|---|
| `dashboard.html` | 31 |
| `dashboard.js` | 151 |
| `dashboard.css` | 115 |
| `handlers/dashboard.rs` | 23 |
| `handlers/mod.rs` | +1 |
| `lib.rs` | +3 |
| `CHANGELOG.md` | +4 |
| **Prod total** | **328** (cap 300, +28 déclaré) |
| `tests/test_dashboard_frontend.rs` | 351 (hors cap) |

Vertical-slice dépassé +28 LOC (déclaré, accepté humain). CSS 115 vs plan ~40.

## Findings

### HIGH
Néant.

### MED

- **MED-1 — CSS 115 LOC vs plan ~40** : design system CSS custom-properties (`--color-*`, `--font-*`) + reset global `*` ; pas de palette partagée avec `styles.css` UI-001. Premature-abstraction mineure. Follow-up ticket : factor `_palette.css` ou strip custom-properties. Non-bloquant.

### LOW

- **L1 — `dashboard.js:104` pagination déviation sémantique** : spec AC-18 littéral "offset += 50" ; impl `currentOffset = offset + body.items.length`. Équivalent happy-path, plus sûr sur page courte. Défensible.
- **L2 — `catch (_)` x3** (`:67, :93, :121`) : swallow d'exception. Violation `no-workaround.md §Forbidden`. Mitigé car AC-16 trigger erreur visible. Suggérer `console.error(err)` pour dev.
- **L3 — Pas de null-check `getElementById`** (`:14, :106, :127`) : couplé HTML/JS phase 1, acceptable.
- **L4 — AC-2/11 anonyme 401 vs 403 byte-for-byte** : `RequireAuthor` propage `AuthError::InvalidToken` avant tier check (`auth/extractor.rs:179`). Aligné PR1 déjà mergé (`test_dashboard_backend.rs:147, 241`). Docstring `:156-161` documente. Follow-up : amender spec OU re-mapper `RequireAuthor` pour missing-token → `AuthorRequired`. Non-bloquant PR2.
- **L5 — `#![allow(clippy::unwrap_used, clippy::expect_used)]`** dans tests : convention repo (cf `static_test.rs:5-6`). Pas un finding.

## Out-of-scope

Aucun. Les 8 fichiers du diff sont tous dans `## Files to touch` du plan + CHANGELOG.

## OpenAPI / properties

- `specs/openapi/gateway-to-workers.yml` : inchangé (AC-22 hérité PR1). Diff confirme 0 touch.
- `specs/properties.md` : aucun INV applicable (pas de DB write, pas de LLM, pas de random).

## Hand-off

- Humain owes AC-24 manual checklist (16 steps spec `## Test manuel auteur`) avant `Status: ready to ship`.
- Pas de `/eval` (frontend pur, hors pipeline RAG).
- Suivant : `/ship` (gate review APPROVE ✓ + CI vert + eval N/A).
