# Review — ING-013 (round 2, post-fix)

## Verdict
APPROVE

## Fix commit verified
`58f1ee9 fix(scripts): ING-013 review feedback (HIGH .env restore, MED status enum + PNG md5 source)`

| Previous finding | Status |
|---|---|
| HIGH `.env.example` destructive rewrite | FIXED — 13 main entries restored verbatim; 2 new commented vars appended (`GDRIVE_SA_KEY_JSON`, `GDRIVE_ROOT_FOLDER_ID`); pre-existing `GOOGLE_APPLICATION_CREDENTIALS=` (line 16) reused, no duplicate. Net `git diff main...HEAD -- .env.example` = +4 lines (header + 2 vars + 1 mojibake fix), 0 deletions of substance. |
| MED sync.py:246 gdoc `"create"` non-dry-run | FIXED — line 251: `return f"{prefix}create" if dry_run else "created"`. |
| MED sync.py:309 PNG `"create"` non-dry-run | FIXED — line 330: `status = f"{prefix}create" if dry_run else "created"`. |
| MED sync.py:298 PNG content_signature source | FIXED — `_process_png` now takes `drive_md5` from `file.get("md5Checksum")` (sync.py:89, :168, :181, :293) and short-circuits the unchanged path at lines 302-308 **before** `drive_client.download_png(file_id)` at line 310. Client-side hash retained as defense-in-depth fallback when Drive omits the field (lines 318-325). |
| LOW dead `structlog` dep | NOT FIXED — human ack (mypy still reports `unused section(s): module = ['structlog']`). |
| LOW sync.py:280 redundant `incoming_body_hash != existing.body_hash` clause | NOT FIXED — human ack. |
| LOW `_set_archived_frontmatter` bypasses `_FM_KEY_ORDER` | NOT FIXED — human ack. |
| LOW residual `# type: ignore[no-untyped-call]` in `auth.py` | NOT FIXED — accepted (upstream stubs gap). |
| LOW LOC overrun (sync.py 479, total src ~750) | NOT FIXED — human ack. |

## New test assertions (regression guards)

| Test | Assertion |
|---|---|
| `test_gdoc_written_and_state_saved` (scripts/tests/test_sync_integration.py:128-131) | `file_logs[0]["status"] == "created"` (exact match, not prefix). |
| `test_png_written_and_state_saved` (scripts/tests/test_sync_integration.py:160-163, 166-167) | Status exact `"created"` AND `content_signature == f"md5:{drive_md5}"` where `drive_md5` is the value supplied via the Drive list response. |

## Findings (round 2)

| File:line | Severity | Pattern | Evidence | Suggested fix |
|---|---|---|---|---|
| _none introduced by fix commit_ | — | — | All previously NOT-fixed LOWs remain acknowledged; no new gaming, no spec drift, no security regression. | — |

## Spec coverage (delta vs round 1)

| AC | Previous | Now |
|---|---|---|
| AC-11 (content_signature) | partial (PNG used client-side md5) | OK — PNG uses Drive `md5Checksum` field; client-side fallback only when field absent. Test asserts equivalence with Drive-supplied md5. |
| AC-17 (status enum) | partial (`"create"` non-dry) | OK — non-dry emits `"created"` for both gdoc and PNG. Tests assert exact string. |
| AC-22 (`.env.example`) | FAIL (13 prior entries deleted) | OK — diff vs main is purely additive (2 new commented vars + 1 header comment + 1 mojibake fix). |
| AC-1..AC-10, AC-12..AC-21 | OK | OK (unchanged). |

## Security re-check (post-fix)
- No secrets touched. `.env.example` contains only commented placeholders.
- No new dependencies, no new I/O paths, no new external calls.
- `drive_md5` is plumbed from the Drive API list response (trusted-source field), never user-supplied.
- PNG short-circuit no longer triggers a network fetch on unchanged files (perf + Drive quota reduction).
- No fail-fast / error-leak regression.

## Lints / tests (run from `D:\Projet-perso\archiviste-nocilia\.worktrees\ING-013\scripts`)
- `uv run ruff check .` → All checks passed!
- `uv run mypy .` → Success: no issues found in 24 source files. (Same non-fatal `unused section(s): module = ['structlog']` note — tracked LOW.)
- `uv run pytest -q` → 147 passed in 2.74s.

## Out-of-scope changes
- None. Fix commit touches only `.env.example`, `scripts/gdrive_export/sync.py`, `scripts/tests/test_sync_integration.py` — all in scope.

## Verdict rationale
- Single HIGH (HIGH `.env.example`) — resolved cleanly, diff is now strictly additive vs main.
- Three MEDs — all three resolved with tight, mirrored fixes and matching test assertions that would catch any future regression.
- LOWs intentionally deferred per human ack (recorded above).
- Lints + tests green. No new findings.

APPROVE — ready to merge.
