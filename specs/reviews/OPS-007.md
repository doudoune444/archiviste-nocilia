# Review — OPS-007

## Verdict
APPROVE

## Findings

| File:line | Severity | Pattern | Evidence | Suggested fix |
|---|---|---|---|---|
| specs/acceptance/OPS-007.md:77 | LOW | human-only file touched | `draft` → `ready` status flip on acceptance file (CLAUDE.md: `specs/acceptance/<ID>.md` is human-only). AC text unchanged. | Confirm human approved the status flip; cosmetic, not gating. |
| infra/terraform/cloud_run_job.tf:112-115 | LOW | magic literal vs symbolic ref | `value = "archiviste-conversations"` is a string literal duplicating `google_storage_bucket.conversations.name` (gcs.tf:3). Plan OQ-1 explicitly accepts the literal per touch-point wording. | Optional: reference `google_storage_bucket.conversations.name`. Spec-sanctioned as-is; no action required. |

No HIGH or MED findings.

## Spec coverage
- AC-1: ✓ contract — `storage.tf:26-30` adds `google_storage_bucket_iam_member.lore_corpus_bucket_reader`, `role = roles/storage.legacyBucketReader`, `bucket = google_storage_bucket.lore_corpus.name`, member `gha_deploy`. Bucket-scoped resource type (not `google_project_iam_member`). `legacyBucketReader` carries `storage.buckets.get` (AC-1 explicitly sanctions this predefined least-privilege role).
- AC-2: ✓ run-real (human) — rsync step present `gdrive-sync.yml:125` (`gcloud storage rsync lore/ gs://archiviste-lore-corpus --recursive`); binding now satisfies the `storage.buckets.get` it needs. Green-run oracle is human-run per spec.
- AC-3: ✓ contract — `cloud_run_job.tf:112-115` injects `GCS_BUCKET=archiviste-conversations`. Confirmed `settings.py:28` `gcs_bucket: str = Field(...)` (required), pydantic `env_prefix` absent → field maps from `GCS_BUCKET` env. Sole reader is `main.py:71` (conversation persistence, not ingest path) — claim verified. `archiviste-conversations` bucket exists (`gcs.tf:2-3`), so not a magic string.
- AC-4: ✓ contract — `archiviste-runtime` lore_corpus binding unchanged at `objectViewer` (`storage.tf:34-38`). No `storage.buckets.get` granted to runtime: `iam.tf` runtime project roles = cloudsql.client/instanceUser/secretAccessor (no storage); runtime storage bindings = `objectAdmin` on conversations (pre-existing, iam.tf:56-59) + `objectViewer` on lore_corpus. OPS-006 AC-3 preserved.
- AC-5: ✓ run-real (human) — full-chain oracle, human-run per spec pre-conditions. Code-side prerequisites (binding + env) both present.

## Property invariants
- n/a — no RAG path, no DB schema, no invariant from `specs/properties.md` touched. Confirmed in plan Test strategy.

## Security
- No secrets detected. `GCS_BUCKET=archiviste-conversations` is a bucket name, not a credential — correctly injected in clear like `INGEST_ROOT` / `LORE_CORPUS_BUCKET`. `MISTRAL_API_KEY` remains `secret_key_ref` (unchanged, cloud_run_job.tf:88-92).
- Least-privilege: ✓ new `gha-deploy` grant is bucket-scoped (`google_storage_bucket_iam_member`), no project-wide `storage.buckets.*` (iam.tf:17-23 project roles carry no storage role). `legacyBucketReader` adds read-of-bucket-metadata only, no write.
- No over-grant to `archiviste-runtime`: ✓ (AC-4).
- No new SA key / JSON, auth via WIF (gha) + Cloud Run identity (Job) — unchanged.
- No SSRF / SQLi / injection / JWT / CORS surface in this diff (IAM + env only).

## Gaming patterns
- No hardcoded test values, no match-on-test-input, no stub returns, no swallowed errors, no disabled/skipped tests. Diff is pure Terraform IAM + env. `GCS_BUCKET` literal is spec-sanctioned (OQ-1), backed by a real bucket — not a fake/magic constant.

## Lints / tests
- `terraform fmt -check -recursive` (infra/terraform): PASS (exit 0).
- `terraform validate` (after `terraform init -backend=false`): `Success! The configuration is valid.`
- No Rust/Python application code in diff → cargo/ruff/mypy/pytest not applicable to this slice. ING-001 pipeline, CLI `--root`, `download_corpus.py` unchanged (confirmed: only CHANGELOG + 2 .tf files in code-side diff).

## Out-of-scope changes
- None in code. Files touched (`storage.tf`, `cloud_run_job.tf`, `CHANGELOG.md`) match plan "Files to touch" exactly.
- `specs/acceptance/OPS-007.md` status `draft`→`ready` and new `specs/plans/OPS-007.md` are workflow artifacts, not code scope. Acceptance status flip flagged LOW (human-only file).

## Size
- Code diff: 23 insertions (CHANGELOG 4 + cloud_run_job.tf 9 + storage.tf 10), 0 deletions. Well under 300 LOC vertical-slice limit.
