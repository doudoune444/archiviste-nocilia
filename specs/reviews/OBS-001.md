# Review — OBS-001

## Verdict
APPROVE

## Findings

| File:line | Severity | Pattern | Evidence | Suggested fix |
|---|---|---|---|---|
| gateway/tests/test_observability.rs:445-447 | MED | weakened assertion | `ac8_index_html_nav_links` uses `html.find(r#"href="/"#)` (no closing quote). First match is `href="/assets/styles.css"` at index.html:7, NOT the Chat link `href="/"`. `pos_chat` points to the stylesheet; test passes even if the `Chat` link is deleted. Does not prove AC-8 ("Chat → `/`"). | Match the exact anchor: `html.find(r#"<a href="/">Chat</a>"#)` (or `href="/"` + literal `Chat`), and assert the `Chat` label is present. Same fix for `ac8_observability_html_nav_links:465-467`. |
| gateway/tests/test_observability.rs:444 | LOW | assertion gap | AC-8 requires "exactly two links" in order; tests assert presence + relative order only, never that there are exactly two nav anchors or that labels (`Chat`, `Observabilité`) are correct. | Assert nav `<a>` count == 2 and both label strings present. |
| gateway/tests/test_observability.rs:576-658 | LOW | thoroughness note | AC-5 says `n1 ≠ n2 → distinct exposed values`. Covered implicitly by 3 separate fixtures (0/1/42) each asserting exact equality; no single test asserts the distinctness property directly. Acceptable (identity proven by 3 exact points), called out for completeness. | Optional: collapse the 3 near-identical DB tests into one parametrized loop over `[0,1,42]` to cut duplication (~80 LOC). |

## Spec coverage
- AC-1: ✓ `ac1_observability_page_200_html_with_widget` (200 + text/html + `<section id="usage-widget"`)
- AC-2: ✓ `ac2_observability_anonymous_no_auth`, `ac2_observability_member_jwt_gets_200`, `ac2_observability_author_jwt_gets_200` (all 200, never 401/403)
- AC-3: ✓ `ac3_observability_js_200_javascript_content_type`, `ac3_observability_css_200_css_content_type`
- AC-4: ✓ `ac4_ac5_stats_zero_conversations` asserts `{"conversation_count":0}` + exactly 1 key (no extra field). (DB test — runs on CI Postgres; fails locally only on host-resolution, env constraint.)
- AC-5: ✓ `ac4_ac5_stats_{zero,one,forty_two}_conversations` — exact identity at 0/1/42, no bucket/rounding. (Distinctness property proven by 3 exact points; see LOW finding.)
- AC-6: ✓ `ac6_contract_stats_handler_has_no_auth_extractor` (no `RequireAuthor`/`RequireMember`/`author_only`/`member_only` in stats.rs) + `ac6_contract_lib_mounts_stats_in_public_api`. Confirmed in source: lib.rs:531-533 mounts `/v1/stats` in `public_api`, handler has no auth extractor (stats.rs:41-44).
- AC-7: ✓ `ac7_stats_no_pool_returns_503_sanitized` — 503, exact body `{"error":"upstream_unavailable","request_id":<36-char>}`, exactly 2 keys, negative-asserts `conversations`/`select`/`postgres`/`host`/`panic` absent. Source: errors.rs:42 maps `UpstreamUnavailable` → 503 + fixed code; stats.rs:56 `.map_err(|_| ApiError::UpstreamUnavailable)` discards the raw sqlx error (intentional sanitization, not a swallowed error — error is converted to a typed variant, no info loss that matters).
- AC-8: ~ implemented in index.html:10-13 + observability.html:11-14 (correct nav, two links, correct order). Tests are too loose (see MED finding) but the artifacts themselves satisfy AC-8; manual AC-15 step 5 covers click behavior.
- AC-9: ✓ (manual AC-15) observability.js:22 `fetch('/v1/stats',{credentials:'same-origin'})`; observability.js:51 `widget.textContent = 'Conversations : ' + count` (textContent, never innerHTML); observability.html:16 `<h1>Observabilité</h1>`.
- AC-10: ✓ (manual / unit-of-JS) observability.js renders literal `Données indisponibles.` on network error (23-26), non-ok (29-32), malformed JSON (36-41), and non-number count (45-48). No per-code branching.
- AC-11: ✓ `ac11_observability_page_security_headers`, `ac11_observability_js_security_headers`, `ac11_observability_css_security_headers`, `ac11_stats_503_security_headers` — all 5 headers byte-for-byte incl. CSP unchanged. Source: lib.rs:574-594 applies the layer router-wide AFTER merging public_api + static_router; CSP constant untouched by this diff.
- AC-12: ✓ `ac12_no_inline_in_observability_html` — no `<script>`/`<style>`/`style=`/`on*=`. Confirmed: observability.html loads JS via `<script src>` (line 21), CSS via `<link>` (lines 7-8).
- AC-13: ✓ `ac13_unknown_asset_404`, `ac13_path_traversal_blocked` (400/404 + no `[package]` leak). ServeDir unchanged.
- AC-14: ✓ `git diff main...HEAD -- specs/openapi/` is empty. OpenAPI untouched.
- AC-15: manual author checklist — human-gated, spec Status: ready.

## Property invariants
- `specs/properties.md` lists no invariant touching stats/count (plan §Test strategy confirms INV-1..10 unrelated). No property test required — correctly not invented.

## Security
- A01 Broken Access Control: `/v1/stats` public anonymous by design (AC-6), exposes only aggregate `conversation_count` (no per-user data, no PII, no resource id in path/query → no IDOR). ✓
- A03 Injection: stats.rs:53 `sqlx::query_as("SELECT count(*) AS count FROM conversations")` — static string, zero user input, no `format!`. ✓
- A05 Error sanitization: 503 path returns fixed `{"error":"upstream_unavailable","request_id":"<uuid>"}` (errors.rs:34-57); raw sqlx error discarded at stats.rs:56. No SQL/host/table/stack leak — test-enforced. ✓
- Output handling: observability.js uses `textContent` exclusively (line 11, 51); count is type-checked `=== 'number'` before render (line 45). No `innerHTML`, no XSS sink. ✓
- CSP: unchanged (AC-11 enforced byte-for-byte). ✓
- No secrets in static assets or handler. ✓
- No `unwrap`/`expect`/`panic` in src/: stats.rs:58 uses `unwrap_or(u64::MAX)` (infallible, no panic). ✓

## Lint / build
- `cargo fmt --check`: PASS
- `cargo clippy --all-targets -- -D warnings`: PASS (clean)
- `cargo test --test test_observability`: 18/18 non-DB tests PASS. 3 `ac4_ac5_*` `#[sqlx::test]` tests fail locally with `Hôte inconnu` (no Postgres host) — known env constraint, authoritative on CI (Linux + Postgres). Not a defect.

## Quality
- Handler `stats` body ≤ 40 lines (stats.rs:41-69, ~24 lines). ✓
- Naming: `StatsResponse`, `CountRow`, `conversation_count` — clear, no abbreviations. ✓
- Diff size: OBS-001 implementation files (handler + lib + static + tests + CHANGELOG) ~860 added lines, but ~658 are the test file and ~70 the handler/static. Excluding tests/generated, production code is well under 300 LOC (vertical-slice budget excludes tests). Test file is 3.5x the plan's ~190 estimate — thorough rather than bloated, but carries duplication: the 3 `ac4_ac5_*` tests share identical scaffolding (sentinel-user insert + state build + request) and the 3 `ac11_*_security_headers` tests are near-identical. Could be parametrized to cut ~120 LOC. Not blocking.
- No N+1, no blocking I/O in async (single `fetch_one`). Observability: `tracing::info!` on success path (stats.rs:61) + spec-mandated `warn` is via `ApiError` mapping; success log present. ✓

## Out-of-scope changes
- `docs/vision.md` (174 lines changed) is in a SEPARATE commit `729380a docs(vision):` predating the OBS-001 implementation commit `3612e05`. Per review instructions this is a pre-existing vision purge, not part of OBS-001's slice. Not counted against OBS-001 scope. No code/spec/plan files outside the plan's "Files to touch" were modified by the implementation commit.
