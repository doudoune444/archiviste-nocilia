# Review — OBS-002

## Verdict
REQUEST_CHANGES

Core endpoint is correct, secure, lint-clean, and well-tested (10/10 OBS-002 tests green).
Blocking reasons are vertical-slice / scope violations, not correctness or security.

## Findings

| File:line | Severity | Pattern | Evidence | Suggested fix |
|---|---|---|---|---|
| workers/src/archiviste_workers/conversation/repository.py:31-55 | MED | out-of-scope change | `_UPSERT_ANON_USER_SQL` + transaction wrapping is a separate bug fix (anon `user_id` FK `unknown_user` → `/v1/stats` counter stuck at 0). Belongs to OBS-001 stats, not OBS-002. Not in plan "Files to touch". | Split into its own fix ticket/PR, or get explicit human sign-off to piggyback. SQL itself is fine (parameterized `$1::uuid`, no injection). |
| workers/tests/test_conversation_integration.py:132-161 | MED | out-of-scope change | New test for the anon-user fix above; same scope concern. | Move with the fix above. |
| gateway/static/assets/styles.css:35-53 + gateway/static/assets/observability.css:1-13 | MED | out-of-scope change | Nav CSS moved from `observability.css` into shared `styles.css` (OBS-001 nav-unstyled fix). Not OBS-002. Inflates diff and churns an OBS-001 file. | Split nav fix to its own PR; OBS-002 should only add `#health-widget` rules. |
| docs/vision.md:83-90 | LOW | out-of-scope change | Roadmap edits (OBS-002 row reword + new OBS-006 row). Not in plan. Benign but unscoped. | Acceptable to keep if human approves; otherwise revert to OBS-006 PR. |
| (whole diff) | MED | LOC ceiling breach | Net prod diff ~471 LOC (status.rs 246, health_cache 101, observability.js 75, css ~32, html 3, state 10, lib/mod 4). Tests +493, specs +368. Ceiling is 300 (excl. migrations/generated). Pure-OBS-002 prod (excl. nav/workers fixes) is still ~440. Plan risk note predicted ~290-389 and flagged "if reviewer judges >300 net, signal split static in OBS-002b" — not done. | Either split static widget to OBS-002b (locked decision said widget itself not split — but static-vs-handler split is fair) or get explicit human waiver. |
| gateway/src/handlers/status.rs:88 | LOW | dead value | `checked_at: chrono::Utc::now().to_rfc3339()` is always overwritten by `HealthSnapshotCache::get_or_refresh` (health_cache.rs:79). Verified the overwrite is real — AC-4 comment is truthful, not a lie. The literal here is wasted work / misleading. | Set `checked_at: String::new()` with a comment, or have cache own the field entirely. |
| gateway/src/handlers/status.rs:163-225 | LOW | DRY (3rd occurrence) | `probe_gcs` and `probe_workers` share an identical `match send().await { Ok(r) if is_success() => ok, _ => warn+down }` tail (+ postgres has a 3rd near-variant). Three occurrences = clean-code DRY trigger. | Extract a small `classify_response(result, t0, dep) -> DepStatus` helper. Judgment call; not blocking. |
| gateway/tests/test_observability_status.rs:368-388 | LOW | AC under-tested | AC-10 oracle requires (a) >10s → distinct `checked_at` and (b) probe-counter not re-run. Only "within-TTL same checked_at" is tested. | Add a probe-counter stub + a TTL-expiry case (or document why timing test is omitted). |
| gateway/tests/test_observability_status.rs:124-135 | LOW | AC under-tested | AC-4 oracle = "`checked_at` parse en RFC3339". Test only asserts it is a present string, never parses it. | `DateTime::parse_from_rfc3339(checked_at)` assertion. |

## Spec coverage
- AC-1: ✓ `ac1_ac2_ac5_status_shape_200_sanitised` (3 deps, no mistral, exactly 3 root keys)
- AC-2: ✓ same test (exactly {status, latency_ms}, enum ok|down, negative leak assertions)
- AC-3: ✓ `ac3_aggregate_status_property_table` — all 8 combinations, real `ok ⟺ all ok` check (not faked)
- AC-4: ~ partial — `checked_at` present + stable-in-cache verified; never parsed as RFC3339 (LOW)
- AC-5: ✓ `ac1_ac2_ac5...` + `ac6_all_down_returns_200_degraded`
- AC-6: ✓ `ac6_all_down_returns_200_degraded`; no 503/5xx path exists in handler (returns `Json<StatusResponse>`, infallible)
- AC-7: ✓ `ac7_contract_status_no_auth_guard` (include_str! on lib.rs public_api + status.rs no RequireAuthor/Member); verified mounted in `public_api` lib.rs:534
- AC-8: ✓ `ac8_ac9_all_probes_timeout_bounded_wall_clock` — real TCP listener that never responds, latency_ms ≤ 2200
- AC-9: ✓ same test — wall-clock < 3.5s (real `tokio::join!`, not faked); handler uses `tokio::join!` status.rs:71
- AC-10: ~ partial — `ac10_two_calls_within_ttl_same_checked_at` covers within-TTL; >TTL distinct + probe-counter not covered (LOW)
- AC-11: ✓ `ac11_probe_error_down_no_detail_in_body` (no host/error/`error` key in body); warns are sanitised (dep + reason only)
- AC-12: ✓ `ac12_workers_2xx_is_ok` + `ac12_workers_non_2xx_is_down` (mockito); probe mirrors health.rs::probe_workers
- AC-13: ✓ (manual AC-17) — `observability.js` renders via `textContent`, classes via `className`, no innerHTML, no inline style
- AC-14: ✓ `observability.js` non-200/network/parse → `'Santé du service indisponible.'`, no per-code branching
- AC-15: ✓ `ac15_status_security_headers` — 5 headers byte-for-byte; test CSP/HSTS literals match lib.rs:490/494 exactly (CSP unchanged)
- AC-16: ✓ `git diff` shows ZERO lines in `specs/openapi/gateway-to-workers.yml`
- AC-17: pending human manual checklist (procedure documented)

## Property invariants
- AC-3 aggregate property covered by table test (8 combos). Plan correctly keeps it OUT of `specs/properties.md` (human-only; no status invariant exists there). No `specs/properties.md` change — correct.

## Security
- Secrets: ✓ `secrecy` used; `expose_secret()` only at `.bearer_auth(...)` send site (status.rs:166, 205). No tokens logged.
- SQL injection: ✓ postgres probe is static `sqlx::query("SELECT 1")`; the out-of-scope workers upsert is parameterized `$1::uuid` (no `format!` into SQL).
- SSRF: ✓ GCS host hardcoded `storage.googleapis.com`, bucket from config; workers URL from `state.config.workers_url`. No user-supplied URL.
- 5xx leak: ✓ handler returns `Json<StatusResponse>` (infallible); no path can 5xx and leak detail. Failure detail confined to `tracing::warn` with dep+reason only (no host/message/SQL).
- Headers: ✓ 5 security headers present byte-for-byte; CSP not modified.
- XSS: ✓ widget uses `textContent` + `className` only; never `innerHTML`; no inline `style=`.
- Trust boundary: ✓ public anon endpoint exposes only aggregate ok|down + latency; no per-user data, no IDOR surface.

## Out-of-scope changes
- `workers/src/.../repository.py` + `workers/tests/test_conversation_integration.py` — anon-user FK fix, not OBS-002. Justify or split.
- `gateway/static/assets/styles.css` + nav churn in `observability.css` — OBS-001 nav fix, not OBS-002. Justify or split.
- `docs/vision.md` — roadmap edit (OBS-006 row), not in plan. Benign; confirm with human.

## Lints
- `cargo fmt --check`: clean
- `cargo clippy --all-targets -- -D warnings`: clean (re-touched sources to force recompile)
- `cargo test`: all green except the 4 `#[sqlx::test]` DB tests (`ac5_empty_db...`, `ac5_tickets_list...`, `ac8_ac9_signed_url...`, `ac10_nonexistent_conversation...`) which panic in sqlx testing harness (Docker/Postgres off) — infra-only, authoritative in CI. Not findings. All 10 OBS-002 tests pass.

## Notes for the specific concern raised
- The "checked_at overwrite" comment (status.rs:86-87) is **true**, not a lie: `HealthSnapshotCache::get_or_refresh` records `Utc::now()` BEFORE `probe_fn().await` (health_cache.rs:76) then overwrites `snapshot.checked_at` (line 79). checked_at is the pre-probe instant of the served snapshot → AC-4 satisfied. The `Utc::now()` at status.rs:88 is merely redundant (LOW).
