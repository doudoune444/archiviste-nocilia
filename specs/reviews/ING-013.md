# Review — ING-013

## Verdict
REQUEST_CHANGES

## Findings

| File:line | Severity | Pattern | Evidence | Suggested fix |
|---|---|---|---|---|
| .env.example:1-43 | HIGH | out-of-scope destructive edit | The previous `.env.example` had 13 entries (OPENAI/ANTHROPIC keys, Langfuse, GCS_BUCKET, DATABASE_URL, REDIS_PASSWORD, RUST_LOG, BIND_ADDR, WORKERS_URL, LLM_PROVIDER/MODEL/API_KEY, WORKERS_INTERNAL_BASE_URL, CONVERSATION_INTERNAL_BASE_URL) used by FOUND-002, GEN-001, ING-003, gateway boot. ING-013 wiped the file and replaced it with only 3 gdrive-related vars (43 lines -> 11, 35 deletions). AC-22 mandates *adding* 3 commented vars, not deleting all others. Plan said "creer si absent" — file was *not* absent, it had to be appended to. This breaks new-dev onboarding for every other ticket. | Restore the previous 13 entries verbatim from `main`, then *append* the 3 new gdrive vars (`GDRIVE_SA_KEY_JSON=`, `GDRIVE_ROOT_FOLDER_ID=`, `GOOGLE_APPLICATION_CREDENTIALS=` — the third already exists in main, so only 2 new lines). |
| scripts/gdrive_export/sync.py:246 | MED | spec violation AC-17 (status enum) | `_gdoc_status_and_rename` returns `f"{prefix}create"` for `existing is None`, regardless of `dry_run`. In non-dry mode this yields `status="create"`, but AC-17 requires the status enum `created|updated|renamed|archived|unchanged|error` (past participle). Note lines 252 and 254 correctly switch tense via `if not dry_run else` — only the create branch on line 246 forgot this. No test catches this because `test_gdoc_written_and_state_saved` only checks `summary.created`, not the log status string. | Change line 246 to `return f"{prefix}create" if dry_run else "created"`. Mirror the fix at sync.py:309 (PNG `status = f"{prefix}create"`) which has the same bug. Add an assertion in `test_gdoc_written_and_state_saved` and `test_png_written_and_state_saved` that the per-file log status is exactly `"created"`. |
| scripts/gdrive_export/sync.py:309 | MED | spec violation AC-17 (status enum) | PNG path: `status = f"{prefix}create"` in non-dry yields `"create"` instead of spec-mandated `"created"`. Same root cause as the gdoc bug above. | Same as above. |
| scripts/gdrive_export/sync.py:298 | MED | spec violation AC-11 (PNG content_signature source) | AC-11 explicitly says PNG signature = `md5:<hex>` = `md5Checksum` **natif renvoye par l API Drive sur le fichier binaire** (the field returned by the Drive list API). The implementation computes `hashlib.md5(png_bytes).hexdigest()` client-side after downloading. `drive_client.py:23` already fetches `md5Checksum` in `_LIST_FIELDS` but the field is never read in `sync.py`. Functional consequence: every PNG must be downloaded each run before unchanged detection runs, burning Drive quota and time for the unchanged case. Test `test_png_written_and_state_saved` asserts only the `md5:` prefix, not source equivalence with `md5Checksum`. | In `_process_png`, accept `file["md5Checksum"]` (passed down from list response) as the canonical signature; short-circuit the `existing.content_signature == new_sig` branch *before* calling `download_png`. Keep client-side md5 as a verification (defense-in-depth) but use the API field per spec. |
| scripts/pyproject.toml:10 + mypy override | LOW | dead dependency | `structlog>=24` added as a runtime dep, mypy override declared, but `grep structlog` in production code returns no match. Sync.py uses `json.dumps + print` for logging. The plan acknowledged "fallback logging + json.dumps manuel suffit" — so the dep should not have been added at all. mypy itself flags it: `pyproject.toml: note: unused section(s): module = ['structlog']`. | Remove `structlog>=24` from `dependencies` in `scripts/pyproject.toml`, remove the corresponding `[[tool.mypy.overrides]] module = [..., "structlog"]` entry, regenerate `scripts/uv.lock`. |
| scripts/gdrive_export/sync.py:275 | LOW | dead clause | `if current_hash != existing.body_hash and incoming_body_hash != existing.body_hash` — the second conjunct is provably true at this call site (caller already verified `existing.content_signature != new_sig` which uses the same body bytes hashed differently, so `body_hash` must also differ). The `# noqa: PLR1714` further hints the author wrestled with the lint. Spec AC-20 requires only the local-drift condition (`current_hash != existing.body_hash`). | Drop the second conjunct: `if current_hash != existing.body_hash:`. |
| scripts/gdrive_export/sync.py:396-408 | LOW | partial spec coverage AC-4 | `_set_archived_frontmatter` mutates the frontmatter dict via `fm["archived"] = True; fm["archived_at"] = ...` and re-dumps, but does **not** apply `_FM_KEY_ORDER`. If the local file's frontmatter keys are not already in canonical order (e.g. human edit reordered them), the archived rewrite preserves the wrong order. Since first writes go through `_write_gdoc -> _build_ordered_frontmatter`, this is only triggered after external edits. | Route through `_build_ordered_frontmatter` (passing the existing yaml as `existing_yaml` and the two archived flags as `script_managed`) so key order is reasserted on every write. |
| scripts/gdrive_export/auth.py:79-83 | LOW | acknowledged `# type: ignore` (gating) | Single residual `# type: ignore[no-untyped-call]` on `service_account.Credentials.from_service_account_info` — annotated in code as upstream `google-auth` stubs gap, not a workaround. Reasonable. Pre-flight note: a typed wrapper would require local `.pyi` stubs; out-of-scope for this slice. | Accept as-is. Track upstream stubs gap if appetite for local stubs ever appears. |
| scripts/gdrive_export/sync.py | LOW (acknowledged) | LOC 458 / total src 729 > 300 | Vertical-slice limit blown, acknowledged in plan + orchestrator-indivisible rationale. Reasonable extraction would be `frontmatter_io.py` (~80 LOC: `_write_gdoc`, `_set_archived_frontmatter`, `_read_existing_frontmatter`, `_build_ordered_frontmatter`) -> sync.py drops to ~378, total to ~649 src. Still over 300 but tighter. Not a blocker per human ack; tracked as ergonomic debt. | Optional follow-up: split frontmatter I/O out, or accept and tag as known debt. |

## Spec coverage

| AC | Status | Evidence |
|---|---|---|
| AC-1 (list recursive) | OK | `drive_client.list_folder_recursive` + `test_drive_client.TestListFolderRecursive` (5 cases incl. pagination + nested) |
| AC-2 (gdoc export) | OK | `_process_gdoc` + `test_sync_integration.test_gdoc_written_and_state_saved` |
| AC-3 (PNG download) | OK | `_process_png` + `test_png_written_and_state_saved` (asserts no `.md` produced) |
| AC-4 (frontmatter order) | partial | `test_frontmatter_key_order` checks first 6 keys only (`title..archived`). Archived rewrite bypasses ordering (see finding above). |
| AC-5 (state versioned) | OK | `scripts/.gdrive_state.json` committed with `{}`, `.gitignore` line removed |
| AC-6 (rename) | OK | `test_gdoc_rename`; no integration test for PNG rename + `image_renamed` log (only gdoc), but `_process_png` logs the event |
| AC-7 (creds env + conflict log) | OK | `test_auth` full matrix incl. `test_conflict_log_emitted_exactly_once` |
| AC-8 (archive idempotence) | OK | `test_gdoc_archived_on_deletion` + `test_archive_idempotent` |
| AC-9 (collision suffix) | OK | `test_collision_resolved_with_suffix` + sort-by-id on sync.py:62 |
| AC-10 (normalize body) | OK | `test_c0_control_chars_stripped` |
| AC-11 (content_signature) | partial | gdoc OK; PNG implementation deviates from spec source (`md5Checksum` field unused — see finding above) |
| AC-12 (dry-run) | OK | 2 tests assert no writes, `would_*` prefix, summary counters |
| AC-13 (start + first_run order) | OK | `test_first_run_log_order` + `test_no_first_run_log_when_state_exists` |
| AC-14 (1 MiB cap post-normalize) | OK | `test_body_over_1mib_skipped` (gdoc); no explicit PNG-over-1MiB test but code path identical |
| AC-15 (429 continue) | OK | `test_429_logged_and_continued` |
| AC-16 (5xx exit 1) | OK | `test_5xx_causes_exit_1` (asserts summary.errors >= 1; exit mapping in `__main__` covered by `test_exit_1_on_errors`) |
| AC-17 (summary + per-file logs) | partial | summary OK; per-file `status` enum violated for `created` non-dry (see MED finding) |
| AC-18 (timeout 30s) | OK | `test_http_timeout_30s` asserts `httplib2.Http(timeout=30)` |
| AC-19 (exit code) | OK | `test_exit_0_on_no_errors` + `test_exit_1_on_errors` |
| AC-20 (body_hash drift) | OK | `test_drift_overwritten_and_logged` |
| AC-21 (runbook section) | OK | `docs/runbook.md` "## Sync Google Drive (ING-013)" — manual review |
| AC-22 (.env.example) | FAIL | 3 vars present but 13 prior vars deleted — see HIGH finding |

## Property invariants
- No relevant invariant in `specs/properties.md` for runtime orchestration. None expected per plan. OK.

## Security

- No secrets in code or fixtures — `_FAKE_SA` in `test_auth.py` uses placeholder `"fake-private-key-placeholder"`, not a real PEM.
- `.gitignore` keeps `*.bak` ignored; `scripts/.gdrive_state.json` is `{}` — safe to commit.
- SA credentials loaded only from env vars; no default fallback (AC-7) — matches `secret-hygiene.md`.
- `httplib2.Http(timeout=30)` enforces hard timeout per security.md A04.
- Drive API client scoped `drive.readonly` — least-privilege.
- No SSRF risk: no user-supplied URL is fetched; all calls go to `googleapis.com` via the SDK.
- No `verify=False`, no unsafe deserialization, no shell subprocess.
- No `# type: ignore` slipped through inappropriately; the single remaining ignore in `auth.py` has a WHY comment citing upstream stubs gap.
- `print(..., file=sys.stderr)` for fail-fast credential errors — no stack traces leak; messages do not contain credentials.

## Out-of-scope changes
- `.env.example` (HIGH — file existed on `main`, plan said "creer si absent" but implementer rewrote whole file rather than appending). Must be reverted+appended.
- All other files in diff match `Files to touch` in plan. No other unintended scope creep.

## Lints / tests
- `cd scripts && uv run ruff check .` -> All checks passed.
- `cd scripts && uv run mypy .` -> Success, no issues found in 24 source files. (Note: mypy emits a non-fatal `unused section(s): module = ['structlog']` — tracked as LOW finding above.)
- `cd scripts && uv run pytest -v` -> 147 passed.
