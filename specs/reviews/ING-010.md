# Review — ING-010

## Verdict
APPROVE

Lints clean (`ruff` + `mypy --strict` exit 0), 78/78 tests pass, AC-1..AC-15 all covered, no security issues. Findings below are MED/LOW polish items, none blocking merge. The diff exceeds the 300-LOC vertical-slice budget but is driven by tests (~710 LOC) not src (277 LOC) and was already split from the broader Drive integration scope per plan; logged as LOW.

## Findings

| File:line | Severity | Pattern | Evidence | Suggested fix |
|---|---|---|---|---|
| scripts/gdrive_export/paths.py:30-33 | MED | incomplete collision handling | Second collision attempt (`<slug>-<id[:8]>`) is returned unconditionally; no check it is not also in `taken_paths`. AC-7 spec says "réessaye" — at minimum the suffixed path SHOULD also be guarded, or the function must document the single-retry guarantee | After computing `candidate` with suffix, assert `candidate not in taken_paths` and raise (or extend suffix). Add a test covering "both base and suffixed path taken" |
| scripts/gdrive_export/state.py:68-77 | MED | non-atomic write | `save_state` writes directly to `path` with `write_text`. A crash mid-write corrupts the state file. `.gitignore` reserves `scripts/.gdrive_state.json.bak` (AC-12) but no backup/atomic-rename code exists | Write to `path.with_suffix(".json.tmp")` then `Path.replace(path)`; optionally keep `.bak` of previous content |
| scripts/gdrive_export/frontmatter_merge.py:21-24 | MED | shared mutable default leak | Module-level `USER_MANAGED_DEFAULTS["tags"]` is a list; on line 50 `merged[key] = default` assigns that exact list reference. If a caller ever holds the merged dict and appends to `tags`, the module constant mutates | Deep-copy via `copy.deepcopy(default)` on insertion, or store an immutable sentinel and materialize per-call |
| scripts/gdrive_export/rename.py:14-19 | LOW | TOCTOU on destination | `new_path.exists()` then `old_path.rename(new_path)`. On POSIX `Path.rename` silently overwrites; the pre-check is racy. Single-process offline tool so practical risk is nil, but document it | Use `os.link` + `os.unlink` or open with `O_EXCL`; acceptable to leave as-is given offline single-process scope, but add a WHY comment noting POSIX overwrite semantics |
| scripts/tests/test_frontmatter_merge.py:79-86 | LOW | weak assertion vs plan-risk | `test_tags_yes_no_preserved` only asserts `len(parsed["tags"]) == 2`; does NOT detect the `yes`→`True` / `no`→`False` coercion the plan flagged as risk. Test passes today but won't catch a regression toward unwanted coercion (or, conversely, won't fail if pyyaml upgrade preserves the literal) | Assert exact round-trip semantics: `assert parsed["tags"] == [True, False]` (documents current behavior) or switch to quoted-form preservation test |
| scripts/tests/test_normalize.py:15-21 | LOW | spec coverage gap | AC-4 test oracle calls for "zero-width chars + NULL bytes + NFD séquences". Test covers NULL bytes + NFD but NOT zero-width (U+200B, U+200C, U+FEFF). Code does not strip them either (NFKC keeps them). Either spec or test should align | Either: (a) extend `normalize_body` to strip zero-width per W-T-1 cohérence, or (b) drop "zero-width" from AC-4 oracle wording. Current code matches spec text strictly, just diverges from oracle prose |
| docs/vision.md:52 | LOW | out-of-scope edit | Modified to reflect ING-010/011/012 split. Not in plan "Files to touch". Pure doc tweak, harmless | None — note in PR description that vision.md table was updated to reflect split |
| (whole diff) | LOW | LOC budget overrun | 987 LOC added excluding `uv.lock` (vs `vertical-slice.md` cap 300). Src = 277 LOC (within plan ≤200 estimate +38%); tests = ~710 LOC. Tests are not excluded by the rule | Acceptable given ticket is already a scope-split utilities-only slice; flag for awareness — ING-013 should not piggy-back additional surface |
| scripts/tests/test_rename.py:26-40 | LOW | env-dependent test setup | `_init_repo` calls bare `git init` without `--initial-branch=main`; relies on user's `init.defaultBranch`. AC-9 oracle reads `git status --porcelain` which is branch-agnostic, so test still passes, but future assertions on branch name would break | Pass `--initial-branch=main` for determinism |

## Spec coverage

- AC-1 : OK — `scripts/pyproject.toml` + `scripts/uv.lock` isolated; `uv sync` + `uv run pytest` green (verified locally, 78 passed).
- AC-2 : OK — `slugify.py:9-26` implements full pipeline; tests `test_slugify.py::TestSluggifyMatrix` cover matrix (Unicode, CJK, emoji, spaces, edge).
- AC-3 : OK — property tests `test_property_idempotence`, `test_property_length`, `test_property_alphabet` (200 examples each).
- AC-4 : OK — `normalize.py:10-17`; test class `TestNormalizeBody` covers NFKC + C0 strip + preserve `\n`/`\t` (gap: zero-width not tested, see LOW finding).
- AC-5 : OK — `state.py:11-77`; tests cover absent / corrupted / round-trip / archived_at None / hash determinism / sha256 known-vector.
- AC-6 : OK — `frontmatter_merge.py:31-51`; tests cover first export, re-export with user keys preserved, custom keys preserved, defaults created, invalid YAML, non-mapping root.
- AC-7 : OK — `paths.py:8-33` + `_assert_under_root`; tests cover basic, nested, collision, traversal `..`, slash-in-component (gap: see MED finding on second-collision guard).
- AC-8 : OK — `rename.py:6-19`, pure `Path.rename`, no `subprocess` in prod code (verified by `grep subprocess scripts/gdrive_export/` → exit 1).
- AC-9 : OK — `test_git_detects_rename_as_r` exercises real git repo, asserts `R` line in `git status --porcelain` (test passed locally).
- AC-10 : OK — `uv run ruff check .` and `uv run mypy .` exit 0 locally.
- AC-11 : OK — `.pre-commit-config.yaml:75-87` adds `scripts-ruff` and `scripts-mypy` hooks scoped `^scripts/.*\.py$`.
- AC-12 : OK — `.gitignore:90,93-94` adds `.gcp/`, `scripts/.gdrive_state.json`, `scripts/.gdrive_state.json.bak`.
- AC-13 : OK — `docs/adr/0006-gdrive-api-client.md` status `accepted`, date `2026-05-09`.
- AC-14 : OK — `scripts/tests/conftest.py:11-37` enforces grep at `pytest_sessionstart`; would fail fast on any forbidden import.
- AC-15 : OK — mypy strict passes; all public functions in `__init__.py` exports have docstrings; longest function = `load_state` at ~30 LOC (well under 40).

## Property invariants

`specs/properties.md` has no INV directly applicable to this utilities lib (no RAG-path invariant). Module-local property tests in `test_slugify.py` cover idempotence / length / alphabet, which is the right level for these primitives.

## Security

- No secrets detected (gitleaks clean).
- No SQL / no network / no `subprocess` in prod code — entire attack surface is local filesystem.
- Path traversal: mitigated via `slugify` per component (`..` → fallback `file-<id[:8]>`) plus `Path.is_relative_to(root)` assertion in `paths.py:36-42`. Tests `test_path_traversal_dotdot_slugified` + `test_path_traversal_slash_in_component_slugified` confirm escape attempts stay under root.
- Lockfile isolation per ADR-0006 honored: no Drive API deps in `scripts/pyproject.toml` (only `pyyaml` + dev tools); workers/ untouched.
- AC-14 import-grep guard active at test-session start.
- Secret hygiene: `.gitignore` extended; no `.env` / `.gcp/` content committed.
- No `unsafe`, no `unwrap`, no unsafe deserialization, no shell-mode subprocess. Test-only `subprocess.run([...], check=True)` uses argv arrays — no shell injection surface.
- `yaml.safe_load` used (NOT `yaml.load`) — safe deserialization confirmed.

## Out-of-scope changes

- `docs/vision.md:52` — single-line table update reflecting ING-010/011/012 split. Not listed in plan "Files to touch". Trivial doc; flag for transparency, not a blocker.
- `specs/acceptance/ING-011.md`, `ING-012.md`, `ING-013.md` — sister-ticket draft specs committed on this branch (separate `docs(spec):` commits, not in the impl commit). Spec authoring on a feature branch is unusual but not destructive; humans authored these in prior commits before the impl. Note: per CLAUDE.md "Sources de vérité humain-only", these were created in dedicated `docs(spec):` commits which is the human-authored pattern. Acceptable.
