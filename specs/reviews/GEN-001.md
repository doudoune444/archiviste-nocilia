# Review — GEN-001

## Verdict
REQUEST_CHANGES

## Findings

| File:line | Severity | Pattern | Evidence | Suggested fix |
|---|---|---|---|---|
| workers/src/archiviste_workers/services/llm.py:68,72,76,80,84 | HIGH | lint failure (PLC0415) | `uv run ruff check .` → 5 errors `import should be at the top-level of a file` on lazy provider imports inside `_build_chat_model`. Project ruff config selects `PL` group so PLC0415 is enforced. CI gate `ruff check` blocks merge. | Move imports to module top-level inside `if TYPE_CHECKING:`-guarded blocks for typing + actual imports at top, or add explicit `# noqa: PLC0415` per import with WHY comment justifying cold-start optimization. |
| workers/src/archiviste_workers/generate/router.py:40-41 | HIGH | spec violation (response shape) | `_http_error` returns `HTTPException(detail={"error": code})`. FastAPI serialises this as `{"detail": {"error": "..."}}`. Spec AC-11/17/18/19/21/22/23 mandates body literally `{"error": "..."}`. Tests at `test_generate_router.py:204,224,264` and `test_generate_validation.py:49,58,66,76,84` assert `r.json()["detail"]["error"]` — tests adapted to implementation, not spec. | Replace with custom exception handler returning `JSONResponse(status_code=..., content={"error": code})`. Update tests to assert `r.json()["error"]`. |
| workers/tests/test_generate_router.py:146 | HIGH | gaming pattern (tautological assertion) | `assert body["retrieve_ms"] + body["llm_ms"] <= max(body["retrieve_ms"] + body["llm_ms"], 1)` — always true (X <= max(X,1) for X≥0). AC-24 explicitly requires `retrieve_ms + llm_ms <= latency_ms` *as persisted in `query_log`*. Test does not query `query_log` nor compare to handler `latency_ms`. | Use `db_pool` fixture, fetch `latency_ms` from `query_log`, assert `body["retrieve_ms"] + body["llm_ms"] <= row["latency_ms"]`. |
| workers/src/archiviste_workers/services/llm.py:115-117 | MED | swallowed-error broadening | `except Exception as exc: ... raise LlmUpstreamError(...)` catches ALL non-Timeout exceptions (including `KeyError`, `AttributeError`, `ImportError`, programming bugs) and returns 502 `llm_upstream` to client. Programming errors get masked as upstream LLM failures, polluting logs/metrics and frustrating debugging. | Catch only the provider HTTP/SDK exception base classes (e.g. `httpx.HTTPError`, provider-specific `APIError`), let unexpected exceptions propagate as 500. |
| workers/tests/test_generate_router.py:69-71 | MED | weak assertion (AC-3) | `captured["json"] = httpx.Request(...).read()` stores raw bytes that are never decoded nor asserted. AC-3 mandates verifying outgoing body has `top_k=5` and `user_tier` propagated. Only `X-Request-Id` header is asserted. | `assert json.loads(captured["json"]) == {"query": ..., "top_k": 5, "user_tier": "anonymous"}`. |
| specs/acceptance/RET-001.md (added) | MED | out-of-scope change | Plan `Files to touch` lists only `specs/acceptance/GEN-001.md` + `specs/plans/GEN-001.md`. RET-001 spec is added in this diff although RET-001 was already merged on `main` (per ticket Pre-conditions). `specs/` are humain-only (CLAUDE.md). | Revert `specs/acceptance/RET-001.md` from this PR; restore via separate commit if it is genuinely missing on main. |
| workers (overall production diff) | MED | vertical-slice budget | Production LOC ≈ 779 (router 216 + llm 157 + conversation_client 62 + query_log 58 + others). `vertical-slice.md` budget = 300 LOC. Spec OQ-8 explicitly accepts >300 LOC, so allowed contractually, but reviewer notes the budget is exceeded by 2.6×. | Acknowledged via OQ-8. No action required, but consider whether `services/conversation_client.py` could be deferred. |
| workers/tests/test_llm_wrapper.py:73-80 | LOW | weak coverage (AC-9 grep) | Forbidden token list omits `"google"` and `"openai"` (only `"ChatOpenAI"` covered). Router currently mentions no provider so passes, but the guard would not catch a future regression like `if provider == "google"` in `router.py`. | Extend list: `("mistral","anthropic","google","openai","deepseek","ChatMistralAI","ChatAnthropic","ChatGoogleGenerativeAI","ChatOpenAI","ChatDeepSeek")`. |
| workers/src/archiviste_workers/generate/router.py:80 | LOW | UUID source not crypto-grade per security A02 | `str(uuid.uuid4())` for `conversation_id`. `security.md` A02 requires `secrets` module for tokens. `conversation_id` is an identifier, not an auth token, so technically out of scope, but worth flagging. | Acceptable for non-sensitive identifier; document if challenged. |
| workers/src/archiviste_workers/generate/router.py:78 | LOW | broad payload type | Handler accepts `payload: dict[str, Any]` rather than the typed Pydantic model directly. Required to drive custom 400/422 mapping (acceptable), but bypasses FastAPI's automatic validation. | Acceptable given AC-17/18/19 require precise error codes per field; but document the trade-off in a WHY comment at `_parse_request`. |

## Spec coverage

- AC-1: PARTIAL · `test_nominal_response_shape` covers shape but error response shape mismatch (see HIGH finding).
- AC-2: PARTIAL · `test_conversation_id_generated_when_null` only asserts `len(body["conversation_id"]) == 36`. Does not assert the `conversations` row was created BEFORE the response (spec wording: « AVANT de répondre »). Append happens before the return, but no DB-backed test verifies conversation row was created in time.
- AC-3: PARTIAL · header asserted, body fields not asserted (see MED finding).
- AC-4: COVERED · `test_zero_chunks_still_canon_with_marker` + nominal both check `mode == "canon"`.
- AC-5: COVERED · `<no_archives_found/>` asserted in `captured_messages[1].content`.
- AC-6: COVERED · `test_generate_prompt.py` (byte-for-byte SYSTEM_PROMPT). Includes OQ-5 trailing sentence.
- AC-7: COVERED · `test_generate_prompt.py` chunk-not-in-system assertion.
- AC-8: COVERED · 3 fail-fast tests in `test_llm_wrapper.py`. Note: AC-8 also requires fail-fast on missing `LLM_MODEL`; not tested explicitly (only missing `LLM_PROVIDER` and `LLM_API_KEY`). MISSING `LLM_MODEL` case.
- AC-9: COVERED but weak (see LOW finding on grep tokens).
- AC-10: NOT TESTED in repo (no `grep` test against `.env.example`). Manual check confirms `.env.example` content matches.
- AC-11: PARTIAL · `test_llm_timeout_504` uses synthetic `LlmTimeoutError`; does not exercise actual `asyncio.wait_for(timeout=30)` path. Acceptable for unit, but no integration test ensures wall-clock 30s gate.
- AC-12: COVERED · `test_generate_parser.py` (verified 47 tests pass).
- AC-13: COVERED · `test_no_citation_log_warn`.
- AC-14: COVERED · `test_conversation_log_failure_does_not_break_response`. Test only asserts ALERT + 200; does NOT verify both POSTs (user + assistant) were attempted in sequence.
- AC-15: COVERED · `test_query_log_inserted_on_success` (DB-backed). Does not assert `query_text` value matches request.
- AC-16: COVERED · `test_generate_pricing.py`.
- AC-17: COVERED.
- AC-18: COVERED.
- AC-19: COVERED.
- AC-20: COVERED · injection_filter unit, but no integration test verifying the prefix actually reaches the LLM HumanMessage (AC-20 (a) requires `[user query, suspected injection]: ` prefix in payload — only unit-tested in `prompt.py`, not e2e via router).
- AC-21: COVERED.
- AC-22: COVERED.
- AC-23: COVERED.
- AC-24: NOT COVERED — tautological assertion (HIGH finding).
- AC-25: COVERED · 5 provider unit tests for `extract_usage`.

## Property invariants
- INV-1..INV-8 from `specs/properties.md` are not impacted by GEN-001 (plan §"Property" is correct).
- No new property test required.

## Security
- Secrets: `LLM_API_KEY` typed `pydantic.SecretStr` ✓ (settings.py:38, llm.py:60). `.env.example` placeholder empty ✓. No hardcoded credentials in tests.
- SQL: parameterized `asyncpg` `$1..$10` placeholders ✓ (`query_log.py:33-39`). Cast to `::uuid` server-side, no string concat.
- Path traversal: N/A — no filesystem path constructed from user input.
- SSRF: `WORKERS_INTERNAL_BASE_URL` and `CONVERSATION_INTERNAL_BASE_URL` are operator-controlled env, not user-supplied. `follow_redirects=False` set on shared `httpx.AsyncClient` ✓. No allowlist needed.
- Trust boundary: `user_tier` taken from request body — endpoint is internal-only (per spec/Non-goals), so trusts upstream gateway. Acceptable phase 1; flagged in spec security section as W-S-1 deferred.
- Prompt injection: `<retrieved_chunks>` confined to `HumanMessage`, never `SystemMessage` ✓ (prompt.py:36, AC-7). System prompt contains anti-injection clause ✓ (AC-6 verbatim).
- Embedding poisoning: out of scope (ingestion side, ING-001).
- LLM output XSS: workers returns plain string, gateway responsibility per spec Non-goal "output sanitization".
- Provider error leak: `HTTPException(detail={"error":"llm_upstream"})` body returned to caller; `str(exc)` only in `RetrieveError.__init__` (not exposed to client) ✓. Provider exception text NOT serialized into client response. Compliant AC-21.
- Rate limit: deferred to gateway public endpoint (worker is internal-only).
- Logging: no `query`, `answer`, or chunk text logged (only counts, IDs, latencies) ✓. AC-15's `query_text` is persisted in DB `query_log` (mandated by spec, not application log).

## Out-of-scope changes
- `specs/acceptance/RET-001.md` (new file) — not in plan `Files to touch`. RET-001 was already merged per ticket Pre-conditions; this file should not appear in the GEN-001 diff. Revert or split into a separate commit/PR.

## Summary

3 HIGH, 5 MED, 3 LOW. The HIGH ruff failure will block CI. The HIGH error-response shape mismatch is a verifiable spec violation that tests were adapted to mask. The HIGH AC-24 tautology is a gaming pattern. Address all three before a re-review; MED items are required this PR.
