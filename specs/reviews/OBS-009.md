# Review — OBS-009

Branch `feat/OBS-009-eval-job` @ `084ea09`. Diff = `git diff main...HEAD` from worktree.
Lints/tests run in worktree: `ruff check .` PASS, `mypy . --strict` PASS (29 files), `pytest tests/test_fetch_golden.py` PASS (4).
Terraform binary not installed → TF validated by static cross-reference against existing `.tf` files (every referenced resource/local confirmed to exist).

## Verdict
APPROVE

## Findings

| File:line | Severity | Pattern | Evidence | Suggested fix |
|---|---|---|---|---|
| eval/fetch_golden.py, eval/pyproject.toml, infra/docker/eval.Dockerfile | LOW | plan deviation (not spec violation) | Plan OQ-3/R1 prescribed inline `httpx`+`google-auth` fetch and listed `eval/*` + Dockerfile as "non touchés"; impl instead added a tested `fetch_golden.py` module + `google-cloud-storage>=2.18` dep + 1 Dockerfile comment. Spec Touch points do NOT list pyproject/Dockerfile as non-touch, so no AC/spec is violated. Chosen path is cleaner and avoids the no-workaround risk the plan flagged. | Acknowledge deviation in PR description; no code change needed. |
| (diff size) | LOW | scope size | 336 LOC excluding generated `uv.lock` (vertical-slice.md excludes generated). Over the 300 nominal cap; runbook (70) + TF (104) dominate. | Acceptable for this slice; note only. |

No HIGH or MED findings.

## Gaming-pattern audit (explicit rule-out)
- Hardcoded test values / match-on-test-input: NONE. `fetch_golden` is generic over bucket/object/dest/timeout.
- Stub returns: NONE. `main()` returns 0 only after `fetch_golden` actually runs; `download_to_filename` is invoked.
- Swallowed errors: NONE. No try/except in `fetch_golden.py`; GCS errors propagate → SystemExit non-zero (no-workaround honored). Verified by `test_fetch_golden_propagates_gcs_error`.
- Disabled/skipped tests: NONE.
- Vacuous test? NO. `test_fetch_golden_calls_gcs_correctly` asserts `.bucket("my-bucket")`, `.blob("golden/...")`, `.download_to_filename(dest, timeout=15)` — would fail on arg swap or dropped timeout. Default-timeout test pins 30. SDK contract verified live: `Blob.download_to_filename` really accepts `timeout` kwarg, so the mock matches reality (not gaming).

## Ticket-specific risks (ruled in/out)
1. AC coverage — all ACs have honest oracles, all operator-side (one-shot prod execution). None faked, none silently unmet. See AC table below.
2. Terraform correctness vs ingest mirror:
   - `local.ar_base` (cloud_run.tf:5), `google_cloud_run_v2_service.workers.uri` (cloud_run.tf:86 / outputs.tf:8), `google_service_account.archiviste_runtime` (iam.tf:9), `google_sql_database_instance.archiviste_db` (cloud_sql.tf:3), `google_secret_manager_secret.mistral_api_key` (secrets.tf:3), `var.region` (variables.tf:6) — ALL exist. No dangling reference → `terraform plan` would not fail on unresolved refs.
   - DATABASE_URL `%40`-encoding (`replace(trimsuffix(...),"@","%40")`), `postgresql+asyncpg://...@/archiviste?host=/cloudsql/<conn>` form: byte-identical to ingest Job (cloud_run_job.tf:80). AC-6 met.
   - cloudsql volume + `volume_mounts /cloudsql`, `INSTANCE_CONNECTION_NAME`, `CLOUD_SQL_IAM_AUTH=true`: present, mirror ingest. AC-6 met.
   - `max_retries = 0`: present. `lifecycle ignore_changes = [template[0].template[0].containers[0].image]`: present, correct nesting matches ingest.
   - `secret_key_ref` for Mistral key → injected as env `LLM_API_KEY` (NOT `MISTRAL_API_KEY`). Verified correct: `eval/metrics.py:111` reads `LLM_API_KEY`. Var name is intentional and accurate.
   - image tag `${local.ar_base}/eval:latest`: correct (deploy.yml pushes eval:latest per OBS-008).
3. Two-step bash command (eval_job.tf:46-49): `/bin/bash -c "fetch && run"`. `&&` short-circuits → fetch failure aborts before eval run (failure mode "golden absent → no row" holds). Quoting sound (single double-quoted string, no nested-quote breakage; `${...uri}` is a TF interpolation resolved at apply, not a shell var). `/bin/bash` present in `python:3.12-slim` (Debian slim ships it; ingest Job uses the same `/bin/bash` successfully). `/tmp` writable in Cloud Run container (tmpfs). Runner flags `--mode/--persist/--set/--output/--workers-url` all exist (ragas_runner.py:60-71). `eval.fetch_golden` importable: `eval/__init__.py` present + Dockerfile `PYTHONPATH=/app`.
4. fetch_golden.py: A04 30s default timeout present + passed to SDK. A09 logs bucket/object/dest/timeout only — no credentials. No bare-except. mypy --strict clean. ruff T20 clean (no print, uses structlog). Test is honest (see gaming audit).
5. Dependency hygiene: `google-cloud-storage>=2.18` pinned via `eval/uv.lock` (lines 206-207 in both `live` and `dev` extras; full pkg entry line 769). `uv lock --check` resolves clean (143 pkgs). Dockerfile has ZERO `uv pip install` — install is the pre-existing `uv sync --frozen --extra live --no-dev`. Reproducible build (A08) intact. Dep present in both live (runtime image) and dev (PR gate) extras.
6. Golden privacy: `specs/golden_qa.jsonl` gitignored (.gitignore:105), `git ls-files` confirms NOT tracked. Diff commits nothing under specs/golden. Bucket `archiviste-lore-corpus` private (`public_access_prevention=enforced`). No exposure.
7. Out-of-scope creep: NONE. No Cloud Scheduler/cron, no GHA exec (the `run jobs execute` strings live only in runbook + TF comments, NOT in `.github/workflows/`), no quality.rs change, no Gate A / baseline re-bake, no migration/openapi change, no new IAM member/binding in diff.

## Spec coverage
- AC-1 (Job exit 0): ✓ oracle = operator `gcloud run jobs execute --wait` (runbook §b). Command chains fetch+runner; runner persists. Honest, operator-verifiable.
- AC-2 (count +1 live): ✓ oracle = runbook §c before/after `SELECT count(*)`. Single execution = single persist (no idempotence loop). Honest.
- AC-3 (runner_mode='live', finished_at non-null): ✓ oracle = runbook §c `ORDER BY finished_at DESC LIMIT 1`. `--mode live --persist` drives it.
- AC-4 (GET /v1/quality ≠ no_data, finished_at matches): ✓ oracle = runbook §d `curl | jq`. quality.rs unchanged (reads live row) — correct, no code needed here.
- AC-5 (golden from gs://.../golden_qa.jsonl, sha256 match): ✓ command fetches that exact path; runbook §a documents upload + sha256 verify. Operator invariant, not CI.
- AC-6 (Cloud SQL IAM authn, no plaintext pwd): ✓ DATABASE_URL IAM-auth form + cloudsql volume, mirror ingest. No password anywhere.
- AC-7 (TF command frozen live+persist, no GHA): ✓ command literal present; grep of `.github/workflows/` = no `run jobs execute`.
- AC-8 (no new IAM role/binding): ✓ `git diff` shows zero `*_iam_member`/`iam_binding`; runtime already holds objectViewer (storage.tf:34), cloudsql.client/instanceUser (iam.tf:41-42), run.invoker on workers (cloud_run.tf:287).

## Property invariants
- None in `specs/properties.md` apply (infra/operational slice, no algorithmic invariant). Plan confirms. Not a gap.

## Security
- Secrets: none in diff. Mistral key via `secret_key_ref` → env `LLM_API_KEY` (read as `pydantic.SecretStr` in metrics.py). DATABASE_URL IAM-auth, no plaintext password.
- SSRF: golden fetch URL is hardcoded bucket/object (not user-supplied); workers URL is TF-resolved service URI, not user input. N/A.
- A04: 30s timeout on the GCS download. A08: frozen lock. A01: least-privilege, zero new grant.
- A09: fetch_golden logs no credentials.

## Out-of-scope changes
- `eval/fetch_golden.py`, `eval/tests/test_fetch_golden.py`, `eval/pyproject.toml`, `eval/uv.lock`, `infra/docker/eval.Dockerfile`: not in the PLAN's "Files to touch", but NOT forbidden by the SPEC's Touch points (which only bars runner code, openapi, migrations, iam.tf, storage.tf). Spec is source of truth over plan. Deviation is quality-positive (tested SDK module vs untested inline one-liner). LOW note, not a blocker.
