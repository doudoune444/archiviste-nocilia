# Review SEC-003 — verdict APPROVE

## Summary

Implémentation minimale et conforme au plan : ajout d'une constante `HSTS_VALUE` + un 5ème `SetResponseHeaderLayer::if_not_present` dans le bloc `.layer(...)` racine de `gateway/src/lib.rs`, extension du helper `assert_security_headers` à 5 headers + assertion de non-duplication `get_all().count() == 1`, et 5 nouveaux tests d'intégration couvrant `/healthz` (200 mocké + workers-down), body-limit 400, 404 asset, 404 router fallback. Aucune nouvelle crate, aucun nouveau middleware custom. Diff ~141 LOC. `cargo fmt --check`, `cargo clippy -- -D warnings` (all-targets), `cargo test` tous verts (16/16 sur `static_test`, suite complète OK).

## AC coverage

| AC | Test | Status |
|---|---|---|
| AC-1 (HSTS sur toute réponse) | helper `assert_security_headers` appelé dans 9 tests | OK |
| AC-2 (4 headers UI-001 préservés) | helper assertions littérales + tests AC-6/AC-17 verts | OK |
| AC-3 `/healthz` 200 | `sec003_healthz_200_has_headers` (mockito) | OK |
| AC-3 `/healthz` "5xx" workers-down | `sec003_healthz_workers_down_has_headers` (200 degraded, déviation spec documentée dans plan §Risks) | DOC |
| AC-4 body > 1 MiB → 400 | `sec003_chat_body_limit_400_has_headers` | OK |
| AC-5 404 ServeDir | `sec003_unknown_asset_404_has_headers` | OK |
| AC-5 404 router fallback (spec "405-or-404") | `sec003_unknown_route_404_has_headers` (404, plan §Risks) | OK |
| AC-6 non-duplication | boucle `get_all(name).iter().count() == 1` sur 5 noms dans helper | OK |
| AC-7 `SetResponseHeaderLayer::if_not_present` router-wide, pas de crate | `gateway/src/lib.rs:130-133`, `Cargo.toml` inchangé | OK |
| AC-8 helper étendu + const `HSTS_VALUE` + tests UI-001 inchangés | `static_test.rs:54, 59-115`, 4 tests UI-001 verts | OK |
| AC-9 5 nouveaux tests | `static_test.rs:360-456` | OK |
| AC-10 CI vert, no static/handlers/migrations | fmt/clippy/test verts, diff stat = `CHANGELOG.md` + `lib.rs` + `static_test.rs` uniquement | OK |

## Findings

### HIGH
Aucun.

### MED
Aucun.

### LOW

| File:line | Pattern | Evidence | Fix suggéré |
|---|---|---|---|
| `gateway/tests/static_test.rs:360-377` (`sec003_healthz_workers_down_has_headers`) | spec deviation documentée | AC-3 dit "échec amont workers retournant 5xx", mais `handlers/health.rs:38` retourne 200 `degraded` quel que soit l'état workers. Le test assert `StatusCode::OK`, pas 5xx. La déviation est explicitement reconnue dans le plan §Risks (ligne 49) et dans le docstring du test. | Aucun changement requis dans SEC-003 ; le scope du changement de comportement `/healthz` est explicitement out-of-scope. Si l'humain veut un vrai 5xx, ouvrir un ticket séparé `/healthz` status code. |
| `gateway/tests/static_test.rs:446-456` (`sec003_unknown_route_404_has_headers`) | spec "405-or-404" partielle | AC-5 dit "404 router fallback (`GET /chemin/inexistant` 405-or-404)". Axum 0.8 sans `fallback()` rend 404 — testé. Le cas "405 méthode mismatch" (ex. `POST /healthz`) n'est pas couvert par un test dédié, mais le helper `assert_security_headers` est sémantiquement uniforme et les `SetResponseHeaderLayer` racine wrap tout, donc le risque est nul en pratique. | Optionnel : ajouter un test `sec003_method_not_allowed_has_headers` (ex. `PUT /healthz`) pour fermer la couverture 405. Non-bloquant. |
| `gateway/src/lib.rs:122` | commentaire stale | Le commentaire ligne 122 dit encore `// Root router: merge API + static, then apply global security headers (AC-6).` — AC-6 référence UI-001, mais le bloc applique désormais 5 headers (SEC-003 AC-1+AC-2). | Mettre à jour pour mentionner SEC-003 (cosmétique). |

## Security

- Aucun secret introduit. `gitleaks` patterns non déclenchés.
- HSTS valeur exacte alignée sur `.claude/rules/security.md` §A02 ligne 14.
- `if_not_present` sémantique : un handler aval qui émettrait `Strict-Transport-Security` divergent verrait sa valeur conservée → couvert par AC-6 (non-duplication assertée).
- Aucun handler/middleware actuel n'émet de header parmi les 5, vérifié par succès de tous les tests.
- Pas d'élargissement de surface d'attaque, pas de nouveau path d'entrée.
- A02/A05 baseline complétée per spec.

## Out-of-scope changes

Aucune. Diff strictement limité aux fichiers listés dans `specs/plans/SEC-003.md` §Files to touch :
- `gateway/src/lib.rs` : +6 LOC (constante + layer)
- `gateway/tests/static_test.rs` : +~125 LOC (helper + 5 tests)
- `CHANGELOG.md` : +4 LOC (entrée `### Security` sous `## [Unreleased]`)

Pas de modification `Cargo.toml`, `gateway/static/`, `gateway/src/handlers/`, `migrations/`, ou workers Python.

## Lints & tests

- `cargo fmt --check` : vert.
- `cargo clippy --all-targets -- -D warnings` : vert (`Finished dev profile`, zéro warning).
- `cargo test` : vert (16/16 sur `static_test`, suite complète OK).
- `Cargo.toml` / `Cargo.lock` : inchangés.

## Verdict
APPROVE

Implémentation propre, conforme au plan ligne par ligne, AC-1 à AC-10 couverts par tests d'intégration verts, non-duplication explicitement asserted, aucun gaming, aucun secret, aucun écart hors-scope. Les deux LOW (déviation `/healthz` 200-not-5xx, absence de test 405) sont documentés dans le plan §Risks et explicitement out-of-scope SEC-003. Le commentaire stale ligne 122 est cosmétique. Mergeable en l'état.
