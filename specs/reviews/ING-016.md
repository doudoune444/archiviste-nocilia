# Review — ING-016

## Verdict
APPROVE

## Lints / tests (local, from `workers/`)
- `uv run ruff check .` → All checks passed.
- `uv run mypy src/` → Success, no issues (45 files).
- `uv run pytest tests/test_chunker.py` → 15 passed.
- `uv run pytest` (full) → 14 failed / 11 errored, all `asyncpg.UndefinedTableError: relation "conversations" does not exist` (no migrated Postgres in env). Pre-existing infra failures; this diff touches no DB code. Chunker slice is fully green.

## Findings

| File:line | Severity | Pattern | Evidence | Suggested fix |
|---|---|---|---|---|
| infra/docker/workers.Dockerfile:8-10 | LOW | wheel force-include untested by image build | Image installs via `COPY src/` + `uv sync` (source/editable), never builds the wheel. The `[tool.hatch.build.targets.wheel.force-include]` in pyproject.toml:60-61 is correct but never exercised by the actual build path. Asset ships because `COPY src/` carries the `.json`, not because of force-include. | None required for AC-3 (asset is present at runtime, verified). Optionally add a packaging smoke test (`uv build` + assert json in wheel) if a wheel-based deploy is ever introduced. |
| workers/tests/test_chunker.py:163-178 | LOW | weak overlap oracle | `test_adjacent_chunks_share_overlap` asserts only character-substring overlap exists between adjacent chunks, not a token-measured ~64 overlap. | In-scope per plan (overlap = "non-zero where split", not exact 64). Accept; no change required. |
| workers/tests/test_chunker.py:167-168 | LOW | conditional skip | `pytest.skip("not enough chunks to test overlap")` — defensive branch unreachable for the 300x fixture (always splits). Not a CI bypass. | Harmless; could drop the guard. |
| workers/src/.../chunker.py:9 / tests:20 | LOW | blanket `type: ignore[import-untyped]` | `tokenizers` has no stubs; targeted ignore is acceptable per project convention. Not a `no-workaround` violation (lib genuinely lacks types, not an error suppression). | None. |

No HIGH/MED findings.

## Spec coverage
- AC-1 (no `transformers` import): ✓ `test_chunker_source_has_no_transformers_import`, `test_transformers_not_in_sys_modules_after_chunker_import`. Grep of `workers/src` for `transformers|AutoTokenizer|from_pretrained` → zero matches. `transformers` not even installed in resolved venv (`importlib.util.find_spec('transformers') is None`).
- AC-2 (Mixtral-8x7B-v0.1 via `tokenizers`, no new runtime dep): ✓ `test_tokenizer_identity_vocab_size` (vocab==32000), `test_tokenizer_identity_eos_token_id` (`</s>`→id 2), `test_no_new_runtime_dep_in_pyproject`. Verified independently: asset model.type=BPE, vocab len=32000, added_tokens {0:`<unk>`,1:`<s>`,2:`</s>`}. `tokenizers` confirmed transitive in uv.lock (langchain-mistralai dep), not a direct dep. Identity pin genuinely pins Mistral SP-BPE family (32000 + EOS 2), not "a tokenizer loads".
- AC-3 (network-free init, vendored artifact): ✓ `test_build_chunker_is_network_free` (blocks `socket.socket`), `test_build_chunker_calls_from_file_not_from_pretrained` (asserts `from_file` called, `from_pretrained` forbidden). Note: socket block is Python-level; a Rust-side libc fetch wouldn't be caught by it — but the companion `from_pretrained`-forbidden test closes that path. Adequate. Missing-artifact `RuntimeError` branch (chunker.py:24-25) is present but not directly tested (failure-mode, not an AC).
- AC-4 (chunk_size=512 / overlap=64 in Mixtral tokens, separators order): ✓ `test_each_chunk_within_token_limit` (each chunk ≤512 Mixtral tokens, guarded by `len>=2`), `test_chunker_constants_match_spec`, `test_chunker_short_text_single_chunk`, `test_chunks_are_non_empty_and_ordered`, `test_adjacent_chunks_share_overlap`. `length_function=lambda text: len(tokenizer.encode(text).ids)` uses Mixtral token ids; ≤512 cap proven on a multi-chunk fixture. No BGE-M3 comparison (correct per non-goal).
- AC-5 (`transformers` absent from all dep tables): ✓ `test_transformers_absent_from_all_dep_tables`. pyproject diff removes runtime dep + both mypy overrides. uv.lock grep for transformers/sentence-transformers/torch/safetensors → zero matches.
- AC-6 (`embedder-fallback` block + `sentence-transformers` removed): ✓ `test_embedder_fallback_block_absent`, `test_sentence_transformers_absent_from_all_tables`. Block deleted in pyproject diff.
- AC-7 (image ≥ -50 MB): not automatable; CHANGELOG records "to be verified at build time"; commit msg claims torch/safetensors/transformers/sentence-transformers removed. uv.lock confirms those four left the graph (consistent with ≥50 MB). Manual build delta = human/CI step, not gameable in pytest. Accept as out-of-pytest per oracle.
- AC-8 (Ragas standing gate stays green): not run locally (eval gate = CI). Tokenizer swap affects new ingestion chunking only; existing corpus untouched (non-goal). No new threshold introduced. Defer to CI eval gate.

## Property invariants
- `specs/properties.md`: no invariant maps to the chunker tokenizer swap. None required.

## Security
- No secrets detected. Vendored `tokenizer.json` = public vocabulary data (Apache-2.0), no entropy of a secret. `check-added-large-files` exclude scoped narrowly to `assets/*.json` (.pre-commit-config.yaml:19) — does not weaken secret scanning (gitleaks/detect-private-key unaffected).
- No network added to chunker init (AC-3) — consistent with W-E-1.
- No `os.getenv(..., default)` / silent fallback. Missing artifact → explicit `RuntimeError`, no network fallback (consistent no-workaround + secret-hygiene).
- No SQL / SSRF / deserialization / CORS / JWT surface in this diff.

## Gaming-pattern audit
- No hardcoded test values / match-on-test-input in `chunker.py`.
- No stub returns: `build_chunker` builds a real splitter from the real tokenizer.
- No swallowed errors: `from_file` failure propagates; missing asset raises.
- No disabled/skipped tests for CI (single conditional skip is a defensive guard, unreachable).
- Identity pin is non-trivial (vocab 32000 + EOS id 2), not "any tokenizer".

## Out-of-scope / file-touch audit
- `specs/acceptance/ING-001.md` modified — flagged but OK: it is a separate human-authored `docs(spec)` commit (94bfcf2), NOT in impl commit 78707de. It records the supersession ING-001 AC-6/decision-108 → ING-016 exactly as ING-016 spec mandates. Human-only file edited by human step. No agent overreach.
- All impl-commit files are within the plan's "Files to touch" except `infra/docker/workers.Dockerfile` (plan listed it as "verify wheel install includes it" — verified, no edit needed) and a 1-line `.pre-commit-config.yaml` exclude (justified: vendored 1.75 MB artifact vs `--maxkb=1024`). Both justified.
- Non-generated diff: +215 / -39 LOC (excl. uv.lock, vendored json, specs) — under 300 vertical-slice cap.

## Notes for shipper
- AC-7 (image delta) and AC-8 (Ragas gate) are CI/manual gates not reproducible in this review env; both must be green in CI before merge. Local evidence (uv.lock dropped torch/safetensors/transformers/sentence-transformers) strongly supports AC-7.
