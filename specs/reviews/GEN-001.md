# Review — GEN-001 (post-fix `d75000f`)

## Verdict
APPROVE

## Scope of this re-review

Adversarial re-review of fix commit `d75000f fix(workers,infra): GEN-001 — review HIGH+MED (compose defaults, lifespan test guard)`. Two narrow changes (11 insertions / 4 deletions across 2 files):

1. `docker-compose.yml:48-50` — `${LLM_PROVIDER:?...}` / `${LLM_MODEL:?...}` / `${LLM_API_KEY:?...}` softened to `${VAR:-}` empty defaults. Process-level fail-fast in `LlmClient.from_env()` remains the single source of truth for AC-8.
2. `workers/tests/test_main_lifespan.py` — skip exception narrowed from `(OSError, ConnectionError, RuntimeError)` to `(OSError, ConnectionError)`. Three `monkeypatch.setenv` lines added for `LLM_PROVIDER`, `LLM_MODEL`, `LLM_API_KEY` with WHY comment explaining the `LlmConfigError ⊂ RuntimeError` masking.

## Local CI gates

| Gate | Result |
|---|---|
| `uv run ruff check .` (workers/) | All checks passed |
| `uv run mypy src/` (workers/) | Success: no issues found in 38 source files |
| `uv run pytest` (workers/, full suite) | 135 passed, 1 skipped (only the expected `test_conversation_integration.py` GCS-emulator skip) |
| `REDIS_PASSWORD=test docker compose config workers` | exit 0, all `LLM_*` resolve cleanly without interpolation error |

## Verification of HIGH (compose interpolation) fix

| Aspect | Status | Evidence |
|---|---|---|
| `${LLM_PROVIDER:?...}` removed | OK | `docker-compose.yml:48` is now `LLM_PROVIDER: ${LLM_PROVIDER:-}` |
| `${LLM_MODEL:?...}` removed | OK | `docker-compose.yml:49` is now `LLM_MODEL: ${LLM_MODEL:-}` |
| `${LLM_API_KEY:?...}` removed | OK | `docker-compose.yml:50` is now `LLM_API_KEY: ${LLM_API_KEY:-}` |
| `docker compose config workers` no longer requires `LLM_*` | OK | Reproduced locally with only `REDIS_PASSWORD=test` set; exit 0; no `required variable LLM_* is missing a value` error. |
| AC-8 process fail-fast preserved | OK | `LlmClient.from_env()` raises `LlmConfigError("LLM_PROVIDER missing or invalid …")` on empty env; covered by `test_llm_wrapper.py::test_from_env_*` (15 tests in that file all green). |
| `boot-sla.yml` jobs `measure` + `stack-integration` will succeed | OK | Both write a `.env` with `REDIS_PASSWORD` only; with `:-` defaults, compose interpolation succeeds, the worker container boots empty `LLM_*`, fails-fast at process level — but that path is only triggered if the smoke tests bring up `workers`. The compose-only invocations (`config`, `--services`, `build`) no longer abort. |
| `.env.example` consistent | OK | Already carries `LLM_PROVIDER=mistral`, `LLM_MODEL=mistral-small-latest`, `LLM_API_KEY=` (empty placeholder, secret-hygiene compliant). |

## Verification of MED (lifespan test masked-skip) fix

| Aspect | Status | Evidence |
|---|---|---|
| Skip exception narrowed | OK | `test_main_lifespan.py:57` is now `except (OSError, ConnectionError) as exc:` — `RuntimeError` removed. |
| `LLM_*` env populated in test | OK | Lines 45-47 add `monkeypatch.setenv("LLM_PROVIDER", ...)`, `LLM_MODEL`, `LLM_API_KEY` mirroring the existing `DATABASE_URL` / `GCS_EMULATOR_HOST` pattern. |
| WHY comment justifying the env injection | OK | Lines 41-44 explain `LlmConfigError ⊂ RuntimeError` and that fail-fast is tested separately in `test_llm_wrapper.py`. Satisfies clean-code.md "WHY comment when non-obvious". |
| Test now actually runs (no silent skip) | OK | Local `pytest tests/test_main_lifespan.py -v` → `PASSED [100%]`, `1 passed in 15.39s`. Full suite shows `tests\test_main_lifespan.py .` (dot, not `s`). The RET-001 pgvector regression is again exercised in CI. |
| `LlmConfigError` no longer swallowed | OK | Verified `LlmConfigError.__mro__ == [LlmConfigError, RuntimeError, Exception, BaseException, object]`. The narrowed `except (OSError, ConnectionError)` will not catch it, so a future config regression would fail loud, not skip silent. |
| Test placeholders not real credentials | OK | `LLM_API_KEY=test-key-not-used` — non-secret literal; `LlmClient.from_env()` accepts the format and never makes a live call in this test (lifespan only initializes the client, doesn't invoke the LLM). Matches secret-hygiene.md "Tests: use fixtures, never real credentials". |

## New issues introduced by `d75000f`

None detected. The fix is minimal (2 files, 11 insertions / 4 deletions), surgical (addresses each finding 1:1), and introduces no new code paths.

Sanity checks performed:
- No new secrets / hardcoded credentials (only `test-key-not-used` literal in test, justified).
- No new error swallowing (the narrowed `except` is the opposite of swallowing).
- No out-of-scope edits (`d75000f` touches exactly the two files cited in prior review).
- No `unwrap`/`unsafe`/`verify=False`/format-into-SQL/CORS-wildcard.
- No spec drift (acceptance, plans, openapi, golden_qa, properties, migrations untouched).
- Diff size remains within the OQ-8-acknowledged exception (no further growth).

## Status of carry-over findings (unchanged by `d75000f`)

| Prior carry-over | Severity | Status |
|---|---|---|
| `services/llm.py:108` — `except Exception` broadening | MED | STILL PRESENT — out of scope for this fix commit; acceptable per OQ-8 acknowledgement and slated for follow-up ticket. |
| `test_generate_router.py:69-71,127,146` — outgoing body fields not asserted (AC-3 partial) | MED | STILL PRESENT — carry-over. |
| Vertical-slice >300 LOC | MED | STILL PRESENT — OQ-8 acknowledged exception. |
| `test_llm_wrapper.py:73-80` — AC-9 grep token list incomplete | LOW | STILL PRESENT. |
| `generate/router.py:78,98` — `dict[str, Any]` without WHY comment | LOW | STILL PRESENT. |
| `test_generate_router.py:215-230` — AC-2 DB-row not asserted | LOW | STILL PRESENT. |
| `test_generate_router.py:341-356` — AC-14 call-count not asserted | LOW | STILL PRESENT. |
| `test_llm_wrapper.py` — missing-`LLM_MODEL` fail-fast not tested (AC-8 partial) | LOW | STILL PRESENT. |

Carry-overs are unchanged. None block merge per prior review acceptance.

## Findings table

| File:line | Severity | Pattern | Evidence | Suggested fix |
|---|---|---|---|---|
| (none new) | — | — | — | — |

Both prior blocking findings (HIGH compose, MED masked-skip) are resolved. No new findings introduced by `d75000f`.

## Spec coverage delta

Unchanged from previous review. AC-1, AC-4..AC-7, AC-10..AC-13, AC-15, AC-16, AC-19, AC-20, AC-22, AC-23, AC-24, AC-25 covered. AC-2, AC-3, AC-8, AC-9, AC-14 partial (low-severity carry-overs). AC-17, AC-18, AC-21 covered.

The lifespan test fix actually *strengthens* AC coverage indirectly: RET-001 INV (pgvector codec on prod pool) is no longer silently uncovered when GEN-001 lands.

## Property invariants

INV-1..INV-8 unchanged. INV-9 / INV-10 (RET-001) are now actively re-enforced in CI by `test_main_lifespan.py` running instead of skipping.

## Security

- No new secrets in `d75000f` (verified `git show d75000f`).
- `LLM_API_KEY` still typed `SecretStr` in production code.
- `test-key-not-used` test placeholder is non-functional (not used to make live calls); secret-hygiene.md compliant.
- No CORS / CSP / SSRF / SQL / JWT / unsafe-deserialization changes.

## Out-of-scope changes

None. `d75000f` touches only the two files explicitly cited in the prior review.

## Summary

`d75000f` cleanly resolves both blocking findings from the prior review:

1. **HIGH (compose CI breakage)** — `${LLM_*:?...}` → `${LLM_*:-}`. `docker compose config workers` now succeeds with only `REDIS_PASSWORD` set, so `boot-sla.yml`'s `measure` and `stack-integration` jobs no longer abort at compose interpolation. AC-8 fail-fast is preserved at the worker process level (`LlmClient.from_env()`), which is the spec-mandated location.

2. **MED (silent test skip masking RET-001 regression)** — `(OSError, ConnectionError, RuntimeError)` → `(OSError, ConnectionError)`, plus `monkeypatch.setenv` for the three `LLM_*` vars with a clear WHY comment. The pgvector codec regression test now executes (verified locally: `PASSED [100%]`), and any future `LlmConfigError` will fail loud rather than skip silent.

The fix is surgical, introduces no new findings, no new lints, no new test failures, and no new secrets. Carry-over MED/LOW findings remain unchanged and were already accepted under OQ-8.

**Verdict: APPROVE.** Ready to merge.
