# Review — GEN-001 (re-review after fix `3365f3f`)

## Verdict
APPROVE

## Status of prior HIGH findings

| Prior HIGH | Status | Evidence |
|---|---|---|
| ruff PLC0415 (lazy provider imports inside `_build_chat_model`) | FIXED | `services/llm.py:11-15` — provider imports moved to module top-level. `uv run ruff check .` → `All checks passed!`. |
| Error response shape `{"error":...}` (AC-11/17/18/19/21/22/23) | FIXED | `generate/router.py:41-52` — `_GenerateError` sentinel + `JSONResponse(content={"error": code})`. Tests at `test_generate_router.py:246,266,306` and `test_generate_validation.py:49,58,66,76,84` now assert `r.json() == {"error": "..."}` directly. |
| AC-24 tautological assertion | FIXED | `test_generate_router.py:151-191` — new `test_timings_consistent_with_query_log_latency` reads `latency_ms` from `query_log` via `db_pool` fixture and asserts `body["retrieve_ms"] + body["llm_ms"] <= row["latency_ms"]`. Verified PASS against real Postgres. |

## Local CI gates

| Gate | Result |
|---|---|
| `uv run ruff check .` | All checks passed |
| `uv run mypy src/` | Success: no issues found in 34 source files |
| `uv run pytest tests/test_generate_*.py tests/test_llm_wrapper.py` | 48 passed |
| `uv run pytest` (full workers suite) | 98 passed, 1 skipped (GCS emulator unavailable, unrelated) |
| `test_timings_consistent_with_query_log_latency` (AC-24, DB-backed) | PASSED (not skipped) |

## Findings

| File:line | Severity | Pattern | Evidence | Suggested fix |
|---|---|---|---|---|
| workers/src/archiviste_workers/services/llm.py:108 | MED (carry-over) | swallowed-error broadening | `except Exception as exc: ... raise LlmUpstreamError(...)` still catches all non-Timeout exceptions; programming bugs (`KeyError`, `AttributeError`, `ImportError`) are masked as 502 `llm_upstream`. Untouched by fix commit. | Catch only provider HTTP/SDK base classes (`httpx.HTTPError`, provider-specific `APIError`). Let unexpected exceptions propagate. |
| workers/tests/test_generate_router.py:69-71,127,146 | MED (carry-over) | weak assertion (AC-3 body) | `captured["json"]` raw bytes are stored but never decoded nor asserted. Only `x-request-id` header is asserted. AC-3 mandates verifying outgoing body has `top_k=5` and `user_tier` propagated. Untouched by fix commit. | `assert json.loads(captured["json"]) == {"query": ..., "top_k": 5, "user_tier": "anonymous"}`. |
| specs/acceptance/RET-001.md (added) | MED (carry-over) | out-of-scope change | Plan `Files to touch` does not list this. RET-001 was already merged on `main` per ticket Pre-conditions. `specs/` are humain-only (CLAUDE.md). Untouched by fix commit. | Revert from this PR; restore via separate commit if genuinely missing on main. |
| workers (overall production diff) | MED (carry-over) | vertical-slice budget | Production LOC ≈ 779 vs 300 budget. Acknowledged by spec OQ-8 (humain accepts > 300 to keep ticket end-to-end). | No action; documented exception. |
| workers/tests/test_llm_wrapper.py:73-80 | LOW (carry-over) | weak coverage (AC-9 grep) | Forbidden token list omits provider strings `"google"` and `"openai"`. Router currently passes but guard would not catch a future regression like `if provider == "google"` in `router.py`. Untouched by fix commit. | Extend list to include all 5 provider strings + 5 `Chat*` class names. |
| workers/src/archiviste_workers/generate/router.py:78,98 | LOW (carry-over) | broad payload type | `payload: dict[str, Any]` rather than typed Pydantic model. Required to drive precise per-field error codes (AC-17/18/19). | Acceptable; no WHY comment was added at `_parse_request`, recommend one. |
| workers/tests/test_generate_router.py:215-230 | LOW (carry-over) | AC-2 partial | `test_conversation_id_generated_when_null` asserts only `len == 36`. Spec wording « AVANT de répondre » → `conversations` row creation is not DB-asserted. | Add DB-backed assertion that the row exists post-response (or confirm ING-003 logger creates row before response — currently the `conversation_handler` mock returns 201 unconditionally, so the « AVANT » order is not actually verified). |
| workers/tests/test_generate_router.py:341-356 | LOW (carry-over) | AC-14 partial | `test_conversation_log_failure_does_not_break_response` asserts ALERT + 200 but does NOT verify both POSTs (user + assistant) were attempted in sequence. The conversation handler is single-purpose (always 503), no call-count assertion. | Track call count on the conversation handler; assert exactly 2 calls with roles `user` then `assistant`. |
| workers/tests/test_llm_wrapper.py | LOW (carry-over) | AC-8 partial | Missing `LLM_MODEL` fail-fast case is not tested explicitly (only `LLM_PROVIDER` and `LLM_API_KEY`). Spec AC-8 enumerates all three. | Add a third fail-fast test for missing `LLM_MODEL`. |

## Spec coverage delta vs prior review

- AC-1: COVERED (error-shape fix removes the previous PARTIAL caveat).
- AC-3: PARTIAL (body fields still not asserted — see MED carry-over).
- AC-11/17/18/19/21/22/23: COVERED (response body shape now `{"error":...}` as spec; tests updated and pass).
- AC-24: COVERED (DB-backed assertion against `query_log.latency_ms` — gaming pattern eliminated).
- All other ACs: unchanged from prior review.

## Property invariants
- Unchanged. INV-1..INV-8 not impacted by GEN-001.

## Security
- No regression introduced by fix commit. Imports moved to top-level do not introduce new attack surface (deps were already in `pyproject.toml`).
- Error responses still leak no provider details (`{"error":"llm_upstream"}` enveloped, AC-21 compliant).
- No secrets in fix diff.

## Out-of-scope changes
- `specs/acceptance/RET-001.md` still present in diff (carry-over MED).

## Summary

The 3 prior HIGH findings are correctly fixed: ruff is green, error body shape matches spec literally, and AC-24 is now verified against the database (no longer tautological). All 48 GEN-001 tests pass, the full workers suite is green (98 passed), `mypy --strict` clean.

Remaining items are all MED/LOW carry-overs from the prior review. The broad `except Exception` (MED) and the AC-3 body assertion (MED) and the out-of-scope `RET-001.md` (MED) are not blockers individually — they are quality-of-implementation issues. Given the ticket's spec-acknowledged > 300 LOC scope (OQ-8) and the absence of any new HIGH finding introduced by the fix commit, verdict moves to APPROVE. Recommend addressing the MED carry-overs in a follow-up PR rather than blocking this slice further.
