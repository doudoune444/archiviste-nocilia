# Review — OPS-005

## Verdict
REQUEST_CHANGES

One real, CI-breaking defect: the Terraform config does not pass `terraform validate`. `max_retries` is placed in the wrong nesting level of `google_cloud_run_v2_job`. Everything else (workflow, runbook, gdrive-sync, CHANGELOG, security posture) is correct and in scope. Fix the one schema error and this is an APPROVE.

## Findings

| File:line | Severity | Pattern | Evidence | Suggested fix |
|---|---|---|---|---|
| infra/terraform/cloud_run_job.tf:19 | HIGH | broken config / fails CI gate | `terraform validate` → `Error: Unsupported argument ... An argument named "max_retries" is not expected here` (line 19). `max_retries` is set on the OUTER `template` (ExecutionTemplate); the google provider expects it on the INNER `template.template` (TaskTemplate, sibling of `containers`/`volumes`/`service_account`). `terraform plan`/`apply` impossible, CI `terraform validate` red. | Move `max_retries = 0` (and its comment) from `template { max_retries ... }` into the inner `template { template { ... } }` block alongside `service_account`. Re-run `terraform validate` to surface any further nested errors hidden behind this first failure. |
| infra/terraform/cloud_run_job.tf:34 | LOW | pattern divergence from reference | Workers service carries BOTH the `run.googleapis.com/cloudsql-instances` annotation (`cloud_run.tf:151-153`) AND the `cloudsql` volume; the Job keeps only the volume. For Cloud Run v2 the `volumes.cloud_sql_instance` block is the native mechanism and the annotation is the legacy v1 path, so the omission is technically fine — but it diverges from the cited reference pattern. | No change required; optionally add the annotation for parity, or leave a one-line note that v2 volume is intentional. Not a blocker. |

## Lint / build status

| Check | Result |
|---|---|
| `terraform fmt -check cloud_run_job.tf` | PASS (exit 0) |
| `terraform validate` (infra/terraform, after `init -backend=false`) | FAIL — see HIGH finding line 19 |
| YAML parse `ingest-lore.yml` | PASS |
| YAML parse `gdrive-sync.yml` | PASS |
| cargo / pytest / ruff / mypy | N/A — no Rust/Python source touched (diff = 5 files: 2 workflows, 1 .tf, runbook, CHANGELOG). Confirmed. |

## Spec coverage

| AC | Status | Evidence |
|---|---|---|
| AC-1 (`google_cloud_run_v2_job archiviste-ingest`, europe-west9, workers image, secret_key_ref Mistral, SA archiviste-runtime) | ✓ | cloud_run_job.tf:5-6 name+`var.region`; image `${local.ar_base}/workers:latest` (l.39); `service_account = google_service_account.archiviste_runtime.email` (l.24); MISTRAL_API_KEY `secret_key_ref` (l.78-86). "sidecar" interpreted as managed Cloud SQL volume per plan R1 — interpretation holds (workers service uses the same volume, no literal sidecar container). |
| AC-2 (command, CLOUD_SQL_IAM_AUTH, INSTANCE_CONNECTION_NAME, volume cloudsql, DATABASE_URL SA email percent-encoded) | ✓ | command `["python","-m","archiviste_workers.ingest","--path","lore/"]` (l.44); `CLOUD_SQL_IAM_AUTH=true` (l.58-61); `INSTANCE_CONNECTION_NAME` (l.53-56); volume `cloudsql` + mount `/cloudsql` (l.32-37, 88-91); DATABASE_URL uses `replace(...,"@","%40")` — identical to cloud_run.tf:221. |
| AC-3 (`on push branches:[main] paths:['lore/**']`) | ✓ | ingest-lore.yml:5-10. `workflow_dispatch` added (convenience, allowed by Non-goals). |
| AC-4 (WIF auth@v2, no SA JSON key) | ✓ | ingest-lore.yml:38-42 `google-github-actions/auth@v2` + `workload_identity_provider: secrets.GCP_WIF_PROVIDER` + `service_account: secrets.GCP_SA_EMAIL`. Matches deploy.yml:24-29. No `*-sa.json`, no JSON key secret. |
| AC-5 (`gcloud run jobs execute --wait --region europe-west9`, propagates failure) | ✓ | ingest-lore.yml:52-56; `env REGION: europe-west9` (l.32-33); `--wait` blocks to terminal state, gcloud exits non-zero on non-Succeeded → step red. Bare `gcloud` (no setup step) matches deploy.yml pattern (gcloud preinstalled on ubuntu-latest, wired by auth@v2). |
| AC-6 (Job exit 0/1 from ING-001) | ✓ (inherited) | Behaviour owned by ING-001; not re-tested here, correct per plan. `max_retries=0` preserves the signal (once the schema fix lands). |
| AC-7 (exit→state→verdict mapping) | ✓ | Encoded via `max_retries=0` (no retry masks exit 1) + `--wait` propagation. Documented at cloud_run_job.tf:15-18, 41-43 and ingest-lore.yml:47-50. NOTE: this AC is only actually realized once the HIGH finding is fixed — with `max_retries` rejected, the resource cannot be created at all. |
| AC-8 (`concurrency group ingest-lore cancel-in-progress:false`) | ✓ | ingest-lore.yml:21-23, exact match. |
| AC-9 (`ingest.summary` in Cloud Logging by execution ID) | ✓ (inherited/doc) | Cloud Run Jobs auto-ship stdout to Cloud Logging; documented runbook l.447-448. No code change needed. |
| AC-10 (runbook rewrite + gdrive-sync checklist) | ✓ | runbook.md:441-475 rewritten (auto trigger + manual fallback + embedder correction R4); gdrive-sync.yml:141 checklist line replaced with "runs automatically via ingest-lore.yml". |

## Security

| Check | Result |
|---|---|
| Secrets in code | None. MISTRAL_API_KEY via `secret_key_ref` (cloud_run_job.tf:78-86), never plaintext. |
| DATABASE_URL secret exposure | Clean — IAM authn, empty password, percent-encoded SA email; no password literal (matches SEC-005 service pattern). |
| WIF / no SA JSON key (secret-hygiene) | ✓ OIDC only, `id-token: write` + `service_account` email; no JSON key file or secret. |
| Least-privilege SA | ✓ Job runs as `archiviste-runtime` (Cloud SQL instanceUser + Mistral secret accessor only). |
| A09 logging — no secrets logged | ✓ Nothing in workflow or Job config logs the token/key; `--quiet` on gcloud; `ingest.summary` (ING-001) carries no key/doc content. |
| Trust boundary | ✓ Trigger = push to protected `main` (human-reviewed); ingester reads versioned `.md` only (ING-001 NFKC + 1 MiB cap), no arbitrary network input. |

## Gaming patterns
None detected. No hardcoded test values, no stub returns, no swallowed errors, no disabled tests. `max_retries=0` and `lifecycle ignore_changes` are justified with WHY comments (AC-7 signal preservation; image drift from deploy.yml).

## Out-of-scope changes
None. Diff touches exactly the 5 files in plan "Files to touch". TOCTOU ING-001 (`workers/src/archiviste_workers/ingest/pipeline.py:98-109`) was NOT touched — confirmed (pipeline.py absent from diff). Debt correctly left open per plan/spec.

## Quality
- No N+1, no blocking I/O (infra/CI only).
- Diff = ~180 LOC added, well under the 300 vertical-slice cap.
- Comments are WHY-only and accurate (R1/R4/R5 from plan reflected in code).
- Embedder correction (R4) propagated to both CHANGELOG and runbook.

## Required action
Fix the single HIGH finding (relocate `max_retries` to inner `template.template`), re-run `terraform validate` to confirm green (and catch any further nested-schema errors that were short-circuited), then re-request review.
