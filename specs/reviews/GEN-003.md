# Review GEN-003 — verdict APPROVE

## Summary
Mode 2 off_topic ships cleanly. Intent classifier is a dedicated 5 s call through the `LlmClient` abstraction with strict fail-open semantics on timeout, upstream error, and unparseable output (AC-6/13/14). Refusal branch reuses the same wrapper with a frozen `OFF_TOPIC_SYSTEM_PROMPT`, never injects retrieved chunks (zero prompt-injection surface from documents — confirmed AC-5), propagates the suspected-injection prefix from GEN-001 to both classifier and refusal prompts (AC-17), aggregates usage/cost across both LLM calls (AC-10), and persists `query_log.intent` on both canon and off_topic paths (AC-2/AC-19). All 153 non-DB tests green; ruff and mypy --strict clean.

## AC coverage

| AC | Test | Status |
|---|---|---|
| AC-1 shape | test_off_topic_response_shape | OK |
| AC-2 query_log.intent off | test_off_topic_ac2_query_log_intent_field (DB-gated) | OK |
| AC-2 query_log.intent in | test_query_log_intent_in_domain_canon (DB-gated) | OK |
| AC-3 no retrieve/canon LLM | test_off_topic_no_retrieve_called (MockTransport raises if hit) | OK |
| AC-4 no provider name in intent.py | test_no_provider_name_in_intent_module | OK |
| AC-5 INTENT_SYSTEM_PROMPT byte-for-byte | test_intent_system_prompt_byte_for_byte / test_system_prompt_matches_constant | OK |
| AC-5 no lore chunks injected | test_user_prompt_contains_query_with_prefix | OK |
| AC-6 normalisation table 7 fixtures | test_normalisation_table | OK |
| AC-7 refusal prompt sent | test_off_topic_refusal_prompt_contains_off_topic_system_prompt | OK |
| AC-8 5 required instructions | test_off_topic_system_prompt_contains_required_instructions | OK |
| AC-9 retrieve_ms=0, citations=[] | test_off_topic_retrieve_ms_zero_and_citations_empty | OK |
| AC-10 usage aggregation | test_off_topic_usage_aggregated | OK |
| AC-11 2x ING-003 + 503 fallback | test_off_topic_conversation_{two_messages_posted,failure_does_not_break_response} | OK |
| AC-12 query_log row off_topic | test_off_topic_query_log_row (DB-gated) | OK |
| AC-13 classifier timeout fail-open | test_fail_open_on_timeout (unit) | OK (unit-only, see LOW-1) |
| AC-14 classifier upstream fail-open | test_fail_open_on_upstream_error (unit) | OK (unit-only, see LOW-1) |
| AC-15 refusal 504 | test_off_topic_refusal_timeout_504 | OK |
| AC-16 refusal 502 | test_off_topic_refusal_upstream_502 | OK |
| AC-17 injection prefix propagation | test_off_topic_injection_prefix_propagated | OK |
| AC-18 input validation | test_generate_validation.py::test_* (5 cases) | OK (see LOW-2) |
| AC-19 in_domain to canon | test_query_log_intent_in_domain_canon | OK |
| AC-20 log INFO intent | test_off_topic_log_info_contains_intent_and_zero_chunks / canon variant | OK |

## Findings

### HIGH
None.

### MED
None.

### LOW

| File:line | Pattern | Evidence | Suggested fix |
|---|---|---|---|
| diff size | LOC | ~434 production LOC + ~620 test LOC (well above 300) | Already acknowledged by humain; tests-heavy. Note only. |
| workers/tests/test_generate_router.py | AC-13/14 oracle drift | Spec test oracle for AC-13/14 says "intégration · stub classifier sleep 6 s -> assert pipeline canon exécuté". Coverage is unit-level on `classify_intent` only; no router-level integration asserting the canon branch executes when classifier fails. Router branch is trivial (`if intent_result.intent == "off_topic"`) so functional coverage is implied, but the spec text is stricter. | Add 1 router-level test injecting a stub classifier that raises `LlmTimeoutError`, then assert retrieve_client called + `query_log.intent='in_domain'`. |
| workers/tests/test_generate_validation.py:23-26 | AC-18 weak assertion | Validation tests use `AsyncMock()` for `llm_client` but never assert `llm_client.invoke.assert_not_called()`. Spec says: "classifier mock asserted NOT_CALLED + aucune ligne `query_log`". A future regression making `_parse_request` happen after `classify_intent` would not be caught. | Append `app.state.llm_client.invoke.assert_not_called()` and `app.state.query_log_repo.insert.assert_not_called()` after each 400/422 assertion. |
| workers/tests/test_mode2_off_topic.py:76 | classifier dispatch by timeout magic | `_MultiResponseLlmClient.invoke` differentiates classifier vs refusal via `timeout_s is not None and timeout_s <= 5.0`. Couples test to the 5 s constant. If `INTENT_TIMEOUT_S` were ever bumped (e.g. to 8 s) the fake would silently misroute. | Either compare against `INTENT_TIMEOUT_S` import or dispatch via call ordinal (1st=classifier, 2nd=refusal). Cosmetic. |
| workers/src/archiviste_workers/services/llm.py:131 | broad except (pre-existing GEN-001) | `except Exception as exc` funnels everything into `LlmUpstreamError`. Not introduced by this diff. | None — out of scope. |

## Security
- No secrets in code; `LLM_API_KEY` continues to flow through `SecretStr`.
- Prompt injection: pattern detected via existing `injection_filter.detect_injection`, prefix propagated to both classifier and refusal user messages (AC-17 verified).
- Retrieved chunks to classifier surface: ZERO. `build_off_topic_messages` does not accept chunks; `_build_intent_messages` does not accept chunks. Test asserts `"<chunk"` and `"<retrieved_chunks>"` absent.
- Trust boundary: classifier is internal worker-side LLM call; provider name never leaks (AC-4 statically asserted via grep on `intent.py`).
- SQL: parameterized ($1..$11) in `query_log.insert`. No `format!` / f-string interpolation.
- Error responses: shape `{"error": code}` — no stack traces.
- No `unwrap` / `expect` / unsafe deserialization / raw HTML render. No `verify=False`. No CORS wildcard introduced. No JWT logic in this diff.

## Out-of-scope changes
None. Diff matches plan "Files to touch" exactly: `services/intent.py` (new), `services/llm.py` (timeout_s param + protocol), `services/query_log.py` (intent column), `generate/models.py` (Mode literal), `generate/prompt.py` (OFF_TOPIC), `generate/router.py` (branch split), 3 test files, CHANGELOG. No surprise touch of gateway/, specs/, migrations/.

## Verdict
APPROVE
Tests green (153 pass, 20 DB-skipped — expected on local Win), ruff + mypy --strict clean, every AC has at least one test (DB-gated ones will run in CI), no forbidden patterns, no spec violations, no out-of-scope changes. LOW findings are nice-to-haves and not blockers.
