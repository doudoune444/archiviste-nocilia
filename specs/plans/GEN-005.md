# Plan — GEN-005 Mode 4 mystère + ACL post-retrieval filtering

## Goal

Move ACL filtering from retrieve SQL to a pure post-retrieval module in workers; add Mode 4 mystery branch in `generate/router.py` triggered when all top-K chunks are ACL-blocked; preserve canon timing structurally; trace `mode='mystery'` in `query_log`.

## Acceptance criteria recap

See `specs/acceptance/GEN-005.md` AC-1..AC-20 (verbatim, non recopié pour respecter ≤ 100 lignes).

## Files to touch

### New
- `D:\Projet-perso\archiviste-nocilia\.worktrees\GEN-005\workers\src\archiviste_workers\services\acl.py` — pure module: `FilterResult` dataclass `(visible, blocked_count)`, `ALLOWED_ACCESS_TIERS_BY_USER_TIER` constant (AC-3 mapping byte-for-byte), `filter_chunks_by_tier(chunks, user_tier) -> FilterResult` with **fail-closed (D-4) on unknown chunk tier** (logs `acl_unknown_tier` with `chunk_id` only, chunk counted as blocked). No imports of `fastapi`/`httpx`/`asyncpg` (AC-20). ~40 LOC.
- `D:\Projet-perso\archiviste-nocilia\.worktrees\GEN-005\workers\tests\test_acl_filter.py` — unit, no I/O: 9-cell permission matrix (3 user_tier × 3 access_tier) + 3 unknown-tier fail-closed cases + grep static import check (AC-3, AC-20, D-4). ~50 LOC.
- `D:\Projet-perso\archiviste-nocilia\.worktrees\GEN-005\workers\tests\test_mode4_mystery.py` — integration: AC-1 shape, AC-4 all-blocked → mystery, AC-5/19 partial-block → canon + no leak, AC-6 empty retrieve, AC-9/10 nominal, AC-11 ING-003 success + failure, AC-12 `query_log` mystery row, AC-13 injection propagation, AC-16/17 LLM timeout/upstream, AC-18 log shape with conditional `blocked_count`. ~120 LOC.
- `D:\Projet-perso\archiviste-nocilia\.worktrees\GEN-005\workers\tests\test_mode4_timing.py` — `@pytest.mark.timing` (D-3), 50 canon + 50 mystery stub round-trips with calibrated `asyncio.sleep` LLM stub, `time.perf_counter()` p95, **hard-fail at delta > 200 ms** (D-3), warn at > 100 ms. ~60 LOC.

### Modified
- `D:\Projet-perso\archiviste-nocilia\.worktrees\GEN-005\workers\src\archiviste_workers\retrieve\search.py` — drop `WHERE d.access_tier = ANY($2::text[])`, drop `$2` param (shift `top_k` from `$3` to `$2`), add `d.access_tier AS access_tier` in SELECT, signature keeps `allowed_tiers` ignored with inline `# D-2: ignored post GEN-005, filtering moved to services/acl.py` (D-2). ~10 LOC.
- `D:\Projet-perso\archiviste-nocilia\.worktrees\GEN-005\workers\src\archiviste_workers\retrieve\schemas.py` — add `access_tier: Literal["public","members","author_only"]` on `RetrievedChunk` (strict côté production, AC-2). ~3 LOC.
- `D:\Projet-perso\archiviste-nocilia\.worktrees\GEN-005\workers\src\archiviste_workers\services\retrieve_client.py` — propagation auto via Pydantic si champ ajouté à `Chunk`. ~0 LOC.
- `D:\Projet-perso\archiviste-nocilia\.worktrees\GEN-005\workers\src\archiviste_workers\generate\models.py` — extend `Mode = Literal["canon","off_topic","lore_gap","mystery"]`, add **`access_tier: str` (U-3, libre, pas `Literal[...]`)** sur `Chunk` pour permettre fail-closed business logic (D-4) au lieu de rejet pydantic 422 sur tier inconnu. ~3 LOC.
- `D:\Projet-perso\archiviste-nocilia\.worktrees\GEN-005\workers\src\archiviste_workers\generate\prompt.py` — `MYSTERY_SYSTEM_PROMPT` constant figée byte-for-byte (AC-7 instructions a-f), `build_mystery_messages(query, suspected_injection) -> list[BaseMessage]` retournant `[SystemMessage, HumanMessage]` sans chunk (AC-8). ~30 LOC.
- `D:\Projet-perso\archiviste-nocilia\.worktrees\GEN-005\workers\src\archiviste_workers\generate\router.py` — **position injection ACL filter : juste après `retrieve_ms = ...`, AVANT `max_score = max(...)`** (sinon retrieve all-blocked tombe en lore_gap par erreur). Branche routing : `intent='in_domain'` + `visible == [] and blocked_count >= 1` → mystery (`build_mystery_messages` + LLM + citations=[] + `mode='mystery'`) ; sinon canon avec `result.visible`. **`blocked_count` ajouté en kwarg conditionnel dans le `logger.info("generate", ...)` canon existant** (U-5) : présent seulement quand `mode='mystery'` OU (`mode='canon'` AND `blocked_count > 0`). ~40 LOC delta.
- `D:\Projet-perso\archiviste-nocilia\.worktrees\GEN-005\workers\tests\test_retrieve_integration.py` — **extension (U-4)**, pas de nouveau fichier : flip ACL assertions (retrieve renvoie tous tiers, champ `access_tier` propagé), supprime cas filtre SQL, ajoute cas de couverture all-tiers (AC-2). ~15 LOC delta.
- `D:\Projet-perso\archiviste-nocilia\.worktrees\GEN-005\workers\tests\test_retrieve_properties.py` — adjust INV-9/INV-10 si dépendant du filtre tier. ~5 LOC delta.
- `D:\Projet-perso\archiviste-nocilia\.worktrees\GEN-005\workers\tests\test_generate_prompt.py` — `mystery_system_prompt_byte_for_byte` + `build_mystery_messages_no_chunks` (AC-7, AC-8). ~30 LOC.
- `D:\Projet-perso\archiviste-nocilia\.worktrees\GEN-005\workers\tests\test_generate_router.py` — fixtures `Chunk(...)` gain `access_tier="public"` default. ~5 LOC delta.
- `D:\Projet-perso\archiviste-nocilia\.worktrees\GEN-005\CHANGELOG.md` — entrée `[Unreleased] / Added` : `**GEN-005** : Mode 4 mystère + ACL post-retrieval filtering`.

### Humain-only — flagged for explicit approval
- `D:\Projet-perso\archiviste-nocilia\.worktrees\GEN-005\specs\openapi\gateway-to-workers.yml` — ajout `access_tier` sur `RetrievedChunk` (sanction AC-2 + Non-goal §38).

### Explicitly NOT touched
- `migrations/*.sql` — pas de delta schéma (Non-goal §48).
- `specs/properties.md`, `specs/golden_qa.jsonl` — D-7.
- `gateway/**` — D-6.

## Test strategy

- **Unit pur** `test_acl_filter.py` : 9-cell matrix + 3 fail-closed cases (D-4) + grep static `import` (AC-3, AC-20).
- **Unit prompt** `test_generate_prompt.py` : byte-for-byte `MYSTERY_SYSTEM_PROMPT` + regex sur AC-7 a-f + `build_mystery_messages` shape + absence `<chunk` (AC-7, AC-8).
- **Integration** `test_mode4_mystery.py` : TestClient + `httpx.MockTransport` retrieve + LLM stub patché, Postgres réel pour `query_log` + ING-003 mock. Couvre AC-1/4/5/6/9/10/11/12/13/16/17/18/19.
- **Timing** `test_mode4_timing.py` : 50+50 stub samples, `time.perf_counter()` p95, hard-fail > 200 ms (D-3), `@pytest.mark.timing`.
- **Integration RET-001 drift** : `test_retrieve_integration.py` étendu (U-4) — assert tous tiers + `access_tier` champ.
- **Contract** : schemathesis re-run sur `/v1/retrieve` après update OpenAPI yml.
- **Eval** : aucune (D-7).

## Implementation steps (ordered, TDD)

1. **Test fail Mode 4 d'abord** — écrire `test_acl_filter.py` (9-cell + fail-closed) ET squelette `test_mode4_mystery.py::all_blocked_triggers_mystery` (AC-4). Red.
2. Créer `services/acl.py` (`FilterResult`, mapping const, `filter_chunks_by_tier` fail-closed D-4). Green unit.
3. Étendre `generate/models.py` : `Mode` literal + `Chunk.access_tier: str` (U-3 libre).
4. Étendre `retrieve/schemas.py` : `RetrievedChunk.access_tier: Literal[...]` strict.
5. Patcher `generate/router.py` : **branche routing + injection position ACL juste après `retrieve_ms = ...` AVANT `max_score`** + propagation `blocked_count` kwarg conditionnel `logger.info` (U-5).
6. Ajouter `MYSTERY_SYSTEM_PROMPT` + `build_mystery_messages` dans `generate/prompt.py` (AC-7/8).
7. Modifier `retrieve/search.py` : drop SQL filter, add `d.access_tier` SELECT, inline D-2 comment. Étendre `test_retrieve_integration.py` (U-4) assertions all-tiers. Green RET drift.
8. Tests `test_mode4_mystery.py` AC-5/19 (partial-block no leak), AC-6 (empty retrieve), AC-12 (query_log), AC-13 (injection propagation), AC-16/17 (timeout/upstream), AC-18 (log conditional). Green.
9. `test_mode4_timing.py` 50+50 samples, hard-fail > 200 ms (D-3). Green ou blocker no-workaround.
10. Update `specs/openapi/gateway-to-workers.yml` (validation humaine) + schemathesis re-run.
11. `ruff check`, `mypy --strict`, `pytest workers/tests/`. Green.
12. CHANGELOG `[Unreleased] / Added` entry.

## Surveillance LOC

`acl.py` ~40 + `router.py` delta ~40 + `prompt.py` ~30 + `models.py`/`schemas.py`/`search.py`/`retrieve_client.py` ~25 = **~135 LOC prod** + ~275 LOC tests = **~275 LOC prod hors tests** (sous seuil 300 vertical-slice). OK.

## Risks / open questions

- **R1 — `specs/openapi/gateway-to-workers.yml` humain-only** : ajout `access_tier` requis par AC-2. Sanction Non-goal §38. **Validation humaine explicite** avant edit.
- **R2 — `Chunk.access_tier` breaking field add** : U-3 relâche en `str`, donc fixtures pydantic existantes échouent quand-même (champ requis). Audit fixture `test_generate_router.py`, `test_generate_parser.py`, `test_generate_prompt.py`.
- **R3 — GEN-003 dependency** : Pre-conditions assument GEN-003 mergé (champ `intent`). Vérifié `git log` ✓. Si absent, blocker no-workaround.
- **R4 — `asyncpg` param order** : drop `$2` shifte `top_k` `$3` → `$2`. `test_retrieve_integration.py` (U-4) attrape.
- **R5 — Timing CI bruyant** : D-3 mitige seuil 200 ms. Si flaky persistant → skip `@pytest.mark.timing` en CI default + ticket OBS-001 V2.
- **R6 — Fail-closed coverage (D-4)** : exige `Chunk.access_tier: str` (U-3 libre côté generate) sinon Pydantic rejette 422 avant la business logic. Cohérent.
- **R7 — Mutation `test_retrieve_integration.py` (U-4)** : changement de sémantique d'assertions historiques (ACL SQL → all-tiers). Implementer doit lire la spec U-4 dans ce plan, pas créer de nouveau fichier.

## Out of scope

- Mode 3 lore-gap (GEN-004), Mode 2 off-topic (GEN-003), classifier `intent` (GEN-003).
- SEC-001 vocab alignment `member`/`author` vs `members`/`author_only` (D-6).
- UI ACL auteur, per-conversation/per-message ACL, audit `acl_deny` table, OTel `acl_blocked_total`, ACL cache (D-5), adversarial near-visible detection, per-tier mystery tuning.
- `specs/golden_qa.jsonl` mystery entries (EVAL-002 ticket future, D-7).
- Gateway Rust changes (D-6).
- Statistical timing >1000 samples (OBS-001 V2).
- Removing `allowed_tiers` param from `retrieve/search.py` signature (kept backward-compat D-2 ; cleanup ticket post-merge).
