# Review — OBS-008 Eval runner container image

## Verdict
APPROVE

1 minor + 2 nits, no blockers, no majors. Both nits applied before commit (`679f8cb`).

## AC coverage

| AC | Status | Evidence |
|----|--------|----------|
| AC-1 Dockerfile, context=eval/, ENTRYPOINT `python -m eval.ragas_runner`, `--help` exit 0 | PASS | `eval.Dockerfile:6,27`; live `docker build` exit 0; `docker run --help` exit 0 with `--mode/--set/--baseline/--output/--persist` |
| AC-2 bakes runner code + `--frozen` deps + `baseline.json`; NOT `golden_qa.jsonl` | PASS | `COPY . /app/eval/` + `uv sync --frozen`; `baseline.json` present in built image; golden absent (outside `eval/` context) |
| AC-3 `--output`/`--persist` runtime-only, not baked | PASS | ENTRYPOINT carries neither; argparse defaults `--output=None`, `--persist` store_true |
| AC-4 live deps, no torch/sentence-transformers/bge-m3 | PASS | `--extra live --no-dev`; built-image `import torch` / `import sentence_transformers` both `ModuleNotFoundError` |
| AC-5 `--network none --help` exit 0, no runtime install | PASS | live `docker run --network none --help` exit 0; all deps build-time |
| AC-6 push `:${sha}` + `:latest` to `archiviste/eval` | PASS | `deploy.yml` both tags, registry `${REGION}-docker.pkg.dev/${PROJECT}/archiviste/eval` |
| AC-7 step fails job on build OR push error | PASS | `docker/build-push-action@v6 push:true`, no `continue-on-error`; in main `deploy` job |
| AC-8 no new Terraform / IAM | PASS | `git diff infra/terraform` empty; reuses `archiviste` repo |
| AC-9 OpenAPI unchanged | PASS | `git diff specs/openapi` empty |
| AC-10 migrations unchanged | PASS | `git diff migrations` empty |

## Findings

| File:line | Severity | Finding | Resolution |
|-----------|----------|---------|------------|
| `infra/docker/eval.Dockerfile:6` | minor | `python:3.12-slim` not digest-pinned | Deferred — spec non-goal (hardening excluded); matches workers/gateway convention |
| `eval/.dockerignore` | nit | missing `.venv/` + `.git/` | **Applied** — added both |
| `.github/workflows/deploy.yml:67` | nit | comment overstated isolation | **Applied** — comment corrected to admit eval gates the deploy job |

## Verification notes

Real bug caught + fixed during ship verification (not by reviewer): venv `/app/eval/.venv` was not on PATH, so ENTRYPOINT ran the empty system python — AC-1/AC-5 failed. Fixed via `ENV PATH="/app/eval/.venv/bin:$PATH"`. Rebuild + all 5 docker acceptance checks re-run green.

OQ-3 dep-shape: `dev` and `live` extras list the 3 live deps with identical specifiers; `uv.lock` resolves each once → byte-identical pins, no drift, PR lint/test path unchanged.

Scope: only the 6 planned files touched. Diff ≈ 60 LOC excluding `eval/uv.lock`. No property invariant applies (infra/build slice).
