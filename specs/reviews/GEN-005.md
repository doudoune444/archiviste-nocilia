# Review — GEN-005

## Verdict
APPROVE

(with 1 LOW finding to address in a follow-up; nothing blocks merge. One MERGE-HYGIENE note: branch base is stale vs origin/main — must merge/update before PR or the diff carries unrelated gateway deletions.)

## Findings

| File:line | Severity | Pattern | Evidence | Suggested fix |
|---|---|---|---|---|
| workers/src/archiviste_workers/services/acl.py:51 | LOW | AC-18 leak (latent) | `logger.error("acl_unknown_tier", chunk_id=chunk.source_path)` — AC-18 + Failure modes forbid logging `source_path` of blocked chunks; here `source_path` is logged under a mislabeled `chunk_id` key. Generate-side `Chunk` has no real `chunk_id` field. Path is fail-closed only (unknown tier). | Either drop the identifier entirely (log only the event) or add a real opaque `chunk_id` to `Chunk`. Do NOT log `source_path`. |
| workers/src/archiviste_workers/services/acl.py:49-53 | LOW | dead branch (in-pipeline) | unknown-tier fail-closed branch is unreachable via the normal pipeline: `RetrievedChunk.access_tier` is a strict `Literal[...]` (retrieve/schemas.py:43) and `Chunk.access_tier` defaults to `"public"` (models.py:71). Only reachable via direct unit call (test_acl_filter.py:81-94). | Acceptable defense-in-depth (D-4), but document that it cannot fire end-to-end, or assert it via an integration test feeding a raw unknown tier. |
| (branch base) | NOTE | stale base | `git diff origin/main..HEAD` shows ~2000 LOC of gateway/UI-002 deletions because the branch forked before PR#59/#60 merged. Merge-base diff is clean (workers-only, +1037/-126). | Merge origin/main into the branch before opening the PR so the PR diff is GEN-005-only. |

## Spec coverage
- AC-1: ✓ `test_mode4_mystery.py::test_mystery_response_shape` (exact key set asserted)
- AC-2: ✓ `retrieve/search.py` SQL has no `WHERE d.access_tier`, `LIMIT $2`, adds `d.access_tier`; `RetrievedChunk.access_tier` Literal; `test_retrieve_integration.py` (skipped locally — PG down, runs in CI)
- AC-3: ✓ `services/acl.py` mapping byte-for-byte; `test_acl_filter.py::test_permission_matrix` (9 cells)
- AC-4: ✓ `test_all_blocked_triggers_mystery` (mode=mystery, blocked_count=5, canon LLM not called)
- AC-5: ✓ `test_partial_block_triggers_canon` (canon, only visible chunks in prompt)
- AC-6: ✓ `test_empty_retrieve_not_mystery` (mode ∈ {lore_gap, canon}, not mystery)
- AC-7: ✓ `test_mystery_system_prompt_byte_for_byte` + required-instruction assertions
- AC-8: ✓ `test_build_mystery_messages_no_chunks` (no `<chunk`/`<retrieved_chunks`)
- AC-9: ✓ `test_mystery_citations_empty_and_retrieve_ms_nonzero` (citations=[], retrieve_ms present)
- AC-10: ✓ `test_mystery_usage_from_llm` (usage from mystery LLM, cost via compute_cost_eur)
- AC-11: ✓ `test_mystery_conversation_persisted` (2 POST) + `test_mystery_conversation_fail_returns_200`
- AC-12: ✓ `test_mystery_query_log_inserted` (skipped locally — PG down; logic verified by code read, runs in CI)
- AC-13: ✓ `test_mystery_injection_propagation` (prefix + single warn)
- AC-14: ✓ `Mode = Literal["canon","off_topic","lore_gap","mystery"]` (models.py:12); canon path unchanged
- AC-15: ✓ `test_mode4_timing.py::test_mystery_timing_constant` (delta ≤ 200ms, D-3) — passed. Structural timing equality holds: mystery path calls LLM once, persists 2 messages, inserts query_log — no short-circuit (router.py:315-443)
- AC-16: ✓ `test_mystery_llm_timeout_returns_504` (+ query_log status 504, tokens NULL in code)
- AC-17: ✓ `test_mystery_llm_upstream_returns_502`
- AC-18: ✓ blocked_count conditional (canon: only if >0, router.py:286-289; mystery: always, router.py:432); `test_mystery_log_no_chunk_text_leak` confirms no text/source_path in `generate` event. NOTE: the separate `acl_unknown_tier` error event does log source_path (LOW finding above) but that event is distinct from the `generate` event AC-18 governs.
- AC-19: ✓ `test_partial_block_canon_no_leak` (no priv chunks in prompt/citations/logs)
- AC-20: ✓ `test_no_forbidden_imports` (AST check: no fastapi/httpx/asyncpg); module is pure dataclass+fn

## Property invariants
- No `specs/properties.md` invariant new to GEN-005. `test_retrieve_properties.py` adjusted for ACL-agnostic retrieve (skipped locally, CI runs).
- AC-15 timing is a property-like guard (50→20 stub samples reduced for CI per test; D-3 threshold 200ms absorbs the reduction). Acceptable per spec Non-goal (no statistical hardening phase 1).

## Security
- A01 Broken Access Control: ACL filtering moved post-retrieval into pure `services/acl.py`; fail-closed on unknown tier (D-4) ✓. Deny-by-default: `ALLOWED_ACCESS_TIERS_BY_USER_TIER.get(user_tier, frozenset())` → unknown user_tier blocks all (test_fail_closed_unknown_user_tier) ✓.
- Timing side-channel (anti-leak): VERIFIED — mystery branch issues a real LLM call, 2 conversation POSTs, and a query_log INSERT, structurally identical to canon. No static/preassembled response, no ACL cache (D-5). Timing test green.
- RAG prompt-injection: blocked chunks NEVER reach the LLM in mystery (build_mystery_messages takes no chunks, router.py:328); in canon partial-block only `visible` chunks are passed to build_messages (router.py:194). Injection prefix propagated (AC-13). Retrieved text confined to HumanMessage `untrusted_data` zone, never system role ✓.
- No secrets in diff. No SQL injection: `_SEARCH_SQL` parameterized (`$1`,`$2`), no f-string into SQL ✓.
- Log leakage: blocked chunk text/source_path absent from `generate` event (AC-18/19 tests). Residual LOW: `acl_unknown_tier` logs source_path (latent, unreachable in pipeline).
- No swallowed errors: retrieve/LLM errors propagate as typed `_GenerateError` → 502/504. ING-003 + query_log failures logged ALERT, response stays 200 per spec (AC-11/12).

## Out-of-scope changes
- None within the GEN-005 commit (merge-base diff = workers/ + specs/plans/GEN-005.md + CHANGELOG.md only).
- `workers/pyproject.toml:+1` — added `timing` pytest marker registration (justified, supports AC-15 `@pytest.mark.timing`). In scope.
- The gateway/UI deletions seen in `origin/main..HEAD` are a STALE-BASE artifact, not authored by this ticket. Resolve by merging origin/main before PR (MERGE-HYGIENE note above). Verified via `git merge-base` isolation.

## CI gates (local)
- `ruff check .` — All checks passed ✓
- `mypy src/` — Success, no issues in 41 files ✓
- `pytest -q` — 219 passed, 30 skipped (all skips = Postgres unreachable on this box; DB-backed AC-12 et al. run in CI). No failures. ✓
- schemathesis / OpenAPI: plan flags `specs/openapi/gateway-to-workers.yml` as human-only edit (R1) — confirm the `access_tier` field was added there before contract CI runs (not in this worktree diff; verify on the PR).
