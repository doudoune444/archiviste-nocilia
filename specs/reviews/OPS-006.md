# Review — OPS-006

## Verdict
APPROVE

## Lints / tests (run locally in `workers/`)
| Gate | Result |
|---|---|
| `uv run ruff check .` | All checks passed |
| `uv run mypy src/` | Success: no issues found in 44 source files |
| `uv run pytest tests/test_download_corpus.py tests/test_ingest_cli.py` | 15 passed |
| `uv run pytest -q` (full) | 252 passed, 1 skipped, 14 failed, 11 errors — ALL failures/errors are pre-existing DB-dependent integration tests (`relation "conversations" does not exist`, `DATABASE_URL required`) in files NOT touched by this diff (test_generate_router, test_ticket_service, test_retrieve_*, test_ingest_integration). Not OPS-006 regressions. |

## Findings

| File:line | Severity | Pattern | Evidence | Suggested fix |
|---|---|---|---|---|
| .github/workflows/gdrive-sync.yml:114 | MED | unpinned action (A08 integrity) | `uses: google-github-actions/auth@v2` — every other action in this file is SHA-pinned (checkout `@34e1148`, setup-uv `@caf0cab`, upload-artifact `@043fb46`); the removed `peter-evans/create-pull-request` was SHA-pinned too. `@v2` is a mutable tag. | Pin to commit SHA, e.g. `google-github-actions/auth@<sha> # v2.x`. ingest-lore.yml should be checked for the same. |
| .github/workflows/gdrive-sync.yml:17,99-108 | LOW | dead permission / orphaned gate | `contents: write` comment says "kept for state file commit" but the commit step was removed with the PR steps; `detect diff` (l.99) gates rsync on `git status lore/ scripts/.gdrive_state.json` yet nothing commits `.gdrive_state.json` anymore, so each scheduled run re-detects the same state delta. State-file persistence is ING-013 scope; gate still works for rsync. | Confirm with human whether state-file persistence belongs in ING-013 follow-up; drop `contents: write` if no commit step remains. |
| infra/terraform/cloud_run_job.tf:50-52 | LOW | shell-mode command | Job `command` is `bash -c "...&&..."` with `$INGEST_ROOT`/`$INGEST_ROOT/lore/`. Values are operator-set env (not user input), so no injection surface; acceptable given the venv-interpreter constraint documented inline. | None required; note for awareness. |

No HIGH findings.

## Gaming-pattern scan
- No hardcoded test values / match-on-test-input in `download_corpus.py` or `cli.py`.
- No magic constants: `INGEST_ROOT`/`LORE_CORPUS_BUCKET` read from env (cli command + Job env), bucket name in Terraform is the resource name (AC-1 literal).
- No stub returns: `download_corpus` performs real list+download; `main()` returns 0 by design (AC-9 no-op), object download is real.
- No swallowed errors: `_safe_target` raises `ValueError` on escape; missing env (`os.environ[...]`) raises `KeyError` (fail-loud); `cli.main` catches only `(ValueError, FileNotFoundError)` and returns EXIT_FILE_ERRORS, then logs.
- No skipped/ignored tests added. The one skip (`test_conversation_integration`) is pre-existing and gated on emulator env.
- `EXIT_INIT_FAILURE` (cli.py:30) is NOT dead — still returned at cli.py:117 (`_run_async` init failure). Verified.

## Spec coverage
- AC-1: ✓ `infra/terraform/storage.tf:5-12` bucket `archiviste-lore-corpus`, `var.region` (europe-west9), `uniform_bucket_level_access = true`, `public_access_prevention = "enforced"`, no public ACL member.
- AC-2: ✓ `storage.tf:16-20` `google_storage_bucket_iam_member.lore_corpus_writer` — `roles/storage.objectAdmin`, member `gha_deploy.email`, bucket-scoped. No 3rd SA, no JSON key.
- AC-3: ✓ `storage.tf:24-28` `lore_corpus_reader` — `roles/storage.objectViewer`, member `archiviste_runtime.email`, bucket-scoped. Identifiers resolve (iam.tf:3,9).
- AC-4: ✓ (manual oracle) `gdrive-sync.yml:122-125` `gcloud storage rsync lore/ gs://archiviste-lore-corpus --recursive`, NO `--delete-unmatched-destination-objects`. PR-opening steps (compute date / build PR body / create PR) deleted. WIF auth added.
- AC-5: ✓ `cloud_run_job.tf:50-52` bash sequence `download_corpus && ingest --root $INGEST_ROOT --path $INGEST_ROOT/lore/`; env `INGEST_ROOT=/tmp/lore`, `LORE_CORPUS_BUCKET=archiviste-lore-corpus`. download_corpus.py writes to `<root>/lore/`. No `.git/`.
- AC-6: ✓ `find_repo_root` removed (cli.py diff); `--root` arg added (cli.py:100); anchor on `--root` (cli.py:80,92,122). Exact message `path must be relative to repo root` preserved (cli.py:63) and asserted in `test_resolve_target_rejects_outside_root`. `--path` outside `--root` → exit != 0 (`test_path_outside_root_rejected`). No `.git/` reference — enforced by `test_no_find_repo_root_in_cli_source` (asserts `.git` absent). Missing `--root` dir → exit != 0 (`test_missing_root_dir_exits_nonzero`).
- AC-7: ✓ (manual run oracle) `ingest.summary` log preserved (cli.py:130-138); exit-0 path unchanged.
- AC-8: ✓ `ingest-lore.yml:5-9` `on: workflow_run: { workflows: [gdrive-sync], types: [completed], branches: [main] }`; job guard `if: workflow_dispatch || workflow_run.conclusion == 'success'` (l.30); `workflow_dispatch` kept.
- AC-9: ✓ empty bucket → `download_corpus` returns 0, dest dir created, `main()` exits 0 (`test_download_corpus_empty_bucket_returns_zero`, `test_main_exits_zero_on_empty_bucket`). Ingest CLI summary `total: 0` path unchanged.

## Security
- No secrets detected in diff (bucket name + SA emails are non-sensitive; WIF via `secrets.GCP_WIF_PROVIDER`/`GCP_SA_EMAIL`, no JSON key).
- Path traversal (AC-5, security.md §"filenames never user-controlled"): `_safe_target` (download_corpus.py:17-27) blocks both `../` escape and absolute blob names. Verified: `dest_root / "/etc/passwd"` → `/etc/passwd`, fails `dest_root in target.parents` → ValueError; `../../etc/pwned.md` → resolves outside → ValueError (tested `test_download_corpus_rejects_path_traversal`, asserts no write). No bypass found.
- Bucket: uniform access + enforced public-access-prevention = no public ACL possible (security.md §RAG Conversation persistence, transposed). Least-privilege: writer objectAdmin, reader objectViewer, both bucket-scoped.
- WIF, no SA JSON key. SSRF N/A (no user-supplied URL; bucket name is operator env). No SQL / JWT / CORS surface in this diff.
- Forbidden patterns: none. No `unwrap`/`expect` equivalent on user input; no unsafe deserialization; env reads fail-loud (`KeyError`), no insecure `getenv` default.

## Property invariants
- N/A — `specs/properties.md` lists no invariant for the ingestion CLI/downloader path (no RAG retrieval/gateway path touched). Confirmed by plan §Test strategy.

## Plan deviation (judged)
- `INGEST_ROOT` default `/tmp/lore` dropped from code (ruff S108 hardcoded-tmp); env var now required (download_corpus.py:54 `os.environ["INGEST_ROOT"]`, fail-loud `KeyError`). ACCEPTABLE: the Job sets `INGEST_ROOT=/tmp/lore` explicitly (cloud_run_job.tf:96-98) and runbook documents it. No AC depends on a code default (AC-5 cites `/tmp/lore` as example only). Fail-loud on missing env is correct per no-workaround/secret-hygiene (no silent fallback). Plan §OQ-A mentioned a default; deviation is a strict improvement.

## Out-of-scope changes
- None. specs/ touched = only `specs/plans/OPS-006.md` (agent-editable). NO `specs/acceptance/ING-001.md` or `ING-013.md` edits (human-only, OQ-B respected). All other files match plan "Files to touch". Diff = +147 LOC src+infra+workflow (well under 300).
