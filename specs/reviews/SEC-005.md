# Review — SEC-005 PR-A (round 2)

Branch `feat/SEC-005-iam-sql-token` @ `6bdb5bc`. PR target = `main`.
Round 1 verdict: REQUEST_CHANGES (2 HIGH + 3 MED + 2 LOW) — see git log `2219596`. R1 findings all resolved by fixup commit `6bdb5bc`.

## Verdict

**APPROVE**

All prior HIGH and MED findings genuinely closed. No new HIGH/MED introduced. Lints + tests green locally. One small LOW residual (CI-flake risk on the 1 ms tolerance) does not block.

## Prior findings — verification

| Prior ID | Status | Evidence |
|---|---|---|
| HIGH-1 (boot log capture) | CLOSED | `gateway/tests/test_sql_pool.rs:108-137` is `#[tracing_test::traced_test]`, drives `init_sql_pool(&provider, "postgres://...")` (real boot helper extracted at `lib.rs:124-163`, called from `run()` at `lib.rs:620` — same code path), and asserts both `logs_contain("boot.sql_pool_init_failed")` AND `logs_contain("metadata_token_failed")`. Not a mock — exercises the actual `tracing::error!` emit at `lib.rs:129-134`. Verified green: `5 passed`. |
| HIGH-2 (cache-hit ≤ 1 ms timing) | CLOSED with caveat | `test_sql_pool.rs:247-281` `sec005_before_acquire_cache_hit_under_1ms` primes cache, then measures `Instant::now()` → `t0.elapsed()` around a second `get_or_refresh().await` and asserts `elapsed < Duration::from_millis(1)`. Doc-comment (lines 240-246) honest that test measures `get_or_refresh` cache-hit, not `before_acquire` directly. AC-4 oracle named `before_acquire` but actual `before_acquire` body does strictly less work than `get_or_refresh` (only `meta.age < Duration::from_secs(...)` arithmetic, no lock), so chosen substitute is a strict upper bound — not tautological. Not measuring an `Arc::clone` or other bypass. Verified green. |
| MED-3 (refresher event + URL-parse code) | CLOSED | `lib.rs:90, 100` both emit `event = "sql_pool.token_refresh_failed"` — distinct from runbook §8 grep oracle `sql_pool.connection_failed`. URL-parse error at `lib.rs:91` now maps to `reason_code = "network"` (not `cloud_sql_auth_failed`). AC-14 grep stays clean. |
| MED-4 (scope percent-encoding) | CLOSED | `auth_metadata/token.rs:188-194` calls `percent_encode_scope(value)` which hand-rolls RFC 3986 unreserved-set encoding (`token.rs:225-240`, justified WHY comment "avoids adding a new crate for a single use"). All 5 mockito assertions in `test_sql_pool.rs` and the lib unit test `for_cloud_sql_uses_sqlservice_admin_scope` (`token.rs:267-288`) match on exact `scopes=https%3A%2F%2Fwww.googleapis.com%2Fauth%2Fsqlservice.admin` form. Doc-comment at `token.rs:263-265` aligned with wire form. |
| MED-5 (immediate first tick) | CLOSED | `lib.rs:77-78`: `let start = tokio::time::Instant::now() + Duration::from_mins(30); let mut interval = tokio::time::interval_at(start, Duration::from_mins(30));` — first tick at boot + 30 min, no double fetch at startup. |
| LOW-6 (gcs/sign re-export) | CLOSED | `gateway/src/gcs/sign.rs:13` imports `use crate::auth_metadata::token::{classify_reqwest_error, TokenError, TokenProvider};`. Back-compat shim at `gcs/mod.rs:7-9` still present for test files only. |
| LOW-7 (magic minute literals in test) | CLOSED | `SQL_CONNECTION_MAX_LIFETIME_SECS` now `pub` at `lib.rs:59`, imported by test at `test_sql_pool.rs:17`, used at `:82, :85` instead of raw `44`/`45 min`. |

## New findings (round 2)

| File:line | Severity | Pattern | Evidence | Suggested fix |
|---|---|---|---|---|
| `gateway/src/lib.rs:78` | LOW | `MissedTickBehavior` defaults to `Burst` for background refresher | `interval_at` default = `MissedTickBehavior::Burst`. If runtime stalls > 30 min (debugger, severe load), refresher fires multiple ticks back-to-back on resume → multiple `set_connect_options` swaps. Each cheap (no network if cache valid) but churn. `MissedTickBehavior::Skip` semantically correct for "refresh every 30 min, drop missed ticks". | `interval.set_missed_tick_behavior(tokio::time::MissedTickBehavior::Skip);` after construction. |
| `gateway/tests/test_sql_pool.rs:278` | LOW | 1 ms tolerance may be tight on heavily loaded CI runners | Cache-hit path is sub-microsecond on any reasonable hardware (in-memory `Utc::now()` compare under `RwLock::read()`), but GitHub Actions runners under stop-the-world GC or noisy-neighbour pressure can stall arbitrarily. Spec AC-4 explicitly says ≤ 1 ms — review cannot raise the bound. If this test ever flakes in CI, spec needs amendment, not test threshold. | No action. If flake observed, surface spec amendment — do NOT silently bump threshold. |

## Spec coverage (round 2)

| AC | Status | Evidence |
|---|---|---|
| AC-1 (module rename + re-export) | covered | `rg gcs::token::` returns 0 hits in `src/`; only re-export + test files. |
| AC-2 (scope ctors + scope query, percent-encoded) | covered (was caveat in R1) | unit tests pass; on-the-wire query string matches AC-2 oracle verbatim. |
| AC-3 (AppState rename) | covered | `rg state\.token_provider\b` in `src/` returns 0; doc-comment in tests non-load-bearing. |
| AC-4 (pool with token + cache-hit ≤ 1 ms) | covered (HIGH-2 closed) | `sec005_before_acquire_cache_hit_under_1ms` green, asserts `< Duration::from_millis(1)`. |
| AC-5 (max_lifetime constant) | covered | `SQL_CONNECTION_MAX_LIFETIME_SECS = 45 * 60` at `lib.rs:59`, now `pub` (used in test). |
| AC-6, AC-7, AC-9, AC-12 (workers) | out of scope (PR-B) | — |
| AC-8 (boot fail-fast log) | covered (HIGH-1 closed) | `sec005_boot_fails_when_metadata_500` asserts both event name + reason_code in captured logs. |
| AC-10 (reason_code mapping) | covered | refresher events use `network` for URL-parse, `metadata_token_failed` for fetch failures; boot uses `metadata_token_failed` + `cloud_sql_auth_failed` (`lib.rs:129-134, 154-160`). |
| AC-11 (4 integration cases) | covered | 5 tests pass (4 AC-11 cases + 1 AC-4 timing). |
| AC-13, AC-14, AC-15 | covered | unchanged from R1. |

## Property invariants

`specs/properties.md` INV-1..INV-10 — none touch Cloud SQL pool path. No property test required.

## Security

Mapped against `.claude/rules/security.md`. No HIT.

- Secrets in code: none. Mock tokens (`test-sql-token`, `cached-token`, `fresh-token`, `stale-token`, `cache-hit-token`, `sql-token`, `gcs-token`) are fixture literals only.
- Sensitive types: bearer wrapped in `SecretString` end-to-end; `expose_secret()` calls only at `PgConnectOptions::password` (`lib.rs:84, 140`) — never logged.
- SQL injection: no new SQL.
- SSRF: metadata URL hardcoded `http://metadata.google.internal`. Injectable base URL only via `with_base_url(...)` used by tests. No user input.
- Trust boundary: gateway computes the SQL token itself, no client header trust.
- A09 logging: refresher emits `error = %e` — `reqwest::Error` Display safe (no headers/body); `TokenError` enum variants are static strings. No bearer leakage.
- CORS / CSP / HSTS / JWT / rate limit / embedding poisoning / prompt injection / LLM XSS / unsafe deserialization: not touched.

## Out-of-scope changes

| File | Status |
|---|---|
| `gateway/Cargo.toml` — `tracing-test` `no-env-filter` feature | dev-deps only, justified WHY comment, no production impact. Acceptable. |
| `SQL_CONNECTION_MAX_LIFETIME_SECS` `pub` + `init_sql_pool` `pub` | new public surface, both justified by integration-test imports (HIGH-1, LOW-7 closures). No unintended re-export via `pub use` chain (verified by grep). Acceptable. |

## Quality

- N+1 / blocking I/O / observability: clean.
- LOC: diff totals `+1041 / -241 = +800` per `git diff --stat`. R1 already acknowledged the LOC overrun (~+585 net effective post-relocation). R2 fixup adds ~120 net (`+197 / -78`) for test additions + helper extraction. Still over 300 LOC cap; module relocation accounts for ~280 of the overrun and is unavoidable. Acknowledged at R1; no change in posture.

## CI / lint posture (verified locally)

```
cargo fmt --check                          clean (no output)
cargo clippy --all-targets -- -D warnings  clean (Finished, no warnings)
cargo test --lib                           2 passed (auth_metadata::token::tests)
cargo test --test test_sql_pool            5 passed (4 AC-11 cases + AC-4 timing oracle)
```

## Required before APPROVE

None. R1 blockers resolved.

---

# Review — SEC-005 PR-B (workers)

Branch `feat/SEC-005-workers-iam-sql-token` @ `3edb55d`. PR target = `main`.
Scope = workers Python only; covers AC-6, AC-7, AC-9 and workers portion of AC-10, AC-12, AC-15.

## Verdict

**REQUEST_CHANGES**

Three HIGH issues: (1) `# type: ignore[union-attr]` (forbidden by `no-workaround.md`); (2) AC-9 / AC-10 log emission **not asserted** by the only AC-9 test (test just checks an exception escapes — does not verify `boot.sql_pool_init_failed` event nor `reason_code` nor bearer-redaction in captured logs); (3) AC-12(a) **not actually exercised end-to-end** — `test_create_pool_nominal_acquire` uses the bare `db_pool_required` fixture which calls `create_pool(url)` *without* `token_provider`, so the new injection codepath is not run against a real Postgres. Combined with the autouse `_mock_sql_token_provider_in_main` that *silently strips* `token_provider` for every other lifespan-using test, AC-12(a) oracle "create_pool succeeds with token_provider against Postgres" is not met.

Plus one MED (broad `except Exception` in lifespan boot) and one MED (reason-code classifier keys on `str(exc).contains(...)` of a string the same module produced — gaming-adjacent fragility).

Lints/tests green locally — but the green is masked by the autouse mock and the missing log assertions, so green ≠ correct here.

## Findings

| File:line | Severity | Pattern | Evidence | Suggested fix |
|---|---|---|---|---|
| `workers/src/archiviste_workers/auth_metadata/token.py:68, 71` | HIGH | `# type: ignore[union-attr]` — explicit forbidden pattern (`no-workaround.md` §Forbidden) | `return self._cached.bearer  # type: ignore[union-attr]` twice. `_is_cache_valid()` returns `True` only when `self._cached is not None`, but mypy can't narrow across a method call. The escape hatch hides what should be a typed predicate. | Inline the cache-validity check: `cached = self._cached; if cached is not None and ...: return cached.bearer`. Removes both `# type: ignore` and the redundant double-check pattern. |
| `workers/tests/test_sql_pool.py:219-239` | HIGH | AC-9 / AC-10 log emission not asserted | `test_lifespan_fails_on_metadata_500` only does `with pytest.raises((TokenFetchError, Exception))`. The spec mandates the test assert `event="boot.sql_pool_init_failed"` AND `reason_code="metadata_token_failed"` are emitted by `structlog` (AC-9 + AC-10), AND that no captured log line contains the mock token / `Bearer` (AC-10 §"Grep négatif"). None of these are checked. Worse: `pytest.raises((TokenFetchError, Exception))` is tautological — every raise matches `Exception`. | Use `structlog.testing.capture_logs()` (or `caplog`) around `lifespan_context`. Assert: (a) one captured entry has `event == "boot.sql_pool_init_failed"` and `reason_code == "metadata_token_failed"` and `phase == "boot"`; (b) no captured field value contains `"Bearer"`, `"eyJ"`, nor the mock token value. Tighten `pytest.raises` to `TokenFetchError` only. |
| `workers/tests/test_sql_pool.py:48-54` + `workers/tests/conftest.py:158-168` | HIGH | AC-12(a) not exercised — `db_pool_required` bypasses the `token_provider` path entirely | `db_pool_required` calls `await create_pool(_database_url())` (no `token_provider=`), then the test does `SELECT 1`. This proves only that the pre-SEC-005 codepath still works; it does NOT prove the new branch (`if token_provider is not None: kwargs["password"] = _password_cb`) succeeds against Postgres. AC-12(a) oracle: "respx mock metadata 200 + docker-compose Postgres; `create_pool(url, token_provider=...)` succeeds; `respx.assert_all_called()` ≥ 1." Not met. The companion `test_create_pool_injects_token_callback_wired` only calls the provider standalone; it never passes the provider to `create_pool`. | Add a true integration test: respx mock metadata; rewrite `_database_url()` so the URL has the *real* docker password but pass `token_provider=SqlTokenProvider(base_url="http://metadata.test")` — assert the callable is invoked (respx route called ≥ 1) AND a `SELECT 1` succeeds. Document why the callable's return is then ignored by Postgres (because docker-compose uses static password auth) OR use `trust` auth on a separate test DB. Alternatively, document explicitly that AC-12(a) requires Cloud SQL IAM and is verified only at runbook §8 — but then spec needs amendment, do not silently weaken it. |
| `workers/src/archiviste_workers/main.py:69` | MED | Broad `except Exception` at boot | `except Exception as exc:` catches `KeyboardInterrupt`-safe but also any unrelated bug (e.g. `AttributeError` from a refactor). Bug surfaces as `reason_code="cloud_sql_auth_failed"` (the fallback in `_classify_pool_init_error:46`), mislabeling the real failure. `security.md` §A05 ("Error responses : never leak stack traces") and clean-code §"swallowed error" both push back. The AC-9 spec mandates the four `reason_code` values, but the catch-all maps anything unknown to `cloud_sql_auth_failed` instead of, say, re-raising untyped. | Tighten to `except (TokenFetchError, asyncpg.PostgresError, OSError, TimeoutError) as exc:`. The classifier `_classify_pool_init_error` already enumerates exactly these four — make the catch match. Drop the `return "cloud_sql_auth_failed"` fallback at line 46 (dead). |
| `workers/src/archiviste_workers/main.py:49-55` | MED | Reason-code classifier keys on `str(exc)` substring match — fragile, gaming-adjacent | `_classify_token_fetch_error` does `if "timeout" in str(exc): return "timeout"`. The string is whatever `TokenFetchError("...")` got constructed with at `token.py:91, 93, 95`. The classifier and the producer share an implicit string contract that is invisible to the type system. If someone changes `TokenFetchError("network")` to `TokenFetchError("network error: ...")` the classifier still works by accident; if they change to `"transport_failure"` it silently degrades to `metadata_token_failed`. | Replace `TokenFetchError(str)` with an enum-tagged subclass tree or an explicit `kind` attribute: `class TokenFetchError(Exception): def __init__(self, kind: Literal["timeout","network","metadata_token_failed"]): self.kind = kind`. Classifier becomes `return exc.kind`. Removes string-match coupling. |
| `workers/tests/conftest.py:85-116` | MED | `autouse=True` fixture silently strips `token_provider` from every lifespan-using test | The fixture monkey-patches `archiviste_workers.main.create_pool` to drop `token_provider` before delegating. This means **every** test that exercises `lifespan` (`test_main_lifespan.py`, `test_conversation_integration.py`) runs against a pool that was created *without* the SEC-005 codepath. So the production `lifespan` path is only ever validated by `test_lifespan_fails_on_metadata_500` (which tests a fake create_pool side-effect, not the real one). Result: a regression in `db.create_pool` token-injection logic would not break any existing test. | Either (a) restrict the autouse fixture with `usefixtures` or a marker so only legacy tests get the patch, or (b) add at least one real lifespan test that exercises `main.create_pool(..., token_provider=real_provider_pointing_to_respx_mock)` end-to-end against docker Postgres (combine with HIGH-3 fix). |
| `workers/src/archiviste_workers/db.py:61` | MED | `assert pool is not None` for type narrowing | Plain `assert` is stripped under `python -O`. `pyproject.toml` ruff `[tool.ruff.lint.flake8-bandit]` typically flags S101 — currently it passes ruff, but `asyncpg.create_pool` can in fact return `None` only on `__aenter__` use (returns `Pool` directly when awaited per asyncpg docs). The assert is dead-defensive. | Drop the assert entirely; awaiting `asyncpg.create_pool(...)` returns `Pool`. If mypy complains, narrow with `cast(asyncpg.Pool, pool)` once and add a WHY comment about asyncpg's `Pool \| None` annotation being incorrect for the awaited path. |
| `workers/src/archiviste_workers/main.py:66` | LOW | `SqlTokenProvider()` constructed outside the try/except — AC-9 wording mismatch | Spec AC-9: "la création du `SqlTokenProvider` se fait en amont du `create_pool` afin que toute erreur d'instanciation soit fail-fast". It IS in amont, but if `httpx.AsyncClient(...)` ever raises (`OSError` on init in some embedded envs), the AC-9 log path does not fire. Marginal. | Move `sql_token_provider = SqlTokenProvider()` inside the `try:` block, or wrap separately with the same log emit. |
| `workers/tests/test_sql_pool.py:144-160` | LOW | Refresh-ahead test uses `expires_in=1` then `asyncio.sleep(0.05)` — the second call always re-fetches because `1 < REFRESH_AHEAD_SECONDS (60)` from t=0 already, so the cache is never even valid on the first call's *next* check | The check at `_is_cache_valid` is `now < expires_at - 60s`. With `expires_in=1`, `expires_at - 60s` is in the past at the moment of the first fetch, so the second call always re-fetches regardless of the sleep. Test passes but does not actually validate the refresh-ahead *threshold* — only that two get_or_refresh calls produce two fetches when the cache is immediately stale. The plan's intent (AC-12d) is to validate the 60s window crossing. | Use `expires_in=61` for first call (cache valid for ~1s), then `asyncio.sleep(1.1)` to cross the threshold; assert two fetches. Add a counter-test: `expires_in=120` + two consecutive calls = ONE fetch (already covered by `test_token_cache_single_fetch`, fine). |
| `workers/tests/conftest.py:42-51` | LOW | Existing `db_pool` fixture (un-renamed) still skips on Postgres unavailability — diverges from AC-12 "no skip CI conditional" | The `db_pool` fixture (pre-existing) does `pytest.skip(f"postgres unavailable: {exc}")`. AC-12 mandates "aucun skip CI conditionnel". Plan said to add `db_pool_required` *without* skip (done at `:158-168`), but the original `db_pool` was supposed to remain only for legacy non-AC-12 tests. The new SEC-005 nominal test correctly uses `db_pool_required`, so AC-12 is technically OK — but `test_create_pool_injects_token_callback_wired` (the main "wired" test) uses no Postgres at all, so the skip-vs-required distinction never bites for AC-12. Confirms HIGH-3 above. | No action on the fixture itself; covered by HIGH-3 fix. |

## Spec coverage (PR-B AC subset)

| AC | Status | Evidence |
|---|---|---|
| AC-6 (`create_pool` accepts `token_provider`, asyncpg async callable) | covered | `db.py:31-62`. OQ-2 correctly resolved via async callable + `max_inactive_connection_lifetime=45*60`. Not exercised against real Postgres (see HIGH-3). |
| AC-7(a) sqlservice.admin scope | covered | `test_fetch_uses_sqlservice_admin_scope` asserts `scopes=` and `sqlservice.admin` in called URL. |
| AC-7(b) cache hit skips refetch | covered | `test_token_cache_single_fetch` asserts `route.call_count == 1` after two `get_or_refresh()`. |
| AC-7(c) refresh-ahead window | weakly covered | `test_token_refresh_ahead` — works by accident (LOW finding above), does not test the 60s threshold semantic. |
| AC-7(d) SecretStr redacts in repr | covered | `test_secret_str_redacted_in_repr` checks `"super-secret-token" not in repr(...)` and `"**" in repr(...)`. |
| AC-9 (lifespan propagates + log emit + reason_code) | **not covered** | `test_lifespan_fails_on_metadata_500` asserts only an exception escapes — no `boot.sql_pool_init_failed` event capture, no `reason_code` assert, no bearer-grep-negative. HIGH-2. |
| AC-10 (reason_code mapping + no bearer/full DATABASE_URL in logs) | partial / unverified | Mapping logic exists in `_classify_pool_init_error` but no test exercises all 4 reason_codes; the bearer-redaction property is not asserted by any test. |
| AC-12(a) nominal create_pool with token_provider against docker Postgres | **not covered** | See HIGH-3. The "nominal" test calls `create_pool` *without* `token_provider`. |
| AC-12(b) boot fails on metadata 500 | covered for the provider; not covered for the boot integration | `test_boot_fails_on_metadata_500` calls `SqlTokenProvider().get_or_refresh()` directly. The boot integration path is not exercised end-to-end (per HIGH-2). |
| AC-12(c) cache hit single fetch | covered | `test_token_cache_single_fetch`. |
| AC-12(d) refresh-ahead two fetches | weakly covered | See LOW on threshold. |
| AC-13 (.env.example) | covered (PR-A) | `.env.example:18-22` has the SEC-005 comment. Verified. |
| AC-14 (runbook §8) | covered (PR-A) | `docs/runbook.md:170-195` has §8 Cloud SQL IAM auth section with grep verification command. |
| AC-15 (CHANGELOG entry) | covered | `CHANGELOG.md:30` PR-B Security entry present. |

## Property invariants

`specs/properties.md` (not re-read this round; assumed unchanged since PR-A review): no invariant touches the workers SQL pool path. No property test required.

## Security

Mapped against `.claude/rules/security.md`.

- **Secrets in code**: none. `"test-iam-token"`, `"super-secret-token"` are test fixtures with `# noqa: S107` justification (`test_sql_pool.py:37`). OK.
- **Sensitive types**: bearer wrapped in `pydantic.SecretStr` end-to-end (`token.py:33, 98`). `get_secret_value()` called only at `db.py:55` (inside the password callback, passed directly to asyncpg). Not logged. OK.
- **A09 logging — bearer leakage**: the spec mandates a test asserting captured logs contain no `Bearer`, `eyJ`, nor token value. **No such test exists in this PR** (HIGH-2 partly captures this gap). Without that assertion, the property is unverified — a future refactor that adds `logger.error(..., token=...)` would not fail any test.
- **A09 logging — DATABASE_URL leakage**: `lifespan` does not log `settings.database_url`. OK. But there is no negative-assert test for this either.
- **A10 SSRF**: metadata URL constants hardcoded `http://metadata.google.internal`. Base URL injectable only via `SqlTokenProvider(base_url=...)`, used only by tests. No user input. OK.
- **A03 / unsafe deserialization**: `response.json()` parses metadata-server response — trusted endpoint (allowlist-of-one). Body shape `{"access_token", "expires_in"}` accessed by key — `KeyError` on malformed → escapes as bare `KeyError` (not `TokenFetchError`), classified as `cloud_sql_auth_failed` by the fallback. MED-2 spillover.
- **CORS / CSP / HSTS / JWT / rate limit / embedding poisoning / prompt injection / LLM XSS**: not touched. OK.
- **Trust boundary**: workers compute their own token via metadata server, no client header trust. OK.

## Out-of-scope changes

| File | In-plan? | Status |
|---|---|---|
| `workers/tests/conftest.py` (51 LOC delta) | Plan §PR-B step 7 said "~10 LOC". Actual: 51 LOC including the autouse fixture. | Larger than planned. The autouse fixture is the root cause of MED-conftest finding above. Acceptable in principle (legacy test compat) but should be marker-scoped, not blanket-autouse. |
| `workers/uv.lock` | Implicit (transitive from `respx` dep). | Acceptable. |

## Quality

- N+1 / blocking I/O: clean.
- Function length: all PR-B functions ≤ 40 LOC. OK.
- Cyclomatic complexity: `_classify_pool_init_error` has 5 branches, OK; `_classify_token_fetch_error` has 3, OK.
- LOC: total diff `+486 / -13 = +473`. Excluding `uv.lock` (14): `+459`. Excluding tests (239 + 51 conftest delta = 290): `+169` source. Plan estimated ~266 incl tests, actual ~459 — **overrun ~73%**. Largely driven by conftest fixture (51) and the test file (239 vs planned 110). Spec rule says "≤ 300 LOC diff (excluding migrations and generated files)" — tests count per `vertical-slice.md`. PR is over budget. Tolerable given AC-12 mandates new test surface, but worth flagging.
- Observability: classifier exists, but no test asserts the classifier is actually invoked at boot (HIGH-2 / AC-10 gap).

## CI / lint posture (verified locally in worktree)

```
uv run ruff check .            All checks passed!
uv run mypy src/               Success: no issues found in 43 source files
uv run pytest tests/test_sql_pool.py        8 passed
uv run pytest tests/test_main_lifespan.py   4 passed
uv run pytest tests/                        14 failed, 231 passed, 1 skipped, 11 errors
```

The 14 failures + 11 errors in the full suite are pre-existing environment issues (`UndefinedTableError: relation "query_log" does not exist` — DB migrations not applied to local docker Postgres), unrelated to PR-B. **Not a blocker for this review**, but the implementer should confirm CI runs migrations before pytest.

## Required before APPROVE

1. Remove both `# type: ignore[union-attr]` in `auth_metadata/token.py` (HIGH-1).
2. Rewrite `test_lifespan_fails_on_metadata_500` to capture `structlog` output and assert `event="boot.sql_pool_init_failed"` + `reason_code="metadata_token_failed"` + no token leakage in any captured field. Tighten `pytest.raises` to `TokenFetchError` (HIGH-2).
3. Add a true integration test that calls `create_pool(url, token_provider=<respx-mocked provider>)` against docker-compose Postgres and proves the async callable path executes against a real DB (HIGH-3).
4. Tighten the boot `except Exception` in `main.py:69` to the exact exception tuple the classifier handles, and drop the `cloud_sql_auth_failed` fallback in `_classify_pool_init_error` (MED-broad-except).
5. Replace `TokenFetchError("kind-string")` with an enum-tagged construction so the classifier doesn't substring-match a message it produces itself (MED-classifier-fragility).
6. Either marker-scope the `_mock_sql_token_provider_in_main` autouse fixture or remove it once HIGH-3's true integration test exists (MED-autouse).

LOW findings (assertion strip, refresh-ahead threshold, token provider try-block placement, db_pool legacy skip) may be deferred but should be tracked.
