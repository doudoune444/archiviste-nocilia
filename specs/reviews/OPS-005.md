# Review — OPS-005

## Verdict
APPROVE

Re-review after fix commit `b741a72`. The single HIGH blocker from the prior round (REQUEST_CHANGES) is resolved: `max_retries = 0` moved from the OUTER `template` (ExecutionTemplate) into the INNER `template.template` (TaskTemplate, sibling of `service_account`/`containers`/`volumes`). `terraform validate` now passes green (exit 0, no second nested error hidden behind the first). `terraform fmt -check` clean. The fix is surgical — lines + their WHY comment relocated, no logic change. All AC coverage and security posture from the prior round hold. Approved.

## Findings (resolved + open)

| File:line | Severity | Pattern | Status |
|---|---|---|---|
| infra/terraform/cloud_run_job.tf:22 | HIGH (was) | broken config / failed `terraform validate` | RESOLVED in `b741a72` — `max_retries = 0` now in inner `template.template` (l.22), validate exit 0, no second error surfaced. |
| infra/terraform/cloud_run_job.tf:30 | LOW | pattern divergence (no legacy v1 cloudsql annotation; only v2 volume) | OPEN — non-blocking. v2 `volumes.cloud_sql_instance` is the native mechanism; annotation is legacy. Cosmetic parity only. |

## Lint / build status

| Check | Result |
|---|---|
| `terraform fmt -check` (infra/terraform) | PASS (exit 0) |
| `terraform validate` (infra/terraform, after `init -backend=false`) | PASS — `Success! The configuration is valid.` (exit 0) |
| YAML parse `ingest-lore.yml` | PASS (prior round) |
| YAML parse `gdrive-sync.yml` | PASS (prior round) |
| cargo / pytest / ruff / mypy | N/A — no Rust/Python source touched. Diff = 5 in-scope files (2 workflows, 1 .tf, runbook, CHANGELOG) + this review. Confirmed via `git diff --stat main...HEAD`. |

## Spec coverage

| AC | Status | Evidence |
|---|---|---|
| AC-1 (`google_cloud_run_v2_job archiviste-ingest`, europe-west9, workers image, secret_key_ref Mistral, SA archiviste-runtime) | ✓ | cloud_run_job.tf:5-7 name+`var.region`; image `${local.ar_base}/workers:latest` (l.39); `service_account = google_service_account.archiviste_runtime.email` (l.17); MISTRAL_API_KEY `secret_key_ref` (l.77-85). "sidecar" interpreted as managed Cloud SQL volume per plan R1 — interpretation holds. |
| AC-2 (command, CLOUD_SQL_IAM_AUTH, INSTANCE_CONNECTION_NAME, volume cloudsql, DATABASE_URL SA email percent-encoded) | ✓ | command `["python","-m","archiviste_workers.ingest","--path","lore/"]` (l.44); `CLOUD_SQL_IAM_AUTH=true` (l.61-64); `INSTANCE_CONNECTION_NAME` (l.56-59); volume `cloudsql` + mount `/cloudsql` (l.30-35, 87-90); DATABASE_URL `replace(...,"@","%40")` — identical to cloud_run.tf:221. |
| AC-3 (`on push branches:[main] paths:['lore/**']`) | ✓ | ingest-lore.yml:5-10. `workflow_dispatch` added (convenience, allowed by Non-goals). |
| AC-4 (WIF auth@v2, no SA JSON key) | ✓ | ingest-lore.yml:38-42 `google-github-actions/auth@v2` + `workload_identity_provider` + `service_account`. No `*-sa.json`. |
| AC-5 (`gcloud run jobs execute --wait --region europe-west9`, propagates failure) | ✓ | ingest-lore.yml:52-56; `env REGION: europe-west9`; `--wait` blocks to terminal state, gcloud exits non-zero on non-Succeeded → step red. |
| AC-6 (Job exit 0/1 from ING-001) | ✓ (inherited) | Behaviour owned by ING-001. `max_retries=0` (l.22) preserves the signal — now actually applies (resource is creatable post-fix). |
| AC-7 (exit→state→verdict mapping) | ✓ | Encoded via `max_retries=0` (l.22, no retry masks exit 1) + `--wait` propagation. Now fully realized — the prior NOTE (resource uncreatable) no longer applies. |
| AC-8 (`concurrency group ingest-lore cancel-in-progress:false`) | ✓ | ingest-lore.yml:21-23, exact match. |
| AC-9 (`ingest.summary` in Cloud Logging by execution ID) | ✓ (inherited/doc) | Cloud Run Jobs auto-ship stdout to Cloud Logging; documented in runbook. |
| AC-10 (runbook rewrite + gdrive-sync checklist) | ✓ | runbook.md rewritten (auto trigger + manual fallback + embedder correction R4); gdrive-sync.yml:141 checklist replaced with "runs automatically via ingest-lore.yml". |

## Security

| Check | Result |
|---|---|
| Secrets in code | None. MISTRAL_API_KEY via `secret_key_ref` (cloud_run_job.tf:77-85), never plaintext. |
| DATABASE_URL secret exposure | Clean — IAM authn, empty password, percent-encoded SA email; no password literal. |
| WIF / no SA JSON key (secret-hygiene) | ✓ OIDC only; no JSON key file or secret. |
| Least-privilege SA | ✓ Job runs as `archiviste-runtime` (Cloud SQL instanceUser + Mistral secret accessor only). |
| A09 logging — no secrets logged | ✓ Nothing logs the token/key; `ingest.summary` carries no key/doc content. |
| Trust boundary | ✓ Trigger = push to protected `main` (human-reviewed); ingester reads versioned `.md` only (ING-001 NFKC + 1 MiB cap). |

## Gaming patterns
None detected. Fix commit is a pure relocation — no hardcoded values, stub returns, swallowed errors, or disabled tests introduced. `max_retries=0` and `lifecycle ignore_changes` retain accurate WHY comments.

## Out-of-scope changes
None. `git diff --stat main...HEAD` = exactly the 5 plan "Files to touch" + this review doc. Fix commit `b741a72` touched only `cloud_run_job.tf` + this review. TOCTOU ING-001 (`pipeline.py:98-109`) untouched — debt correctly left open per plan/spec.

## Quality
- No N+1, no blocking I/O (infra/CI only).
- Diff ~180 LOC added, under the 300 vertical-slice cap.
- Comments WHY-only and accurate (R1/R4/R5 reflected).

## Required action
None. Ready to merge.
