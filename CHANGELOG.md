# Changelog

All notable changes to this project will be documented here.

Format: [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).
Versioning: [SemVer](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added

- **CHAT-001** (gateway + workers): SSE token streaming end-to-end. Workers exposes `POST /v1/generate/stream` emitting `text/event-stream` with event grammar `meta → token* → (done | error)`. Gateway exposes `POST /v1/chat/stream` that relays the SSE byte stream verbatim (no buffering). Conversation persistence and lore-gap ticket creation occur only after a clean `done` event; on mid-stream LLM failure an `error` SSE event is emitted and nothing is persisted. Pre-stream validation failures (bad body, invalid tier) return a normal JSON error envelope. `LlmClient.astream()` added to `services/llm.py` yielding `(delta_text, None)` per token and `("", aggregated_message)` as terminal sentinel for usage/citation extraction. Adversarial-review fixes: (FIX 1) `query_log` row inserted on every streaming failure path (retrieve_failed, llm_timeout, llm_upstream) for all modes — parity with blocking `router.py`; lore_gap/off_topic failure rows carry classifier-only tokens, canon/mystery carry `None`. (FIX 2) New tests prove the lore_gap done path calls `create_or_increment` exactly once, and the timeout/upstream failure paths call neither `create_or_increment` nor `conversation_client.append_message`. (FIX 3) `_finalize_canon` INFO log now includes `chunks=<count>` matching the blocking router. (FIX 4) Retrieve-failure SSE path covered by test asserting `error` code `retrieve_failed` and failure log row. (FIX 5) `_StreamContext` widened to carry service handles (`conversation_client`, `query_log_repo`, `db_pool`, `embedder`); per-mode computed values bundled in `_CanonResult`, `_SimpleResult`, `_LoreGapExtra` dataclasses — all `_finalize_*`/`_stream_*` signatures now ≤ 4 parameters.

- **#193 PLATFORM-004 adversarial-review fixes** (infra + frontend): (#1 BLOCKER) `CLOUD_RUN_SA_IDENTITY_URL` env added to the frontend Cloud Run container so `bff-proxy.fetchIdToken()` reaches the metadata server in production — without it the SA-gated gateway returned 403 on every call. (#2 BLOCKER) Deploy canary smoke now attaches `Authorization: Bearer $(gcloud auth print-identity-token --audiences=<CANARY_URL>)` so the SA-gated gateway canary does not self-rollback; `gateway_deploy_invoker` IAM binding added in `iam.tf` granting `roles/run.invoker` to `gha-deploy` SA on the gateway. (#3 HIGH) `middleware.ts` sets the CSP on both request headers (Next.js 15 nonce injection) and response headers (browser enforcement) with the same per-request nonce — previously only the response header was set. (#4 HIGH) `playwright.config.ts` `webServer.command` changed from `npm run dev` to `npm run build && npm run start` so security-header and CSP-nonce specs run against the production standalone server; build is currently blocked by a pre-existing observability/page.tsx export error fixed in a separate slice. (#6 MEDIUM) `frontend.Dockerfile` runtime stage now creates a non-root `app` user and runs `CMD` as `USER app`. (#7 MEDIUM) `frontend.Dockerfile` base images pinned to `node:22-alpine@sha256:5e8888a165087a80513a7e773bb1a60c2e7dd54ac7cddab404ae2f470815e8e8` (linux/amd64, resolved 2026-06-18). (#8 NIT) `npm ci --frozen-lockfile` changed to `npm ci` (the flag is yarn/pnpm-only, silently ignored by npm). (#10 LOW) `middleware.ts` now emits `Strict-Transport-Security: max-age=31536000; includeSubDomains; preload` at gateway parity. Latent-coupling comment added to `bff-proxy.ts` documenting that the SA identity token is attached on all `forward()` calls, which would cause a spurious 401 on any future `AuthUser`/`RequireAuthor`-gated route proxied through BFF.

- **#193 PLATFORM-004** (infra + frontend): second Cloud Run service for the Next.js frontend as the sole public web origin; gateway demoted to SA-gated (IAM trust boundary). Terraform: new `google_cloud_run_v2_service.frontend` (min=0, max=20, `GATEWAY_URL` env = gateway URI); `gateway_public_invoker` (allUsers) replaced by `gateway_runtime_invoker` (archiviste-runtime SA); `frontend_public_invoker` (allUsers) added; `gateway_iam_no_public_invoker` check block added to `checks.tf` mirroring the workers guard (SEC-006 pattern); Cloudflare CNAME + Worker `ORIGIN_HOST` repointed from `gateway.uri` to `frontend.uri`; `frontend_url` output added. Docker: `infra/docker/frontend.Dockerfile` multi-stage Node 22 build → Next.js standalone runtime (Cloud Run `$PORT=8080`). CI: `deploy.yml` adds frontend build/push (`:sha` tag, `context: frontend`), canary deploy, canary-ready assertion, promote, and rollback steps. Frontend: `next.config.ts` adds `output: "standalone"` and removes the `headers()` block; `src/middleware.ts` (new — must be under `src/` for App Router projects where `pagesDir` is unset; placing it at the project root causes Next.js dev bundler to miss it since it scans `path.join(appDir, '..')` = `src/`) emits per-request nonce-based CSP at gateway parity (`script-src 'self' 'nonce-…'; style-src 'self' 'nonce-…'; base-uri 'none'; form-action 'self'; img-src 'self' data:; object-src 'none'; frame-ancestors 'none'`) — no `'unsafe-inline'`; `bff-proxy.ts` attaches `Authorization: Bearer <ID token>` (audience = gateway URL) fetched from the Cloud Run metadata server (`CLOUD_RUN_SA_IDENTITY_URL`) so the SA-gated gateway accepts requests; 3 Vitest unit tests cover ID-token injection, absent-env skip, and propagated metadata failure. Playwright e2e: `security-headers.spec.ts` asserts all 3 security headers, nonce presence, no `'unsafe-inline'`, and zero CSP console violations during hydration. `docs/architecture.md` topology updated: browser → frontend → gateway → workers.

- **#198 PLATFORM-003** (gateway + frontend): auth-aware nav — cross-layer vertical. Gateway: `GET /v1/me` now returns `email: string | null` for member/author callers via a new `find_email_by_id(user_id)` method on the `UserLookup` trait (bound-param `SELECT email FROM users WHERE id = $1`; never `format!` into SQL). Email is fail-soft: if `user_lookup` is absent or the DB query errors, the identity response still returns `email: null` — the identity endpoint never 500s on an email lookup failure (A09: email is never written to any log or tracing span). `InMemoryUserLookup` mock updated to store email per user for reverse id-lookup. Frontend: `AuthAwareNav` server component (CSS Module, co-located) reads `GET /v1/me` through `bff-proxy` and renders a persistent, tier-driven header on every page. Static view links (Chat `/`, Board `/board`, Observabilité `/observability`) appear for all tiers. Auth cluster is tier-driven: anonymous → "Se connecter"/"S'inscrire"; member → email + "Se déconnecter"; author → email + "Se déconnecter" + dashboard link. Fail-soft: any fetch/JSON/shape error degrades to the anonymous variant — layout never crashes. Responsive. 20 Vitest unit tests cover all tiers, dashboard author-only guard, static links, and 5 fail-soft degradation scenarios.
- **WEBOBS-001** (frontend): public observability page at `/observability`. RSC route fetches `GET /v1/stats` and `GET /v1/quality` through the bff-proxy independently so one failing signal never blanks the other card. `StatsCard` shows the conversation count; `RagasGauges` renders the four Ragas scores (faithfulness, answer relevancy, context precision, context recall) as labeled bar gauges with the golden-set version and finished-at timestamp. `{"status":"no_data"}` renders a clean empty state; any load failure shows a generic error message with the request id — no gateway internals leaked. Shared discriminated-union types (`StatsResult`, `QualityResult`) in `lib/observability-types.ts` make illegal states unrepresentable. Review fixes (WEBOBS-001): (1) BLOCKER: `fetchQuality` now validates the 200 body with a small predicate (`isValidQualityMetrics`) requiring all four scores to be finite numbers and `golden_set_version`/`finished_at` to be present strings before returning `kind:"ok"` — an `{}` body or shape drift now yields `kind:"error"` instead of crashing the RSC. (2) `fetchStats` validates `conversation_count` is a finite number. (3) `Gauge` bar width clamped to [0,1] so an out-of-range score never produces negative width or overflow. (4) `error.tsx` segment added as a defence-in-depth client error boundary so any future render throw is caught at the segment level without blanking sibling cards. 33 Vitest unit tests (13 new fetch-layer tests cover malformed-200, rejection, and non-200 for both signals).
- **BOARD-002** (frontend): public RSC lore-gap board + shared `confirmation-badge` and `ticket-table` modules. New `GET /board` RSC page fetches open tickets server-side via bff-proxy (`/v1/board?sort=priority&limit=20&offset=0`). `ConfirmationBadge` enforces the FIX-BADGE truthfulness invariant: renders "non confirmé par les juges" ONLY when `judges_not_passed === true`; ALL other cases render neutrally with NO affirmative claim. `TicketTable` renders priority as visual intensity (`data-level` high/medium/low), category as chips, question as safe React text (never `dangerouslySetInnerHTML`), date as `<time>`, and the badge. Empty-board state spans all 5 columns. Long lists paginate via a `'use client'` `LoadMoreButton` that fetches from the new internal route `GET /api/v1/board` (which forwards query params to the gateway). Error state carries the `x-request-id` relayed by bff-proxy; no gateway internals leaked. Nav gains a "Tickets" link. 6 Vitest badge truth-table tests + 12 TicketTable RTL render tests (Playwright smoke substituted — requires live server, test coverage is complete at the component level). Note: Playwright board smoke deferred to a follow-up slice (requires running Next.js server in CI). Review fixes: (1) AC5 BLOCKER — `fetchInitialBoard()` wraps both `forward()` and `res.json()` in try/catch; all throw paths (network error, AbortSignal timeout, unset GATEWAY_URL, malformed JSON) route to the error UI with a request id; `?? crypto.randomUUID()` fallback moved exclusively to the catch path where no Response exists. (2) AC2 — badge tests strengthened: `false`/`undefined` cases assert `toBeEmptyDOMElement()`; affirmative-claim guard uses `/(?<!non\s)confirmé/i` (negative-lookbehind) that genuinely excludes the negative label. (3) Runtime shape guard `isBoardPage()` added to `types.ts` (no zod dep); both `page.tsx` and `LoadMoreButton.tsx` guard before accessing `page.items` — prevents `undefined.map()` on shape-drifted 200 responses. (4) `BOARD_PAGE_SIZE = 20` hoisted as a single named constant in `types.ts`. (5) 4 new Vitest tests cover forward()-throw, non-ok response, malformed-200 body, and JSON-parse-throw.

- **BOARD-001 AFK** (gateway): `GET /v1/board` accepts optional `?category=<text>` filter (exact match, bound param — no SQL interpolation) and `?sort=priority|date` ordering. `sort` is modelled as a `SortOrder` enum deserialized from the query param with a `Priority` default; four separate `sqlx::query_as` literal SQL calls express the four (category × sort) combinations — no `format!` macro is ever used. Omitting both params preserves today's exact behaviour (backward-compatible).
- **#192 PLATFORM-002** (frontend): `bff-proxy` deep module — the single server-side place that calls the Rust gateway on behalf of the current request. `forward(request, gatewayPath)` injects `archiviste_session` and `archiviste_anon` cookies onto the outbound call, propagates (or generates) an `x-request-id`, and relays any gateway `Set-Cookie` back to the browser unchanged. Gateway URL (`GATEWAY_URL`) and both cookie names are named constants confined to this module; nothing else in the codebase hardcodes them. App Router route `GET /api/v1/me` exercises the module end-to-end. Fix (CRITICAL): multiple `Set-Cookie` headers from the gateway are now relayed via `getSetCookie()` + `append()` per cookie, preventing comma-join corruption of `Expires` dates when the gateway sets both `archiviste_session` and `archiviste_anon` in the same response. Fix (A04): gateway fetch now carries a 30-second `AbortSignal.timeout` (`GATEWAY_TIMEOUT_MS` named const) — no timeout previously. 13 Vitest unit tests cover cookie-forward (both present, only session, only anon, neither), single/multi Set-Cookie relay, request-id reuse/generation, URL composition, status relay, and presence of the abort signal.
- **#191 PLATFORM-001** (frontend): Next.js 15 App Router bootstrap. `frontend/` scaffold with App Router + React Server Components + TypeScript strict. Global layout shell (`RootLayout`) with warm-brown / near-black / off-white design tokens matching `gateway/static/assets/styles.css` exactly (CSS Modules, no Tailwind). Responsive nav + main + footer. Home page route. Vitest unit tests + Playwright smoke test. CI `frontend` job (typecheck + lint + vitest + playwright). ADR-0012 authored; ADR-0005 marked superseded.

### Fixed

- **frontend build** (frontend/ci): `npm run build` failed on `observability/page.tsx` because Next.js 15 rejects any non-reserved named export from a page file (`fetchStats`/`fetchQuality` triggered `Type 'OmitWithTag<…>' does not satisfy the constraint`). The two server-side fetchers (and their private validators) moved to a co-located `observability/fetch.ts`; `page.tsx` now imports them and exports only the page component. The unit test imports from `@/app/observability/fetch`. CI gained a `build` step (`npm run build`) between vitest and playwright — `tsc --noEmit` does not enforce Next's page-export constraints, so the production build broke silently on `main`. No runtime behaviour change.

- **#173** (frontend): distinct non-recoverable message for `skipped_error` on the send-anyway path. When workers returns `ticket_action="skipped_error"` (HTTP 200, server-side write failure) after a force=true submission, the UI now shows "Le serveur n'a pas pu enregistrer le signalement. Réessayez plus tard." and leaves "Envoyer quand même" disabled (non-recoverable) while re-enabling "Annuler" only — instead of the generic recoverable retry copy that wrongly re-offered a retry the user could never resolve.

- **#172** (workers/gateway/frontend): typed signal outcome + robust verdict parse. (1) `parse_verdict` now scans for the first known keyword (`PRESENT`/`ABSENT`/`CONTRADICTION`/`UNCLEAR`) at a word boundary in the uppercased reply, replacing the ASCII-only leading-token regex that mis-classified accented-lead replies (e.g. `"Évaluation: PRESENT ..."` → spurious `unclear`). (2) Workers response gains an explicit `outcome: Outcome` field (`confirmed`/`refused`/`indecisive`): `confirmed` when judges majority is absent/contradiction (ticket raised); `refused` when majority is present (lore consistent); `indecisive` otherwise (unclear, no-majority, no-sources). A `force=True` ticket never reports `confirmed` — it stays `refused`/`indecisive` (untrusted path). (3) `unclear` removed from `TICKET_TRIGGERING_VERDICTS` → `unclear >=2` is now `indecisive` with no ticket. (4) Gateway passes `outcome` through byte-for-byte (no Rust logic change). (5) Frontend branches on `body.outcome` for distinct human-readable messages; raw verdict token no longer shown.

- **#183** (workers): fix first-turn 422 race + self-heal on missing GCS object. (A) `conversations.created_at` is now set to the first message's `timestamp` instead of DB `NOW()`, eliminating the app-clock vs DB-clock false 422 `timestamp_before_conversation` on turn 1. `ConversationRepository.create_if_absent` gains a `created_at: datetime` parameter threaded from the router. (B) `_read_modify_write` now catches `google.api_core.exceptions.NotFound` on `download_as_bytes` and raises `ConversationObjectMissingError` (new); the router handles it by recreating the GCS object header and retrying the append exactly once, self-healing all orphaned conversations. `specs/acceptance/ING-003.md` AC-2 wording updated (human-approved amendment).
- **diag** (workers/observability): GCS storage failures no longer discard their cause. `GcsConversationStorage` now logs a `gcs_storage_error` event (with `op`, `conversation_id`, `error_type`, `error`) at each `StorageUnavailableError` raise site (`upload_header`, `download_block`, `upload_block`) before re-raising. Previously the underlying `google-cloud-storage` exception was swallowed (`raise StorageUnavailableError from exc` with no log), so a prod conversation-persistence failure surfaced only as an opaque 503 and an empty `conversation_messages` table ("0 messages on reload") with no diagnosable cause.
- **#169** (workers): align workers tier vocabulary to the gateway↔workers contract. `X-User-Tier` values `member` and `author` (contract vocab per `specs/openapi/gateway-to-workers.yml`) were previously rejected by both `generate` and `contradiction` ingress routers with 422/400, causing authenticated members and authors to get no answer and no persistence. Both routers now accept contract values and map them to internal ACL tier names at the boundary (`member → members`, `author → author_only`). Internal ACL logic (`services/acl.py`, `retrieve/schemas.py`) is untouched. Anonymous path unchanged.
- **#176** (gateway/frontend): board badge neutral default + colSpan fix. `judges_not_passed=true` tickets show the "non confirmé par les juges" badge; all other tickets (judge-confirmed, legacy, auto-created with `false`) render neutrally with no badge. Empty-board placeholder cell now spans all 5 columns (was 4, stale after the "Confirmation" column was added in #163).
- **#175** (workers): force signal ("Envoyer quand même") now always inserts a new ticket with `judges_not_passed=True`, bypassing cosine dedup. Previously a forced override that matched an existing open ticket would increment it via the dedup branch, inheriting its "confirmé" badge and becoming invisible as an override on the board. Fix: `create_or_increment` gains a `force: bool = False` parameter; when `force=True` the dedup SELECT FOR UPDATE is skipped and a new row is always inserted. `verify_contradiction` threads `force=True` to the forced call. New tests: `test_force_always_inserts_new_ticket_bypassing_dedup` (unit, ticket_service), `test_non_force_cosine_match_increments_existing_ticket` (replaces old increments-flag test), `test_force_always_creates_new_ticket_even_when_similar_open_exists` (integration, contradiction service), and `force` assertions on the existing spy-based tests.

### Added

- **#163-review** (gateway/frontend): review fixes on top of the #163 slice. (1) **A01 IDOR** (MED): `POST /v1/report-contradiction` now verifies conversation ownership in the gateway handler before forwarding — `SELECT user_id FROM conversations WHERE id = $1`; non-owner or absent id → uniform 404 `conversation_not_found`, not forwarded to workers (mirrors HIST-001 pattern in `conversations.rs`). (2) **Frontend orphan guard** (LOW): submit and send-anyway handlers no longer mint a fresh `conversation_id` via `ensureConversationId()`; if `loadConversationId()` returns null they show "Aucune conversation à signaler — posez d'abord une question." and stop; a 404 response from the gateway shows the same message. (3) **Stuck submit button** (LOW): after a successful first submit the submit button is re-enabled and the claim textarea is cleared; send-anyway success also re-enables all buttons; no path leaves the panel open and stuck. (4) **WHY-comments restored** (LOW): three security-rationale comments deleted by the #163 commit are restored in `app.js` (sources list `textContent` injection guard, history render `textContent` guard, reopen cross-owner 404 note, `loadConversationId()` null guard). New DB-backed tests: `idor_foreign_conversation_returns_404_workers_not_called`, `idor_nonexistent_conversation_returns_404`, `idor_owned_conversation_is_forwarded`.

- **#163** (signal): signal anytime + "send anyway" tagged ticket. (1) **Persistent "Signaler" button** always visible in the chat composer footer (no longer gated on citations). (2) **Two-step flow**: first submission runs the judge panel; if judges did not confirm (`ticket_action == "not_raised"`), the visitor is shown the verdict+reason and offered "Envoyer quand même" / "Annuler". (3) **`force=true` override**: second submission with `force=true` raises a ticket even without judge confirmation — tagged `judges_not_passed=true` in the DB so the board can distinguish it. (4) **Migration** `0007_tickets_judges_not_passed.up/down.sql` adds a `judges_not_passed BOOLEAN NOT NULL DEFAULT false` column to the `tickets` table. (5) **Workers**: `VerifyContradictionRequest` gains an optional `force: bool = False`; `verify_contradiction` applies the force path only when `should_raise` is false; `create_or_increment` writes `judges_not_passed` on INSERT only (increment is immutable). (6) **Gateway**: `report_contradiction.rs` parses and forwards optional `force` bool; invalid type → 400. (7) **Board**: `judges_not_passed` surfaced in `GET /v1/board` and `GET /v1/tickets` JSON items; `board.js` renders a "confirmé" / "non confirmé par les juges" badge via `textContent`. (8) **Frontend** (`app.js` + `index.html` + `styles.css`): old citation-gated `appendReportControl` removed; replaced by persistent panel with `id="signaler-*"` elements; `contradiction_confirmed` read removed; response shape now reads `ticket_action`/`verdict`/`reason`. (9) **Tests**: 3 workers unit tests, 3 gateway report tests, 2 board tests (1 DB-backed). **FLAG: `migrations/0007_tickets_judges_not_passed.{up,down}.sql` and `specs/openapi/gateway-to-workers.yml` are human-only files modified here — require explicit human sign-off before merge.**

- **#162** (signal): reasoned retrieval-backed contradiction/lore-gap judging. Clean-break redesign of `POST /v1/verify-contradiction`. (1) **4-way verdict enum** — `present / absent / contradiction / unclear` replaces the old boolean; response shape drops `contradiction_confirmed`/`confirmations` and gains `verdict` (enum) + `reason` (synthesised single-sentence). (2) **Reframed prompts** — three independent lenses (facts/chronology/entities) now reason about presence vs absence vs contradiction; refute-only framing removed. (3) **Ticket policy** — any non-present verdict winning >=2 votes raises/increments a ticket; `present` >=2 and split votes do not. (4) **No-citation retrieval path** — `citations` is now optional; when absent/empty, workers embeds the claim and retrieves top-5 ACL-filtered chunks as judge sources. (5) **Redaction by construction** — judge prompt forbids quoting source text; synthesised reason references only verdict + `source_path` identifiers. Per-judge reasons logged server-side only. (6) **Gateway updated** — `parse_and_validate` accepts absent/empty citations; test assertions updated to new `verdict`/`reason` shape. New tests: 4-way vote threshold (5 parametrised cases), no-citation retrieval path, redaction assertion, `parse_verdict` matrix (13 cases), model-level optional-citations validation. **FLAG: `specs/openapi/gateway-to-workers.yml` updated — requires explicit human sign-off.**



- **CTR-002** (gateway): public `POST /v1/report-contradiction` endpoint + per-answer "Signaler une incohérence" report control. The gateway validates `{claim (1..4096 bytes), conversation_id (uuid), citations (1..50 items)}`, forwards to workers `POST /v1/verify-contradiction` with `X-User-Id`/`X-User-Tier` identity headers (never trusts a client-supplied `request_id` — always gateway-generated), and passes the 200 verdict body through byte-for-byte. Invalid inputs return 400 `invalid_request`; upstream errors → 502/503/504 with the standard error envelope; the raw claim is never logged. Frontend: each canon answer with citations gains an inline "Signaler une incohérence" button; clicking opens a textarea for the claim, which is POSTed as JSON to `/v1/report-contradiction`; on confirmed contradiction the visitor sees a confirmation message, on refute a neutral message, on error a generic retry message — no internal details ever leaked. All text rendered via `textContent` (never `innerHTML`). Runbook §12 updated with a CTR-002 edge rate-limit note. New integration tests: 13 cases covering the happy path (header propagation, verdict passthrough), all validation error shapes, upstream failure modes (timeout → 504, body-limit → 400), and `X-Gateway-Overhead-Ms` parity with chat.

- **CTR-001** (workers): contradiction verification endpoint `POST /v1/verify-contradiction` (internal gateway↔workers). A visitor's contradiction report — `claim` + the answer's `citations` (`{source_path, chunk_ords}`) + `conversation_id` — is verified by **three independent refute-biased judges** over (claim, sources); on **≥2 confirmations** the existing lore-gap `ticket_service.create_or_increment` raises/increments a ticket carrying the claim verbatim (no new ticket type/category, reuses the `tickets` table — **no migration**). Fewer than two confirmations raises nothing. **Source integrity**: the cited chunk text the judges see is re-resolved server-side from the trusted `chunks`/`documents` store (`contradiction/repository.py`), ACL-bounded by `X-User-Tier` (reuses `ALLOWED_ACCESS_TIERS_BY_USER_TIER`), so a client cannot forge source bodies to force a confirmation (security.md A01); nothing visible to the caller → no judges run, no ticket. The three judges use distinct lenses (literal / timeline / entity-consistency) so they stay independent even at temperature 0, run concurrently (`asyncio.gather`), are refute-biased (default `NO_CONTRADICTION`; ambiguous, empty or a failed judge → refute, never a bogus ticket), and see claim+sources only in the untrusted Human zone, never the system role (security.md A03 / RAG-specific). New `contradiction/{models,prompt,repository,service,router}.py` + `main.py` router wiring. **Contract**: `specs/openapi/gateway-to-workers.yml` gains the endpoint plus `VerifyContradiction{Request,Response,Error}` + `Citation` schemas (human-approved). The public gateway endpoint, the per-answer "report an inconsistency" control, and the edge rate-limit are deferred to **CTR-002**. Tests (`tests/test_contradiction.py`): vote threshold 0/1/2/3 with a fake LLM, confirmed→ticket(claim) / unconfirmed→none, refute-biased parsing matrix, DB-backed ACL re-resolve dropping above-tier chunks, and request validation mapping to the contract error enum.
- **HIST-001**: end-user conversation history + closes the signed-url IDOR. Three owner-scoped gateway routes, all resolving the caller via the `AnonIdentity` extension (anonymous cookie `UUIDv5`, or member/author JWT `sub`) so unregistered visitors reach them: (1) `GET /v1/conversations` lists the caller's own conversations (`WHERE user_id = caller`, newest-activity first, `LIMIT 50` — security.md A04); (2) `GET /v1/conversations/{id}/messages` returns the conversation's turns read **directly from the structured `conversation_messages` store** (MEM-001) — AFK, no workers call, no `gateway↔workers` contract change — with ownership enforced in SQL (`JOIN conversations` on `user_id`, `ORDER BY ordinal ASC`, `LIMIT 500`); a non-owner or unknown id collapses to 404 `conversation_not_found` so existence cannot be probed. (3) `GET /v1/conversations/{id}/signed-url` is now **owner-or-author** (was author-only, UI-002): authors still read **any** conversation's signed URL (moderation dashboard unchanged), every other tier is restricted to conversations they own — closing the signed-url IDOR (`SELECT … WHERE id = $1` gained `AND user_id = $2` for non-authors; security.md A01). The route is no longer behind the `RequireAuthor` gate, so it moved from the `dashboard_api` router to a new `conversations_api` router and a member/anonymous/expired-JWT caller is no longer 401/403-rejected — an expired JWT is discarded by `resolve_identity` and treated as anonymous (cookie identity), never an auth bypass. **Frontend** (`static/index.html` + `app.js` + `styles.css`): a "Conversations précédentes" list loads on page open and after each first turn; clicking an entry reopens it (renders its turns and continues that `conversation_id`). All server data is rendered via `textContent` only (never `innerHTML`) — no inline script/style/handlers, CSP-compatible. History is durable across reloads because it is tied to the `archiviste_anon` cookie identity (IDN-001). Tests: new `gateway/tests/test_hist001_history.rs` (DB-backed) asserts list-own-only + order, owner turns read in order, cross-owner messages → 404, signed-url non-owner → 404 (IDOR), owner/author → pass ownership; the three UI-002 author-only signed-url tests in `test_dashboard_backend.rs` were updated to the new owner-or-author contract and the `ac11` source-contract test now asserts `AnonIdentity` + the owner-scoped `WHERE` clause. No migration, no human-only file touched.
- **MEM-002**: bounded conversation-memory window injection + context-aware retrieval. On every generation the router now reads recent turns newest-first from the structured `conversation_messages` store (MEM-001) via `ConversationRepository.fetch_tail`, accumulates them until a configurable token budget (`MEMORY_TOKEN_BUDGET`, default 2000, `Settings.memory_token_budget`) using the per-turn `token_count` persisted by MEM-001 — **no extra LLM call** — and injects them as alternating Human/AI messages between the system prompt and the current query, across all four answer modes (canon / off_topic / lore_gap / mystery). Previously-retrieved chunks are never carried into the window; chunks are re-retrieved fresh each turn. The window read happens before this turn is persisted, so only prior turns are seen. New `generate/memory.py` (`load_memory_window`, `MemoryWindow`, `MEMORY_MAX_TURNS=50` read cap; the window is rendered chronologically and trimmed to open on a human turn). The four `build_*_messages` builders gained an optional `history` parameter (default empty → unchanged 2-message shape, so the byte-frozen system prompts and existing tests are untouched); history is injected at the Human/AI role only, never the system zone (security.md A03). **Context-aware retrieval**: the most recent prior user turn is prepended to the current query (`"<last_user_turn>\n<query>"`) before embedding, for **both** intent classification and `/v1/retrieve`, so elliptical follow-ups ("et sa sœur ?") classify and retrieve the right lore; generation, persistence and `query_log` keep the raw query. The augmentation is independent of the memory budget (a tiny budget still benefits retrieval). **Security**: the window read is **owner-scoped** — `ConversationRepository.fetch_tail_owned` enforces ownership in SQL (`JOIN conversations` on `user_id`), so a caller passing another user's `conversation_id` gets zero history (closes a cross-conversation history-leak IDOR, security.md A01); and `detect_injection` is re-run on the prepended prior turn so a poisoned history turn still trips the suspected-injection prefix on the classifier input (parity with the current query).
- **MEM-001**: structured `conversation_messages` store with double-write. New migration `0006_conversation_messages.up/down.sql` adds a `conversation_messages` child table (FK to `conversations` with `ON DELETE CASCADE`, `role` check `user|assistant`, `ordinal` 0-based unique per conversation, `content`, `token_count`, `created_at`; index on `(conversation_id, ordinal DESC)` for bounded tail reads). On every POST turn the router now double-writes: GCS markdown append is unchanged and remains source of truth; a best-effort `INSERT INTO conversation_messages` follows, wrapped so any `asyncpg.PostgresError`/`OSError`/`TimeoutError` is logged as `inconsistency_message_insert_after_gcs` ALERT and never propagates (ING-003 AC-5 invariant preserved). `token_count` is recorded using the vendored Mixtral-8x7B-v0.1 tokenizer (already in the dep set via ING-016 `tokenizers` library). New `ConversationRepository.insert_message` and `fetch_tail(limit)` (newest-first by ordinal) methods. Token-budget windowing deferred to MEM-002.

- **#168** (infra): opt-in local GCS emulator persistence. A committed `docker-compose.override.yml.example` lets developers start the bundled fake-gcs emulator alongside the default stack by copying the file to `docker-compose.override.yml` (gitignored). The override uses the Compose `!reset []` tag to lift the `tools` profile gate on the `gcs` service and injects `GCS_EMULATOR_HOST` pointing at the bundled gcs emulator endpoint into the `workers` service. The tracked `docker-compose.yml` is unchanged — `gcs` stays behind `profiles: ["tools"]` and the `test_gcs_service_is_gated_by_tools_profile` invariant test continues to pass (AC-13). `docker-compose.override.yml` added to `.gitignore`. Runbook section "Persistance locale des conversations" added.

### Changed

- **CONTRACT-001** (contract/docs): reconcile the gateway↔workers `/v1/generate` contract and the standing memory non-goal with reality — no behavior change. `specs/openapi/gateway-to-workers.yml` `GenerateResponse` now matches the real worker output (`answer`, `citations`, `mode`, `conversation_id`, `request_id`, `usage`, `retrieve_ms`, `llm_ms`); the stale `model`/`cost_eur`/`lore_gap_detected`/`ticket_id` fields are removed and a `Usage` schema (`prompt_tokens`/`completion_tokens`/`cost_eur`, all nullable) added. The `X-User-Tier` header enum on `/v1/generate` and `/v1/verify-contradiction` reads `anonymous | member | author` consistently with SEC-001 AC-14 (was `members`/`author_only`). `docs/vision.md` memory non-goal now reflects a bounded recent-turns window being in scope. The `/v1/generate` **request** body shape remains stale (`contexts` array + identity headers vs the real `user_id`/`user_tier`/`request_id` body) — flagged for a follow-up ticket, out of scope here.

- **IDN-001**: anonymous `user_id` is now derived **solely** from the `archiviste_anon` cookie UUID — IP address and User-Agent are no longer part of the identity formula. Formula: `UUIDv5(NIL_namespace, cookie_uuid.as_bytes())`. Same cookie always yields the same `user_id` regardless of network change or browser update; different cookie yields a different `user_id`. Missing or invalid cookies still receive a fresh UUIDv4 cookie (no crash, no breaking change in behavior). The `fingerprint` field in `GET /v1/me` now carries the raw cookie UUID string (was SHA-256 hex) — **observable contract change**: existing anonymous `user_id`s change on the first request after deploy (expected and documented). `gateway/src/auth/fingerprint.rs` simplified: removed `compute_fingerprint`, `fingerprint_to_user_id`, `extract_ip`, `extract_user_agent`; new API: `cookie_uuid_to_user_id(cookie_uuid: &Uuid) -> Uuid` + `parse_anon_cookie` now returns `Option<Uuid>`. `gateway/src/lib.rs` `resolve_identity` middleware updated accordingly. Lints: zero new dependencies; `sha2` dep retained (used by auth routes and GCS signing).

### Fixed

- **#161** (gateway/frontend): on page reload the open conversation's turns now re-render. `app.js` init reads `loadConversationId()` after `loadHistory()` and calls `reopenConversation(activeId)` when truthy; when localStorage holds nothing the branch is skipped entirely (no fetch, no error). `handleNewConversation` and the `archiviste_anon` cookie identity are untouched. Backend regression test `hist161_reload_contract_owner_200_nonowner_404` added to `gateway/tests/test_hist001_history.rs` pinning the owner→200+ordered-turns / non-owner→404 contract the JS reload path relies on.

- **EVAL-012**: live Ragas now judges against retrieved chunk **text**, not source paths. `eval/ragas_runner.py` `_run_entry_live` only filled `retrieved_contexts` (the `source_path` list) and `eval/metrics.py` `_run_ragas_evaluate` fed that path list as the Ragas `contexts`, so the judge scored answers against file-path strings — the first completed live run (unblocked by EVAL-011) showed faithfulness≈0.013, context_precision=0.0, context_recall=0.0. `_run_entry_live` now also fills `retrieved_chunk_texts = [c.text for c in chunks]` (parity with the offline path), and `_run_ragas_evaluate` builds the dataset `contexts` from `retrieved_chunk_texts`. `retrieved_contexts` (paths) is kept unchanged — `compute_context_recall_structural` (canon structural recall) depends on it — and `retrieved_chunk_texts` stays transient (`repr=False`, not serialized), so the run-file JSON schema is unchanged. Pre-existing bug, separate from EVAL-011 and only visible once the run completed. Live scores re-validated by a corrected Cloud Run run; `eval/baseline.json` re-bake + Gate A recalibration remain a human follow-up.

- **EVAL-010**: `eval/persist.py` now authenticates to Cloud SQL with an IAM access token, fixing the final exit-4 on persist (a full live run completed all 46 generate entries and the 140/140 Ragas judge phase, then failed only at the DB write). The prod `DATABASE_URL` carries no password — Cloud SQL IAM database auth expects an OAuth token as the password — but `psycopg2.connect(DATABASE_URL)` passed none. When `CLOUD_SQL_IAM_AUTH=true` (already set on the `archiviste-eval` Job), `persist_eval_run` now fetches an `sqlservice.admin`-scoped token via `google.auth` Application Default Credentials and passes it as `password=` (mirroring the workers' asyncpg path in `db.py` + `auth_metadata/token.py`); behaviour is unchanged when the env var is unset (local/CI). The token is never logged (security.md §A09). `PersistError` now carries safe `error_class` + `pgcode` fields (no DSN/message), and `eval/ragas_runner.py` `_maybe_persist` logs the four metric floats as `persist_attempt` before the write so an expensive run's scores survive a persist failure in Cloud Logging, and logs `error_class`/`pgcode` on failure.

- **EVAL-009**: `RAGAS_MAX_WORKERS` is now read from the environment (`eval/metrics.py`, non-secret tuning knob, validated, default `1`) and set to `1` in `infra/terraform/eval_job.tf`. A live run showed `max_workers=3` still tripped Mistral's per-second rate limit, so the 429 backoff (`max_wait=60`) dragged each judge item to ~27 s and projected ~70 min — past even the 3600 s task timeout. Dropping to `1` keeps concurrency under the limit so judge calls run at their natural latency (~full run in ~10 min). Env-driven so concurrency can be retuned to the Mistral tier via `gcloud run jobs update --update-env-vars` with no image rebuild. Also widens the Cloud Run Job task timeout `3600s` → `7200s` as safety headroom against a slow tier / extra 429 backoff.

- **EVAL-008**: three changes together unblock the `archiviste-eval` Cloud Run Job (OBS-009) from completing the Ragas judge phase and persisting a live `eval_runs` row. (1) `eval/metrics.py` `_run_ragas_evaluate` now passes `RunConfig(max_workers=3, ...)` to `ragas.evaluate()` — the default `max_workers=16` flooded Mistral with 16 concurrent requests causing HTTP 429 storms whose retry backoff exceeded the Cloud Run Job task timeout; 3 workers stays within Mistral's per-second limit and the backoff absorbs remaining spikes. (2) `eval/clients.py` `RetrieveClient.search` and `GenerateClient.generate` now retry transient failures (HTTP 429/5xx and `TimeoutException`) with exponential backoff (up to 4 attempts), honouring `Retry-After` headers when present; non-retryable 4xx (400/401/403) still return `EntryError` immediately; the shared `_post_with_retry` helper avoids duplication across both clients. (3) `infra/terraform/eval_job.tf` raises the Cloud Run Job task timeout from the default 600 s to 3600 s to give a throttled run over the full golden set enough headroom.

- **EVAL-007**: two contract-conformance bugs that blocked the `archiviste-eval` Cloud Run Job (OBS-009) from completing a live run. (1) `eval/clients.py` `GenerateClient.generate()` now sends the headers required by the gateway→workers `/v1/generate` contract: `X-User-Id` (fixed synthetic UUID `EVAL_USER_ID`) and `X-User-Tier: anonymous`; the JSON body now contains only `{"query", "request_id"}` — `user_tier`, `top_k`, and `contexts` are removed (workers does its own retrieval). Previously the missing `X-User-Id` header caused a 400 `invalid_user_id` on every entry, making all entries `upstream_error`. (2) `infra/terraform/eval_job.tf` `DATABASE_URL` scheme changed from `postgresql+asyncpg://` to `postgresql://`; `eval/persist.py` uses psycopg2 which cannot parse the SQLAlchemy dialect scheme — the mismatch caused exit 4 on every persist attempt. IAM-auth Cloud SQL socket (`?host=`) is unchanged. These two fixes together allow the Job to persist a real live row, enabling `GET /v1/quality` to return a real measurement.

- **EVAL-006**: live eval workers reachability probe (`eval/ragas_runner.py`) now targets `/health` instead of `/healthz` and waits 30s instead of 5s. Cloud Run's public frontend reserves the literal `/healthz` path and 404s it before it reaches the container (workers exposes `/health` as the reachable alias), so the probe always reported the service unreachable → the `archiviste-eval` Job (OBS-009) exited 3 before running any entry. The 5s timeout also could not clear the ~20s workers cold-start. Path + timeout extracted to named constants; CLI healthcheck test mocks updated `/healthz` → `/health`.

- **EVAL-005**: `GoldenEntry` (`eval/loader.py`) now accepts the optional `category` metadata field carried by every entry of `specs/golden_qa.jsonl`. Previously `model_config = {"extra": "forbid"}` rejected `category` with a Pydantic `extra_forbidden` error, so the live eval runner failed at golden-set parse (exit 2) before any judge call — the `archiviste-eval` Cloud Run Job (OBS-009) could never persist a row. Mirrors the existing optional `difficulty` field; unknown fields stay forbidden.

### Added

- **BOARD-001**: Public read-only lore-gap board. New `GET /v1/board` (anonymous, LIMIT enforced) returns open tickets as JSON. New `GET /board` serves `static/board.html` — a vanilla-JS read-only table (`textContent` only, no `innerHTML`). New assets `board.js` + `board.css` under `/assets/`. Author dashboard (`GET /dashboard`, `GET /v1/tickets`) and all conversation access remain author-gated and unchanged. Five security headers applied by global middleware.
- **CIT-001**: the chat UI now renders a "Sources" list under each canon answer. The gateway already passes the workers' structured `citations` payload (`[{ source_path, chunk_ords }]`) through byte-for-byte, but `gateway/static/assets/app.js` only read `body.answer` and never surfaced sources — citations appeared only as raw inline `[source_path]` text in the prose. A new `appendSources(citations)` renderer builds a `<section data-role="sources">` with a "Sources" heading and one `<li>` per `source_path`, written via `textContent` (never `innerHTML`) so a poisoned source path cannot inject markup (security.md output sanitization). It short-circuits on an empty/absent `citations` array, so off-topic / lore-gap / mystery answers (which carry no citations) show no misleading empty block. Styling added to `styles.css` (`.sources`); behaviour asserted in `gateway/tests/citations_frontend_test.rs` against the served `app.js` (vanilla JS, no JS test runner per ADR-0005). No gateway/workers behaviour change.

- **EVAL-011**: Anthropic (Claude) Ragas judge wired into `eval/metrics.py` so the `archiviste-eval` Cloud Run Job (OBS-009) completes under its task timeout. `RAGAS_JUDGE_PROVIDER` now accepts `anthropic` (alongside `mistral`/`openai`), building a `LangchainLLMWrapper(ChatAnthropic)` chat judge pinned to `claude-haiku-4-5-20251001` (override via `RAGAS_JUDGE_MODEL`). Because Anthropic has no embeddings API, the judge embeddings half is **decoupled**: `RAGAS_JUDGE_EMBEDDINGS_PROVIDER` (`mistral`\|`openai`, default `mistral`) + `RAGAS_JUDGE_EMBEDDINGS_API_KEY` build the `LangchainEmbeddingsWrapper` independently of the chat key; `mistral`/`openai` chat judges keep their coupled embeddings (no regression). `langchain-anthropic>=0.2` added to the `live`+`dev` extras (already a workers dep — no ADR) + mypy override; `eval/uv.lock` regenerated. Terraform `eval_job.tf` flips the job to `RAGAS_JUDGE_PROVIDER=anthropic` (`LLM_API_KEY` ← new `ANTHROPIC_API_KEY` secret), `RAGAS_JUDGE_EMBEDDINGS_PROVIDER=mistral` (`RAGAS_JUDGE_EMBEDDINGS_API_KEY` ← existing `MISTRAL_API_KEY`), and `RAGAS_MAX_WORKERS` `1`→`4`; new `google_secret_manager_secret "anthropic_api_key"` in `secrets.tf` (no per-secret IAM — the project-wide `secretAccessor` on `archiviste-runtime` already covers it). The judge swap is the binding fix: free-tier Mistral at concurrency 1 (~56 s/item × 35 canon × 4 metrics = 140 judge units ≈ 2.2 h) exceeded the 7200 s timeout. Supersedes EVAL-003 AC-1/AC-4 (allowed provider set). Switching judge changes scores → `eval/baseline.json` re-bake + Gate A recalibration deferred to a human follow-up; `--persist` runs before Gate A (`ragas_runner.py`), so the `eval_runs` row (and `GET /v1/quality` → `/observability`) populates regardless of the gate verdict. Operator deploy: provision `ANTHROPIC_API_KEY` secret version post-apply, then `gcloud run jobs execute archiviste-eval`.

- **OBS-009**: Cloud Run Job `archiviste-eval` provisioned in Terraform (`infra/terraform/eval_job.tf`). Executes a one-shot live Ragas eval against prod workers, persists one row in `eval_runs` with `runner_mode='live'`, and makes `GET /v1/quality` return a real measurement instead of `no_data`. Job mirrors the ingest Job (`cloud_run_job.tf`): `archiviste-runtime` SA, `max_retries=0`, Cloud SQL IAM-auth volume, `DATABASE_URL` IAM-auth (SA email `%40`-encoded), `MISTRAL_API_KEY` secret via `secret_key_ref` (injected as `LLM_API_KEY` for the Ragas judge). Two-step command: fetch golden from `gs://archiviste-lore-corpus/golden/golden_qa.jsonl` → `/tmp/golden_qa.jsonl` using `google-cloud-storage`, then `python -m eval.ragas_runner --mode live --persist`. `eval.Dockerfile` updated to install `google-cloud-storage>=2.18` into the venv. No new IAM role or binding (objectViewer + cloudsql.client/instanceUser + run.invoker on workers already granted). One-time golden publication and manual execution procedure documented in `docs/runbook.md`.

- **EVAL-003**: configurable Ragas judge wired into `eval/metrics.py`. New `build_ragas_judge()` function selects `(LangchainLLMWrapper, LangchainEmbeddingsWrapper)` from `RAGAS_JUDGE_PROVIDER` (default `mistral`; `openai` also supported; unknown value raises `ValueError`). Mistral judge uses pinned snapshot `mistral-large-2411` (overridable via `RAGAS_JUDGE_MODEL`) + `mistral-embed` embeddings (overridable via `RAGAS_JUDGE_EMBEDDINGS_MODEL`). API key read from `LLM_API_KEY` via `pydantic.SecretStr` — never serialized. `ragas.evaluate()` now receives explicit `llm=`/`embeddings=` (no more implicit OpenAI default). Run file gains additive `judge` field recording `{provider, chat_model}` for live runs; absent for offline. `langchain-mistralai>=0.2` added to `live` and `dev` extras. Supersedes EVAL-001 AC-14 default `openai` → effective default is now `mistral`.

- **OBS-008**: `infra/docker/eval.Dockerfile` — reproducible eval runner image (context `eval/`, base `python:3.12-slim`, deps from `eval/uv.lock --frozen --extra live --no-dev`, code + `baseline.json` baked, `specs/golden_qa.jsonl` absent). New `live` extra in `eval/pyproject.toml` separates Ragas/datasets/langchain-openai from test/lint tools so the image installs only runtime deps; `dev` extra retains all tools + live deps for unchanged PR gates. ENTRYPOINT = `python -m eval.ragas_runner`; `docker run <image> --help` exits 0 with no network. `.github/workflows/deploy.yml` builds and pushes `archiviste/eval:${sha}` + `:latest` on every merge to `main`, reusing the `archiviste` Artifact Registry repo (no new Terraform resource). `eval/.dockerignore` excludes `tests/`, `runs/`, `fixtures/`, `__pycache__`, `seed_test_corpus.py`.

- **OBS-007**: `eval/ragas_runner.py` now attaches a Google-signed OIDC ID token (`Authorization: Bearer <id_token>`) to all workers calls (`GET /healthz`, `POST /v1/retrieve`, `POST /v1/generate`) when `--workers-url` uses scheme `https` and a non-loopback host. No-op for `http` or `localhost`/`127.0.0.1`/`::1` (local/CI). Token is sourced from the Cloud Run metadata server via `google-auth>=2` (`MetadataIdTokenProvider`); auth decision derived from URL only — no new CLI flag or env var. Token held as `pydantic.SecretStr` (never logged or serialised). Failure to fetch the token on an authenticated target is fail-fast: exit code `5` (distinct from `0–4`), `event=oidc_token_failed` log without token bytes, no unauthenticated fallback. New `eval/oidc.py`: `is_authenticated_target`, `derive_audience`, `IdTokenProvider` Protocol, `MetadataIdTokenProvider`, `OidcTokenError`.
- **OBS-004**: public `GET /v1/quality` endpoint returning the latest live Ragas eval run `{"faithfulness","answer_relevancy","context_precision","context_recall","golden_set_version","finished_at"}` (6 keys exact, `NUMERIC(5,4)` decoded as JSON number without rounding via `bigdecimal` crate). No live row → `200 {"status":"no_data"}`. DB unavailable → 503 sanitised `{"error":"upstream_unavailable","request_id":"<uuid>"}`. No auth gate (public anonymous). `#quality-widget` added to `/observability` (`observability.html/js/css`) with independent `loadQuality()` fetch (failure does not affect `#usage-widget`/`#health-widget`). No migration, no OpenAPI change. 5 security headers preserved. Fix: `bigdecimal` feature changed from `serde` to `serde-json`; metric fields annotated `#[serde(with = "bigdecimal::serde::json_num")]` to emit JSON numbers (not strings) as required by AC-1/AC-3; regression test `serialization_bigdecimal_emits_json_number_not_string` added (no-DB, runs under plain `cargo test`). Fix (review MED): `GET /v1/quality`, `GET /v1/stats`, `GET /v1/status` 200 paths now emit `Content-Type: application/json; charset=utf-8` (security.md §A05) via shared `handlers::json_utf8` helper; exactly one `content-type` header produced (header array `insert` replaces `Json`'s own value).

- **OBS-003**: `--persist` flag for `eval/ragas_runner.py` — when `--mode live --persist` is provided, the runner inserts one aggregated row per run into the new `eval_runs` table (migration `0005_eval_runs.sql`). Columns: 4 Ragas metrics (`NUMERIC(5,4) NOT NULL`), run metadata (`git_sha`, `started_at`, `finished_at`, `entries_total/ok/errors`), and `golden_set_version` (SHA-256 of `specs/golden_qa.jsonl` bytes). Offline runs are never persisted (`event=persist_skipped reason=offline_mode`). DB failures emit `event=persist_failed` (no DSN/stack leaked) and exit code `4`. Artifact-then-persist ordering guarantees the JSON run-file is intact on any DB failure. Zero LLM payload stored; INSERT is always parameterised (no f-string into SQL). Partial index `eval_runs_live_latest_idx` shapes the OBS-004 read path.

- **OBS-002**: public `GET /v1/status` endpoint returning aggregate service health `{"status":"ok"|"degraded","dependencies":{"postgres":{..},"gcs":{..},"workers":{..}},"checked_at":"<rfc3339>"}`. 3 probes run in parallel (AC-9), 2 s timeout each (AC-8), result cached in-process 10 s TTL (AC-10). 200-always (AC-5/AC-6), no auth gate (AC-7), sanitised body — no host/message/SQL detail (AC-2/AC-11). `#health-widget` added to `/observability` (`observability.html/js/css`). No migration, no OpenAPI change (AC-16), 5 security headers preserved (AC-15).
- **OBS-001**: public `/observability` page with `Chat | Observabilité` nav and a usage widget fed by new public `GET /v1/stats` endpoint returning exact `count(*)` of conversations (`{"conversation_count": <int>}`). DB unavailable → 503 sanitized `{"error":"upstream_unavailable","request_id":"<uuid>"}`. Nav added to `index.html`. New static assets `observability.{html,js,css}`. All 5 security headers preserved router-wide. No auth gate, no migration, no OpenAPI change.

### Changed

- **GEN-001**: rewrite Archiviste `SYSTEM_PROMPT` (mode 1 canon, AC-6) — drop role-play tone (in-world/érudit/character), require clear concise informative answers, explicitly forbid fabricating facts absent from the archives, and append exactly 2 relevant follow-up questions after each answer. Spec AC-6 amended (human-approved) + `EXPECTED_SYSTEM_PROMPT` test kept byte-for-byte. Anti-injection clause preserved.
- **GEN-003/004/005**: align the three fallback prompts to the GEN-001 tone — concise, no role-play, no fabrication. `OFF_TOPIC_SYSTEM_PROMPT` (mode 2) now refuses concisely and invites rephrasing instead of inventing 3 fake in-domain questions (root cause of hallucinated lore titles); `LORE_GAP_SYSTEM_PROMPT` (mode 3) drops the in-world/character framing; `MYSTERY_SYSTEM_PROMPT` (mode 4) drops the poetic role-play **but keeps the ACL non-disclosure clause** (never reveal that information is sealed/access-restricted) — security-preserving. Specs GEN-003 AC-7/AC-8, GEN-004 AC-4, GEN-005 AC-7 amended (human-approved); byte-for-byte / substring tests updated.
- **ING-016**: swap chunker tokenizer from BGE-M3 (`transformers.AutoTokenizer`) to vendored Mixtral-8x7B-v0.1 (`tokenizers.Tokenizer.from_file`). `transformers` and `sentence-transformers` removed from all dependency tables; `uv.lock` regenerated (-torch/-safetensors/-transformers/-sentence-transformers). Asset `workers/src/archiviste_workers/ingest/assets/mixtral_8x7b_v0_1_tokenizer.json` vendored (Apache-2.0, baked in wheel). Init is network-free. Closes `docs/blockers.md` 2026-05-18 INFRA-002 entry. AC-7 (image size ≥ -50 MB) to be verified at build time.

### Fixed

- **EVAL-004**: live CI eval judge forced to Mistral (`mistral-large-2411`) — dead `OPENAI_API_KEY` reference removed from `eval.yml`; baseline re-bake and Gate A recalibration deferred to EVAL-002 (blocking precondition: real-corpus + bge-m3 CI seed).
- **fix(workers)**: anonymous conversations were never persisted — the gateway derives a fingerprint UUIDv5 `user_id` (SEC-001) that has no `users` row, so `conversations.user_id` FK rejected the insert with `unknown_user` (best-effort logger swallowed it), leaving `GET /v1/stats` `conversation_count` permanently 0. `ConversationRepository.create_if_absent` now upserts the anon user (`INSERT INTO users (id, tier) VALUES ($1, 'anonymous') ON CONFLICT DO NOTHING`) in the same transaction before the conversation insert; members/authors no-op on conflict. Anonymous chat histories now persist and the usage counter reflects real traffic. Integration test added. No schema/migration change.
- **fix(gateway)**: main `/` page nav rendered unstyled — the shared `<nav>` CSS lived only in `observability.css`, which `index.html` does not load. Moved the base `nav` rules into the shared `styles.css`; `observability.css` keeps only the page-active highlight + `#usage-widget`.

- **OPS-007**: fix(ops) runtime regressions in the gdrive-sync → lore-corpus → ingest chain. (1) `infra/terraform/storage.tf`: add `google_storage_bucket_iam_member.lore_corpus_bucket_reader` — `roles/storage.legacyBucketReader` for `gha-deploy` scoped to `archiviste-lore-corpus`; grants `storage.buckets.get` required by `gcloud storage rsync` to stat the destination bucket (AC-1/AC-2). (2) `infra/terraform/cloud_run_job.tf`: inject `GCS_BUCKET=archiviste-conversations` into the Job env block; satisfies the pydantic required field `gcs_bucket` in `Settings()` (`settings.py:28`) so the Job boots without `ValidationError` (AC-3). `archiviste-runtime` binding unchanged at `roles/storage.objectViewer` (AC-4 non-regression).

### Added

- **OPS-006**: Private corpus channel `gs://archiviste-lore-corpus` closes the OPS-005 exit-2 loop end-to-end. New Terraform `infra/terraform/storage.tf` provisions `google_storage_bucket.lore_corpus` (europe-west9, uniform bucket-level access, public access prevented) with `objectAdmin` binding for `gha-deploy` (AC-2) and `objectViewer` for `archiviste-runtime` (AC-3). New `workers/src/archiviste_workers/ingest/download_corpus.py` module downloads all bucket objects to `<INGEST_ROOT>/lore/` via `google-cloud-storage` (no gcloud binary needed, zero new deps). CLI `archiviste_workers.ingest` now requires `--root <dir>` (replaces `find_repo_root`/`.git/` walk — AC-6); `--path` outside `--root` still rejects with `path must be relative to repo root`. `gdrive-sync.yml` adds WIF auth + `gcloud storage rsync lore/ gs://archiviste-lore-corpus --recursive` (additive only, no `--delete-unmatched-destination-objects`) and drops the PR-opening steps (AC-4). `ingest-lore.yml` trigger replaced from `push: paths: lore/**` to `workflow_run: [gdrive-sync] completed + success guard` (AC-8). Cloud Run Job command updated to `download_corpus && ingest --root $INGEST_ROOT --path $INGEST_ROOT/lore/`.

- **OPS-005**: `lore/` ingestion automated on push `main` via Cloud Run Job. New `infra/terraform/cloud_run_job.tf` provisions `google_cloud_run_v2_job` `archiviste-ingest` (`europe-west9`, workers image, Cloud SQL IAM authn volume, `MISTRAL_API_KEY` via Secret Manager, `max_retries=0`). New `.github/workflows/ingest-lore.yml` triggers on `paths:['lore/**']`, authenticates via WIF, runs `gcloud run jobs execute archiviste-ingest --wait` with `concurrency: {group: ingest-lore, cancel-in-progress: false}`. Runbook `§Ingestion DB après sync` rewritten (auto trigger + manual fallback; embedder is `mistral-embed` API, ~100 MB tokenizer, not 2.3 GiB). `gdrive-sync.yml` checklist updated: ingestion is now automatic post-merge.

- **OPS-003**: workers `/readyz` DB-aware readiness probe — `GET /readyz` acquires a pool connection and runs `SELECT 1` under a 2 s timeout; returns `{"status":"ok"}` 200 on success, `{"status":"degraded"}` 503 on any DB error or timeout. `/healthz` stays shallow (liveness only). Cloud Run `startup_probe` on `/readyz:8000` added to workers Terraform (`failure_threshold=20`, `period_seconds=10` — 200 s budget for cold transformers import). `terraform/iam.tf` grants `gha-deploy` `roles/iam.serviceAccountTokenCreator` on `archiviste_runtime` SA so CI migrate step can impersonate it.

### Fixed

- **OPS-005** (ingest Job runtime prerequisites): the Cloud Run Job failed every execution. Three image/pipeline/CLI defects are fixed here; the corpus delivery channel is split out to OPS-006. (1) The Job ran bare `python`, missing the uv venv where the package is installed (`ModuleNotFoundError: No module named 'archiviste_workers'`) → now runs `/app/.venv/bin/python`. (2) `deploy.yml` pushed only `workers:${sha}`, never `:latest`, so the Job (pinned to `:latest`) never received new images → `deploy.yml` now also tags `workers:latest`. (3) The ingest CLI `_run_async` called `create_pool` without `token_provider=`, so the Job would fail DB auth (`exit 2`) with `CLOUD_SQL_IAM_AUTH=true` → fixed by mirroring the `main.py` lifespan pattern (`SqlTokenProvider() if settings.cloud_sql_iam_auth else None`, passed to `create_pool`, `TokenFetchError` → exit 2, closed in `finally`). The Job stays non-functional until OPS-006 supplies the `lore/` corpus + `.git/` root the CLI requires — the corpus is intentionally kept out of the public repo (`.gitignore` `/lore/*`, spoilers); OPS-006 wires a `git init` root + `gcloud storage rsync` from a private GCS bucket.

- **INFRA (CF host proxy)**: `archiviste.nocilia.fr` now reaches the Cloud Run gateway via a Cloudflare Worker reverse-proxy instead of the Origin Rule from #107. Cloud Run's frontend routes by Host header and 404s on the unknown `archiviste.nocilia.fr` Host; the Origin Rule "Host Header Override" that would fix it requires a **paid** Cloudflare plan (Free returns `not entitled to use the HostHeader override` — see `docs/blockers.md` 2026-06-03). Replaced `cloudflare_ruleset.archiviste_fr_origin_host` with `cloudflare_workers_script.host_proxy` (`infra/terraform/workers/host-proxy.js`) + `cloudflare_workers_route` on `archiviste.nocilia.fr/*`; the Worker rebuilds each request URL with the `<gateway>.run.app` hostname so the outbound fetch derives the correct Host/SNI. Works on Free, no GLB/NEG cost. Token now needs Account > Workers Scripts:Edit + Zone > Workers Routes:Edit (see `variables.tf`).

- **OPS-004**: gateway `/health` + `/healthz` aggregate now authenticates its workers probe with a Google-signed ID token (audience=WORKERS_URL) via `workers_id_token_provider.fetch_id_token()` + `Authorization: Bearer`, exactly like the chat path. The probe previously called workers unauthenticated → workers (internal ingress + `run.invoker`) returned 401/403 → aggregate stuck on `{"status":"degraded"}`, failing the Deploy canary smoke gate (`.status == "ok"`) and triggering rollback. Token-fetch failure degrades (HTTP 200 + `"degraded"`, never 500). Closes the canary-smoke rollback loop.

- **OPS-003**: `deploy.yml` now runs `migrations/run.sh` against prod Cloud SQL after both canary deploys and before promote. Cloud SQL Auth Proxy v2 with `--impersonate-service-account --auto-iam-authn` authenticates as `archiviste-runtime` SA. Migrate failure → rollback, never promote broken schema. Added `canary_ready` step asserting both canary revisions reach `Ready=True` (via `gcloud run revisions describe --format=json | jq`) before smoke + promote. Rollback `if:` conditions extended to also fire on `migrate` or `canary_ready` failure. Closes the 2026-06-02 prod 502 incident (root causes: migrations never applied + CLOUD_SQL_IAM_AUTH env drift).

- **FIX-SEC-001**: workers `POST /v1/generate` now reads identity from `X-User-Id` + `X-User-Tier` headers (gateway-side contract per SEC-001 AC-14); drops body-side extraction that caused 400 `invalid_user_id` on every request post-SEC-006. Headers win over any legacy body fields; both UUID validation and tier enum check enforced with the same error codes. Closes prod 502 on `POST /v1/chat`.

- **INFRA-002 PR-f**: split `DATABASE_URL` per service in `infra/terraform/cloud_run.tf`. Gateway (Rust/sqlx) now receives `postgres://<sa>@localhost/archiviste?host=/cloudsql/<INSTANCE>` (plain `postgres://` scheme + `localhost` placeholder host); the `?host=` query param overrides to the Cloud SQL Auth Proxy Unix socket. Workers (Python/asyncpg) keep `postgresql+asyncpg://...@/archiviste?host=...` unchanged. Root cause: the pre-PR-f shared URL was Python/SQLAlchemy-only — sqlx rejected the empty host with `error with configuration: empty host` (revealed by PR-e finally unblocking gateway boot past the JWT env stage). Unblocks AC-14.

- **INFRA-002 PR-e**: provision JWT Ed25519 keypair (both keys via Secret Manager) + GCS signing placeholder env vars on archiviste-gateway Cloud Run (unblocks gateway boot, AC-14).

- **INFRA-002 (cloudflare provider compat)**: `cloudflare/cloudflare` provider 4.52+ removed `bot_fight_mode` from `cloudflare_zone_settings_override` (moved to `cloudflare_bot_management`, paid plans only). `terraform apply` failed with `Unsupported argument`. Fix: drop `bot_fight_mode = "on"` from `infra/terraform/cloudflare.tf:54` settings block; rename `cloudflare_record.archiviste_fr.value` → `content` (v4.52 deprecation); drop `cloudflare_rate_limit.archiviste_fr` resource entirely (deprecated since June 2025 EOL, 11+ months past in 2026-05 → runtime apply incertain). Both Bot Fight Mode and rate-limit rule now configured manually one-shot via CF UI per `docs/runbook/bootstrap-gcp.md` steps 11 + 12 (Free plan). Spec AC-8 amended in scope: bot_fight + rate-limit via operator runbook instead of Terraform. V2 SEC-002 will replace CF perimeter rate-limit with app-level tower_governor + Redis. See `docs/blockers.md` 2026-05-27 INFRA-002 entry.

### Added

- **UI-002b (dashboard auteur frontend)**: `GET /dashboard` route (author-gated via `RequireAuthor` extractor) serves `gateway/static/dashboard.html` compiled-in via `include_str!`. Static assets `dashboard.css` + `dashboard.js` served under `/assets/` by existing `ServeDir` (no auth required, no secret content). `dashboard.js` fetches `GET /v1/tickets` (credentials: same-origin), renders rows via `textContent` only (XSS-safe, AC-14), supports "Charger plus" offset pagination (AC-18), shows "Aucun ticket ouvert." on empty list (AC-17), shows "Erreur de chargement. Réessayez." with optional req-id on any error (AC-16), opens conversations via `window.open(url, '_blank', 'noopener,noreferrer')` (AC-15). All 4 UI-001 security headers (CSP/nosniff/Referrer-Policy/X-Frame-Options) present router-wide without modification (AC-12). No inline script/style/event handlers in `dashboard.html` (AC-13). Integration tests cover AC-1, AC-3, AC-4, AC-11, AC-12, AC-13 (11 tests green).

### Ops

- **OPS-002 (jsonwebtoken bump)**: bump `jsonwebtoken` 9 → 10 (`aws_lc_rs` backend, drops `ring` transitive dep). `jsonwebtoken` subtree now chains `aws-lc-rs` / `aws-lc-sys`; `ring` no longer pulled via JWT path. `use_pem` feature retained so existing PEM-based Ed25519 key loading compiles without source changes. ADR-0002 amended: JWT row version `10+ (feature aws_lc_rs)`, Forbidden entry `jsonwebtoken feature "rust_crypto"`, Status `accepted (amended 2026-05-25)`. Windows dev build requires NASM (`winget install nasm`). Pre-existing `cargo deny` failures (webpki-roots CDLA-Permissive-2.0, rsa RUSTSEC-2023-0071) pre-date this ticket.

- **OPS-001a (load test scaffold)**: k6 load-test harness for 100/500-VU scenarios against `POST /v1/chat`. Gateway middleware `overhead_header` (`axum::middleware::from_fn`) inserts `X-Gateway-Overhead-Ms` header on every chat response (gateway-only latency = total − workers call duration, measured via shared `Arc<AtomicU64>` request extension). Integration tests cover AC-4 (a–e): header present on 200 and 400, valid integer, overhead < total elapsed, gap ≈ workers delay ± 150 ms margin. `scripts/load/k6/chat-round-trip.js` implements scenarios `chat_100_users` / `chat_500_users` (ramping-vus, 30 s ramp, 60 s steady) with custom `Trend` `gateway_overhead_ms` and `Counter` `gateway_overhead_header_missing`, thresholds per AC-3, `discardResponseBodies: true`. `scripts/load/k6/prompts.json` contains 10 in-domain Nocilia corpus prompts (no PII, no prompt-injection). `scripts/load/README.md` covers pre-reqs (k6 ≥ 0.50, Cloudflare bypass), commands, Mistral budget estimate (€10–100/run, hard cap €30, D-3), summary-export procedure, report procedure. `docs/load-test-report-v1.md` skeleton created with all sections named and `TBD` placeholders pending live run (OPS-001b). `scripts/load/runs/.gitkeep` added for summary JSON commits.

### Security

- **SEC-006**: gateway now signs outbound /v1/generate calls with a Google-issued ID token (audience=WORKERS_URL) via the Cloud Run metadata identity endpoint and reuses the auth_metadata cache pattern; workers ingress switched to INGRESS_TRAFFIC_ALL with IAM run.invoker still restricted to archiviste-runtime (Terraform check block + post-deploy 403 smoke); resolves V1 manual-deploy 502 upstream_error on POST /v1/chat.

- **SEC-005 PR-B**: workers asyncpg pool now fetches a fresh Cloud SQL IAM access token from the metadata server on every new physical connection via `SqlTokenProvider` (new `auth_metadata` module). `create_pool` accepts `token_provider=` kwarg; asyncpg receives an async callable as `password=` (OQ-2 resolved — asyncpg >= 0.21 supports async callables). `max_inactive_connection_lifetime=45 min` recycles connections before IAM token TTL. Boot fail-fast: `lifespan` raises with `event=boot.sql_pool_init_failed reason_code=...` on metadata failure. Bearer wrapped in `pydantic.SecretStr` (AC-7 §A09). Review R1 fixes: removed `# type: ignore[union-attr]` via local non-None binding pattern; `TokenFetchError` carries `.reason_code` at raise-site eliminating substring matching; `lifespan` except narrowed to `(TokenFetchError, asyncpg.PostgresError, OSError, TimeoutError)`; `assert pool is not None` replaced by explicit `RuntimeError`; added `test_create_pool_with_token_provider_against_postgres` exercising end-to-end callable path against docker Postgres; `test_lifespan_fails_on_metadata_500` now asserts structlog `boot.sql_pool_init_failed` event + `reason_code` field + bearer redaction.

- **SEC-005 PR-A**: gateway sqlx pool now fetches a fresh Cloud SQL IAM access token (`sqlservice.admin` scope) from the metadata server on boot and on every new physical connection (45-min `max_lifetime`, `before_acquire` age gate, 30-min background refresher via `Pool::set_connect_options`); resolves INFRA-002 PR-f deploy blocker (empty PG password). `TokenProvider` relocated from `gcs::token` to `auth_metadata::token` (new shared module); `gcs::token` re-exports for SEC-004 call-site compatibility. Two named ctors: `for_gcs_signing()` + `for_cloud_sql()`. `AppState` exposes `gcs_token_provider` + `sql_token_provider` (independent caches, AC-2).

- **SEC-004**: gateway GCS V4 signing refactored from offline RSA-PKCS1 (operator-provisioned SA private key) to IAM signBlob HTTP API (runtime SA self-impersonation); drops `GCS_SIGNING_PRIVATE_KEY_PEM` env var, aligns `security.md` §A02. Adds `TokenProvider` with refresh-ahead token cache (60 s window) and retry-on-401. Six `reason_code` values for structured failure logging. Terraform adds `roles/iam.serviceAccountTokenCreator` self-binding on `archiviste-runtime`.

- **SEC-001 PR-a (infra auth layer)**: JWT EdDSA (Ed25519) verification with strict `alg` pin (AC-12); anonymous fingerprint identity (`SHA-256(IP|UA|anon_cookie)` → `UUIDv5`, AC-10); `GET /v1/me` returns tier + fingerprint for any caller (AC-9); `AuthUser` / `RequireAuthor` Axum extractors enforce auth gating per route (AC-11, AC-16); `X-User-Tier` + `X-User-Id` headers propagated from gateway to workers replacing hardcoded sentinel (AC-14); server-side session check via `sessions` table (AC-13); `archiviste_anon` cookie generation for anonymous visitors (AC-10); migration `0003_users_auth` adds `users.email`, `users.password_hash`, `sessions` table (AC-20). INV-6 property test: any invalid JWT on an authenticated route returns 401, never 200 with degraded tier.
- **SEC-001 PR-a R1 review fixes**: HIGH-1 — fix `fingerprint_to_user_id` namespace from `NAMESPACE_DNS` to `Uuid::nil()` per AC-10 verbatim; add regression test `ac10_verbatim_uuidv5_nil_namespace` asserting exact `UUIDv5(NIL, sha256_hex)` value. HIGH-2 — `try_authenticate_jwt` now fail-closed: `SessionError::Unavailable` with a Bearer token present → `JwtAuthError::DbUnavailable` → 503 (not silent anonymous downgrade); anonymous path unaffected. MED — extractor session check skips when pool=None (test env) instead of returning `SessionRevoked`; extractor doc removes stale `AnyUser` reference; `Set-Cookie` uses `append` not `insert`; `chat.rs` removes `user_id`/`user_tier` from JSON body (headers-only per plan); AC-16 tests tightened to strict 403 for member JWT, strict 401 for no-JWT. MISSING — `AUTH_FAILURES_INVALID_TOKEN` atomic counter + `tracing::warn!` event added (AC-19 phase-1); `ac21_cf_connecting_ip_has_priority` + `ac21_connect_info_used_when_no_cf_header` tests added. MED-3 blocker logged in `docs/blockers.md` (`sqlx::query_as!` macro requires live DB for `cargo sqlx prepare`). MED-6 LOC note: 802 src insertions total (full new auth substrate — cannot split without loss of cohesion); split deferred to human decision per plan U-6.
- **SEC-001 PR-b (signup/login/logout runtime auth)**: `POST /v1/auth/signup` — email normalised (NFKC+trim+lowercase), argon2id (m=19456 KiB, t=2, p=1), `INSERT tier='member'` literal only, 201 response (AC-1/AC-2/AC-3/AC-15). `POST /v1/auth/login` — argon2id verify timing-safe (dummy hash on unknown email / email-taken paths), 5-failure/15-min throttle keyed on `SHA-256(email)`, `Set-Cookie archiviste_session` HttpOnly Secure SameSite=Lax Max-Age=604800, JWT EdDSA signed with Ed25519 private key, 200/401/429 (AC-4/AC-5/AC-6/AC-7). `POST /v1/auth/logout` — server-side session revoke + Max-Age=0 cookie clear, 204 (AC-8). Body limit 4 KiB + Content-Type enforcement on all auth routes, 413/415 (AC-17). Structured tracing events `auth.signup.ok`, `auth.signup.email_taken`, `auth.login.ok`, `auth.login.invalid_credentials`, `auth.login.throttled`, `auth.logout.ok` with `email_sha256` field — raw email never logged (AC-18). `AUTH_FAILURES_INVALID_CREDENTIALS` and `AUTH_FAILURES_THROTTLED` atomic counters (AC-19). `scripts/seed_author.sh` generates author-tier argon2id INSERT SQL for manual execution (AC-15).
- **SEC-001 PR-b review fixes (round 1)**: B1 — `scripts/seed_author.sh` replaced with doc-only skeleton (CWE-94: password string-interpolation into heredoc Python removed; humain-authored content mandate). M1 — `ac8_clear_cookie_format_is_correct` replaced by `ac8_clear_cookie_has_all_required_attributes` (calls `session_cookie` pub fn directly, asserts 5 attributes); `ac19_invalid_credentials_counter_is_accessible` replaced by `ac19_invalid_credentials_counter_wired_in_source` (source-grep instead of manual increment). M2 — `ac5_signed_jwt_has_correct_header_and_payload` unit test added: calls `jwt::sign`, decodes header (alg=EdDSA, kid=TEST_KEY_ID) and payload (sub, tier, sid, iat, exp−iat=604800, iss, aud) with test public key. M3 — `UserLookup` + `SessionCreator` traits extracted (`auth/user_lookup.rs`, extended `auth/sessions.rs`); `AppState` gains `user_lookup: Option<Arc<dyn UserLookup>>` + `session_creator: Option<Arc<dyn SessionCreator>>`; `InMemoryUserLookup` + `InMemorySessionCreator` mocks in `tests/common/auth_mocks.rs`; happy-path tests added: `ac1_signup_returns_201_with_member_tier_and_no_cookie`, `ac2_signup_with_existing_email_returns_409_email_taken`, `ac4_login_success_returns_200_with_jwt_and_cookie`, `ac6_login_with_wrong_password_returns_401_invalid_credentials`, `ac6_login_with_unknown_email_returns_401_invalid_credentials`. M4 — `ac7_throttle_is_case_insensitive` renamed `ac7_throttle_keys_are_stable_for_same_input` with mixed-case simulation via explicit normalisation. M5 — `signup` (78L→36L) and `login` (83L→38L) handlers extracted: `check_email_unique_timing_safe`, `verify_credentials_with_timing`, `build_email_taken_response`, `build_invalid_credentials_response`, `build_throttled_response`. Total auth tests: 48 pass (0 fail, excluding 4 pre-existing sqlx::test DB-gated failures).
- **SEC-001 PR-b review fixes (round 2)**: N-5 — `SessionRevoker` trait + `PgSessionRevoker` added to `auth/sessions.rs`; `AppState` gains `session_revoker: Option<Arc<dyn SessionRevoker>>`; `logout` handler now calls `state.session_revoker.revoke(sid)` instead of `sessions::revoke(pool, sid)` directly; `InMemorySessionRevoker` mock (records revoked sids in `Mutex<Vec<Uuid>>`) added to `tests/common/auth_mocks.rs`; `ac8_logout_returns_204_revokes_session_and_clears_cookie` full happy-path test added. N-2 — `login` body reduced to 39L: extracted `pre_login_throttle_check` + `do_create_session_and_build_response` (was 60L). N-3 — `build_login_success` 7-param signature replaced by `LoginContext<'a>` struct (clean-code.md ≤4 params); `build_jwt_token` sub-helper extracted. N-4 — `ac7_handler_normalises_email_before_throttle_lookup` end-to-end handler test added (5 × `User@Example.COM` → 401; 6th `user@example.com` → 429). N-7 — `build_throttled_response` uses infallible `HeaderValue::from(u64)` instead of dead `if let Ok(...)` branch. N-8 — `new_with_lookup`, `new_with_mocks`, `new_with_all_mocks` gated behind `#[cfg(any(test, feature = "test-utils"))]`; `test-utils` feature declared in `Cargo.toml` and enabled via self dev-dependency. Total auth tests: 52 pass.
- **SEC-001 PR-a refactor (gitleaks fix)**: replace hardcoded `TEST_PRIVATE_KEY_PEM` / `TEST_PUBLIC_KEY_PEM` constants in `gateway/src/auth/jwt.rs` with a runtime-generated Ed25519 keypair (`OnceLock<TestKeypair>` via `ed25519-dalek` + `OsRng`) in `gateway/tests/common/jwt_helpers.rs`. Signing helpers moved to test-only module; `src/` no longer contains any PEM material (security.md § Forbidden patterns).

- **INFRA-002 PR-c (review fixes)**: fix 3 HIGH + 4 MED from review INFRA-002c — (1) smoke test now parses JSON `.status == "ok"` via `jq -e` (HTTP 2xx alone insufficient when workers degraded); (2) rollback uses dynamic revision resolution `gcloud run revisions list --limit=2 | tail -1` × 2 services (no `PREVIOUS` gcloud sentinel exists); (3) canary revision lookup replaced by `gcloud run services describe ... latestCreatedRevisionName` (reliable vs substring filter on random name slugs); (4) explicit `exit 1` appended to each rollback step; (5) smoke test comment documents why only gateway is smoke-tested (workers ingress=internal, gateway /healthz exercises both); (6) `rollback.md` aligned to dynamic resolution + secrets GHA documented.
- **INFRA-002 PR-c**: GHA `deploy.yml` — trigger `push main`, WIF auth (no `credentials_json`), buildx build+push gateway+workers `:<git_sha>` to Artifact Registry, `gcloud run deploy --no-traffic --tag canary` × 2, smoke `curl -sf <canary_url>/healthz | jq -e '.status == "ok"'` (direct `*.run.app`, bypasses Cloudflare DNS race), promote `LATEST=100` ou auto-rollback dynamique `<previous>=100` + `exit 1`. `docs/runbook/rollback.md` finalisé (section auto-rollback + xref deploy.yml + secrets GHA requis).
- **INFRA-002 PR-b (review fixes)**: Fix HIGH — `ssl = "strict"` (provider v4 correct value for TLS Full Strict; was invalid `"full_strict"`); add `bot_fight_mode = "on"` in `cloudflare_zone_settings_override` (AC-8 Bot Fight Mode). Fix MED scope creep — `main.tf` reduced to Cloudflare provider only; `variables.tf` reduced to 2 Cloudflare vars only. Fix MED quota — `.net` 301 redirect migrated from `cloudflare_page_rule` to `cloudflare_ruleset http_request_dynamic_redirect` (R2 free-plan quota 3 Page Rules preserved for `.com`/`.org`/`.eu`).
- **SEC-003**: gateway HSTS header + full coverage of 5 baseline security headers across all routes (success, error, 404, 405, body-limit 400). Adds `Strict-Transport-Security: max-age=31536000; includeSubDomains; preload` via `SetResponseHeaderLayer::if_not_present` router-wide. Completes A02/A05 baseline per `.claude/rules/security.md`.

- chore(ci): INFRA-001 align /healthz wait to 300 s + cache HF Hub (cold-cache fix) — `ci.yml` job `contract` now restores/saves `~/.cache/huggingface/hub` via `actions/cache@v4` (key `${{ runner.os }}-hf-hub-${{ hashFiles('workers/uv.lock') }}`); healthcheck wait loop extended to 300 iterations (warm-cache ≤ 60 s, cold-cache ≤ 300 s; 300 s chosen as ~×2 safety margin over empirical cold boot < 150 s for ~2 GiB BAAI/bge-m3 download + model load). `docker-compose.yml` service `workers` gains `start_period: 90s` so local warm-cache boots never transiently hit `unhealthy`. `eval.yml` deferred to EVAL-001 (PR #39 already rewrites it with the same pattern).

- fix(gdrive_export): render gdocs from Docs API tree instead of Drive markdown export — handles positionedObjects + bypasses Drive 10 MiB export cap (ING-014 follow-up)

### Added

- **GEN-005**: Mode 4 mystery + ACL post-retrieval filtering — `filter_chunks_by_tier` (pure module `services/acl.py`, fail-closed D-4); SQL ACL filter removed from `/v1/retrieve` (D-2, vision §41); `RetrievedChunk.access_tier` field propagated; mystery branch triggered when all top-K chunks are ACL-blocked (`mode='mystery'`, `citations=[]`); timing-constant path (real LLM call, 2×ING-003, `query_log` insert); `MYSTERY_SYSTEM_PROMPT` frozen byte-for-byte (AC-7); partial-block canon no-leak (AC-19); `blocked_count` in `generate` log (AC-18).
- **UI-002 PR1 R1 review fixes**: HIGH-1 — `list_tickets` and `signed_url` handlers now use `RequireAuthor` directly (not `Result<RequireAuthor, AuthError>`) so `AuthError::IntoResponse` propagates the correct status codes: `InvalidToken`/`SessionRevoked` → 401, `Upstream` → 503, `AuthorRequired` → 403 (fixes silent collapse of 401/503 to 403). MED-1 — `request_id = %request_id` field added to `tracing::info!` events `dashboard.tickets.list` and `dashboard.conversation.signed_url` (and `dashboard.signing_failed` warn) via `Extension<RequestId>` extractor. MED-2 — AC-23 tests added: `high1_expired_jwt_on_tickets_returns_401`, `high1_expired_jwt_on_signed_url_returns_401`, `high1_member_jwt_on_tickets_still_gets_403` (401/403 contract), `ac23_tickets_tracing_fields_in_source`, `ac23_signed_url_tracing_fields_in_source`, `ac23_tickets_list_event_emitted_with_correct_fields` (captures tracing subscriber, asserts no `question`/`signed_url`/`gcs_uri` in log payload). `tracing-test = "0.2"` added to `[dev-dependencies]`.

- **UI-002 PR1 (backend)**: `GET /v1/tickets` (paginated open lore-gap tickets, sorted `priority_score DESC, created_at DESC`, author-gated, AC-5 to AC-7, AC-20) and `GET /v1/conversations/{id}/signed-url` (GCS V4 `GOOG4-RSA-SHA256` signed URL, TTL 300 s, author-gated, AC-8 to AC-10, AC-21) added to gateway. `ApiError` enum with uniform `{"error":"<code>","request_id":"<uuid>"}` envelope. `gcs::sign` module (manual V4 signing via `ring`, no external GCS crate). Security headers (AC-12) and structured tracing events `dashboard.tickets.list` / `dashboard.conversation.signed_url` (AC-23) on both routes. No changes to `specs/openapi/gateway-to-workers.yml` (AC-22).

- **GEN-004b**: Mode 3 lore-gap pipeline — after retrieve, `max_score < 0.45` branches to zero-shot LLM « noté pour archives » response; ticket auto-created/incremented via cosine dedup; fail-soft on ticket/embed/db errors (200 OK preserved); `query_log.mode='lore_gap'`; log INFO extended with `top_score` + `ticket_action`.
- **GEN-004a**: migration `0004_tickets_embedding.sql` — `tickets.question_embedding vector(1024) NULL` + HNSW partial index + B-tree indexes `tickets_conversation_id_idx` and `tickets_status_priority_idx`; `ticket_service.create_or_increment` with cosine dedup threshold 0.85 and fail-soft error handling (embed/db failures return `skipped_error` + log ALERT, never block 200 response).

- **GEN-003**: Mode 2 off_topic — intent classifier (LLM zero-shot, fail-open canon) + branche refus poli in-world ; `query_log.intent` renseigné (`'in_domain'` ou `'off_topic'`) ; timeout dédié 5 s classifier ; agrégation usage/cost des deux appels LLM.

- **EVAL-001 (review fixes pass 2 — HIGH-A, HIGH-B, MED)**: Fix CI workflow — apply migrations via official `migrations/run.sh` before seed, export `DATABASE_URL` at seed step, add `LLM_PROVIDER/LLM_MODEL/LLM_API_KEY=ci-placeholder` to workers offline (offline eval uses `/v1/retrieve` only — no real LLM call); fix seed corpus tautology — lore-dummy narrative texts with keywords embedded in context + hash-based pseudo-embeddings (SHA-256, 1024-dim L2-normalised, non-zero, differentiated); document `keyword_overlap_rate` offline as plumbing/integration check in README; fix redaction property test — sentinels injected into `entry.answer`/`entry.citations` (serialised fields), positive assertion on `[REDACTED]` present. Tests: 55/55 green (+9 new seed corpus tests).
- **EVAL-001 (review fixes HIGH-1..HIGH-4, MED-5)**: Fix `baseline.json` schema to full RunFile AC-17 schema; fix AC-17 bypass via merge-base PR diff inspection (anti multi-commit attack); wire `_redact_raw` in `write_run` (dead redaction removed); fix tautological `keyword_overlap_rate` offline (now scores against retrieved chunk texts, not stub answer); implement real `seed_test_corpus.py` with psycopg2 insert; add `test_baseline_schema.py` (6 tests), AC-4 byte-identical double-run CLI test, AC-16 property-100-runs redaction test; decompose `main()` ≤40 lines; remove `_extra_headers` dead field; pin `except (CalledProcessError,OSError)`. 46 tests green. **LOC post-trim** : voir [ADR-0008](docs/adr/0008-eval-001-loc-waiver.md).
- **EVAL-001** : Ragas runner golden_qa (46 entrées 4 modes), gates A/B (absolue live + no-regression bidir), workflow CI offline + dispatch live, baseline.json versionné humain-only. Modules: `eval/loader.py`, `eval/stub_llm.py`, `eval/metrics.py`, `eval/gates.py`, `eval/clients.py`, `eval/run_writer.py`, `eval/baseline_skip.py`. CI fixture sanitisée `eval/fixtures/ci_smoke_qa.jsonl` (8 entrées, 4 modes, non-spoilers). **LOC waiver** : voir [ADR-0008](docs/adr/0008-eval-001-loc-waiver.md). Waiver ad-hoc, non-réutilisable sans nouvel ADR. Plan hypothèse 1 (split EVAL-001a/b) reste activable post-review si reviewer demande.
- Repo scaffolding (CLAUDE.md, .claude/ agents + commands, specs/, docs/, gateway/, workers/, eval/, infra/)
- Workflow Claude Code : architect / implementer / reviewer / eval-runner / debugger sub-agents
- Slash commands : /ticket /plan /impl /review /eval /debug /ship
- ADR 0001 : split Rust gateway + Python workers
- OpenAPI contract gateway-to-workers
- Golden Q/A skeleton + property invariants table
- CI workflows : lint + test + contract + ragas eval
- pre-commit : ruff, fmt, clippy, gitleaks, conventional commits
- **FOUND-001** : minimal viable scaffold — gateway `/healthz` proxying workers `/healthz`, docker-compose dev stack (postgres + gateway + workers), integration test green, CI passing.
- **FOUND-002** : reproducible local stack + boot SLA. Adds `redis` service (auth required, persisted via `redis-data` volume, no host port), `migrator` service under `profiles: ["tools"]` running `migrations/run.sh` (versioned, transactional, gap-detecting), `make migrate` target, `.env.example`, `scripts/measure-boot.sh` writing JSON artefact, dedicated `.github/workflows/boot-sla.yml` (non-blocking), runbook section on migrations + boot baselines.
- **FOUND-002 (review fixes)** : wired migrations integration suite (`tests/migrations/run_tests.sh`) and new stack integration suite (`tests/integration/test_stack.sh`, covers AC-2 redis no-auth rejection, AC-3 persistence across restart, AC-6 migrator excluded from `up -d`) into `boot-sla.yml`. `measure-boot.sh` now parses both NDJSON and JSON-array forms of `docker compose ps --format json`. Boot-metrics JSON shape is validated in CI. Build step trimmed to local-only images (`workers`, `gateway`). Runbook documents `make migrate` as mandatory first-boot step + forbids transaction control statements in migration files.
- **ING-003** : conversation logger workers (`POST /v1/conversations/{id}/messages`, GCS Markdown append-only avec `ifGenerationMatch` + retries déterministes 50/200/800 ms, index Postgres `conversations`, fake-gcs-server pinné `1.49` sous `profiles: ["tools"]`, `GCS_BUCKET` fail-fast au boot via pydantic-settings).
- **FOUND-003** : schema walking skeleton (`users`, `documents`, `chunks`, `conversations`, `tickets`, `query_log`) + HNSW cosine index on `chunks.embedding` (1024-dim) + sentinel anonymous user. Migration runner gains a `-- description: <text>` first-line directive (filename slug fallback). Integration tests cover AC-1..AC-16 against pgvector/pgvector:pg16.
- **GEN-001** : POST /v1/generate mode canon (LLM wrapper config-driven multi-provider Mistral default, citations parser permissive + filter, prompt 3-zones avec `<no_archives_found/>` marker, query_log + conversation log best-effort, timeout dur 30 s, prompt-injection sanitize-prefix).
- **GEN-002** : gateway `POST /v1/chat` forwarder — validates body (query ≤ 4 KiB, conversation_id UUID, body ≤ 1 MiB), generates `UUIDv4` request_id, forwards to workers `/v1/generate` with sentinel `user_id`/`user_tier`, passthrough response ≤ 256 KiB (streamed cap, no full-buffer DoS), uniform error envelopes (400/503/502/504), structured log per request without raw query, connect timeout 5 s / request timeout 35 s. LOC budget note: +15 LOC over cap 300 — `attach_request_id` middleware (decision R2, ~25 LOC) + verbose error-envelope types (~30 LOC) account for the overrun; no split warranted given the changes are functionally inseparable.
- **ING-001** : ingest CLI `python -m archiviste_workers.ingest --path lore/` walks `*.md`, parses YAML frontmatter (title required, `tags`, `access_tier` ∈ {public, members, author_only}), normalizes body NFKC + strips C0 controls, hashes (SHA-256), chunks via `RecursiveCharacterTextSplitter` (bge-m3 tokenizer, 512/64), embeds with `BAAI/bge-m3` (1024-dim, batch via `EMBED_BATCH_SIZE`), and UPSERTs `documents` + `chunks` idempotently (per-file transaction, hash-based skip/update). Adds `lore/sample/` seed (archiviste, scriptorium) and isolated test fixtures under `workers/tests/fixtures/lore/`.
- **RET-001 (review fixes)** : production lifespan now uses `db.create_pool` so the pgvector codec is registered (was raw `asyncpg.create_pool` — would fail to encode 1024-dim list as `vector` on first call); OpenAPI `GenerateRequest.contexts.items.$ref` repointed from removed `Chunk` to `RetrievedChunk` (full-document lint clean); retrieve handler now offloads the synchronous bge-m3 encode via `asyncio.to_thread` so concurrent requests don't serialize on the event loop. Added integration regression `tests/test_main_lifespan.py` asserting the lifespan pool round-trips a 1024-dim `vector`.
  - **CI fixes** : `boot-sla.yml` and `ci.yml` `contract` job now `CREATE EXTENSION IF NOT EXISTS vector` against the postgres service before workers start, so the new lifespan codec registration doesn't crash boot. `tests/test_retrieve_integration.py::test_database_unavailable_returns_503` now mirrors the conftest skip-gate (try/except `OSError` → `pytest.skip`) so the AC-14 test is gated on Postgres availability like sibling tests.
- **RET-001** : workers expose `POST /v1/retrieve` — embeds the query with bge-m3 (loaded once at lifespan startup), runs a single ACL-filtered cosine top-K SQL on `chunks` via the existing asyncpg pool (`<=>` operator, `WHERE access_tier = ANY(...)`, deterministic tie-break on `chunk_id`), and returns ordered chunks with `embedding_ms` / `search_ms` timings. Hard limits: `query` ≤ 4 KiB UTF-8, `top_k` ∈ [1, 20], SQL timeout 5 s. Errors map to `400 invalid_{request,query,top_k,user_tier}` and `503 {embedder,database}_unavailable`. Single redacted JSON log per request (no `query`/`text`/`embedding`/DB error leak). OpenAPI `/v1/retrieve` rewritten to match the new contract; `Settings.embedding_model` default flipped to `BAAI/bge-m3` (was the e5 768-dim placeholder).
- **UI-001** : chat page MVP served by the gateway — vanilla HTML+CSS+JS (`gateway/static/`) served via `tower-http ServeDir/ServeFile`; 4 security headers (CSP, `X-Content-Type-Options`, `Referrer-Policy`, `X-Frame-Options`) applied router-wide covering both static routes and `POST /v1/chat`; `app.js` manages `conversation_id` UUIDv4 in `localStorage`, inserts `<article data-role="user|assistant|error">` via `textContent` (XSS safe), shows generic error `L'archive ne répond pas. Réessayez.` with optional `<small>req: <id></small>`; "Nouvelle conversation" button clears DOM and rotates UUID.

- **ING-010** : `feat(scripts)` — gdrive_export utilities lib (slugify, state, frontmatter merge, paths, normalize, rename). Pure Python package `scripts/gdrive_export/` with isolated `scripts/pyproject.toml` + `scripts/uv.lock` (no Drive API, no runtime dep on workers/). 78 tests (unit + property hypothesis + git integration). Pre-commit hooks scoped `^scripts/.*\.py$`. `.gitignore` extended with `.gcp/`, `scripts/.gdrive_state.json{,.bak}`. ADR-0006 accepted. Ready for ING-013 (Drive API integration).
- **ING-013** : `feat(scripts)` — gdrive_export Drive API integration (auth, drive_client, sync orchestrator, CLI entrypoint). Adds `auth.py` (SA credentials from `GDRIVE_SA_KEY_JSON`/`GOOGLE_APPLICATION_CREDENTIALS`, fail-fast, conflict log), `drive_client.py` (list recursive, export gdoc, download PNG, httplib2 timeout 30s), `sync.py` (diff state↔Drive, dispatch created/updated/renamed/archived/unchanged/error, dry-run `would_*`, 1 MiB cap, body drift detection), `__main__.py` (argparse, exit code AC-19). Adds `scripts/.gdrive_state.json` (versioned, initial `{}`), `.env.example`, runbook section. 57 tests (unit + integration). BREAKING(internal): `StateEntry.drive_md5_checksum` renamed `content_signature` (algo-prefixed `sha256:`/`md5:`).
- **ING-011** : `feat(scripts)` — gdrive_export gsheet + gslide converters. Adds `gsheet_renderer.py` (GFM table per tab, pipe/newline escape, `*(empty sheet)*`, collision `-<gid>` suffix, sha256 signature, NFKC+Cf-strip, HTML comment AC-17), `gslide_renderer.py` (structured `## Slide N` render, Y/X sort, speaker notes blockquote, skip hidden slides AC-16, HTML comment AC-17). Extends `drive_client.py` (Sheets v4 `get_spreadsheet_tabs`/`get_sheet_values`, Slides v1 `get_presentation`, `verify_extra_scopes` fail-fast on 403 insufficientPermissions, scopes `spreadsheets.readonly` + `presentations.readonly`). Extends `slugify.py` (Unicode dash codepoints U+2014/2013/2012/2212 → ASCII `-` before pipeline, non-breaking). Extends `sync.py` (`_process_gsheet` one `.md` per tab with composite state key `<file_id>#<gid>`, `_process_gslide` one `.md` per deck, `_handle_archived` extended for composite keys per AC-7). Updates `docs/runbook.md` (onboarding scopes additionnels). 206 tests total (34 new unit + new integration classes). **LOC budget override**: production diff ≈ 727 LOC — two renderers + sync dispatch form a single cohesive vertical slice (gsheet renderer cannot be shipped without dispatch wire-up); splitting into ING-011a/b would create an artificial seam with no intermediate working state.
- **ING-011 (review fixes)** : addresses all HIGH/MED/LOW findings and coverage gap from `specs/reviews/ING-011.md`. HIGH-1: `get_spreadsheet_tabs` 429/5xx now increments `counts.errors` (was silently 0) so `__main__.py` exits 1 per spec L52. MED-1: phantom `local_path` reservation for gsheet in outer loop removed — no longer causes spurious `-<fileid>` suffix on colliding gdoc/png. MED-2: `_process_gsheet` (~76 LOC) split into `_fetch_tab_content` + `_apply_tab_state` + `_archive_removed_tabs` (each ≤ 40 LOC). MED-3: `SyncContext` dataclass introduced; `_process_file` (was 12 params) and `_handle_archived_tab` (was 10 params) now take `ctx: SyncContext`. LOW-1: `assert isinstance` patterns replaced by explicit raises (stripped by `-O`). LOW-2: broad `except Exception` in `_is_insufficient_scope` narrowed to `(json.JSONDecodeError, KeyError, AttributeError, UnicodeDecodeError)`. LOW-3: `verify_extra_scopes` now re-raises unexpected HTTP codes (non-403/404) as `DriveApiError` so boot fail-fast is visible. LOW-4: `extract_speaker_notes` joins textRuns with `\n`; multi-line notes rendered with per-line `>` prefix to keep blockquote valid. LOW-5: `seen: dict[str, bool]` in `resolve_tab_collisions` converted to `set[str]`. LOW-6: `/` in `tab_title` escaped to `\\/` in `drive_path` frontmatter field. LOW-7: `DriveClient.from_services` classmethod added; tests migrated from private attribute access. LOW-8: `_normalize_slide_text` helper added to `gslide_renderer` (NFKC + C0 + Cf-strip), matching `gsheet_renderer._escape_cell` level. Coverage gap (AC-7 whole-workbook deletion): `TestWholeWorkbookDeletion` integration test added. New tests: 6 (2 HIGH-1, 1 workbook-deletion, 1 LOW-3, 2 LOW-4). Total: 212 passed.
- **ING-011 (merged-header fix)** : `render_tab_markdown` now pads header + rows to `max(len(row))` instead of truncating data rows to header length. Fixes gsheet workbook with a merged cell on row 1 (e.g. title spanning columns A-D): Sheets API returns the merged row as a single value, which previously caused all subsequent data rows to be truncated to 1 column. 2 new tests (merged-header + jagged rows). Total: 214 passed.
- **ING-012** : workflow GitHub Actions `gdrive-sync.yml` (cron lundi 03:00 UTC + dispatch manuel) invoquant `scripts/gdrive_export`, capturant le summary JSON, et ouvrant une PR auto vers `main` (draft si erreurs ≥ 1, ready sinon). Actionlint câblé dans `.pre-commit-config.yaml` (hook `rhysd/actionlint` pinné par SHA) et dans `.github/workflows/ci.yml` (step `lint` dédié). Section `## Onboarding gdrive-sync` ajoutée dans `docs/runbook.md` (procédure SA GCP, secret, variable, premier run).

- **ING-014** : `feat(scripts)` — gdocs embedded image extraction to sidecar `<doc_slug>.images/`. Docs API v1 `get_document` per gdoc (AC-1), authenticated `download_image` via authorized httplib2 transport (AC-2), two-stage Pillow compression pipeline — Lanczos resize if > 2048px (AC-3a) + JPEG q=85 if > 5 MiB (AC-3b), `ImageOversizedError` placeholder on still-oversized (AC-3c). Markdown rewriter `md_rewrite.py` maps n-th `![](url)` to n-th objectId by body traversal (OQ-2, AC-4). Intra-doc dedup by MD5 post-compression (AC-5). Unsupported Markdown constructs (table, fenced_code…) pass through with one-time warning log (AC-6). Failed/oversized images get `#image-failed-<id>` / `#oversized-<id>` placeholders (AC-7). `StateEntry.images: dict[str,str]` added with lazy upgrade (AC-8). Unchanged detection extended to include images manifest equality (AC-9). Orphan cleanup after each run (AC-10). Archive no-op preserves images field (AC-11). Sidecar renamed before image processing on gdoc rename (AC-12). Dry-run skips downloads, counts `would_images_written` (AC-14). Summary counters `images_written/failed/orphans_removed` (AC-15). GHA draft condition extended to `errors != '0' || images_failed != '0'` (AC-16). MD5 filename validated `^[0-9a-f]{12}$` + `Path.resolve()` escape guard (AC-18). ADR-0007 (Pillow ≥ 10.4) added. `documents.readonly` scope added to auth. 291 tests (291 passed, 1 skipped — JPEG below 5 MiB on CI host).
- **ING-014 (review fixes)** : addresses all MED and selected LOW findings from `specs/reviews/ING-014.md`. MED-1: `TestGdocUnchangedPermute::test_unchanged_when_images_permuted` added — proves AC-9 set-based manifest comparison handles permuted image order (spec oracle line 101). MED-2: `TestGdocOversizedIntegration::test_oversized_image_placeholder` added — end-to-end path from `compress_image → ImageOversizedError` through `sync.py` to `#oversized-<objectId>` placeholder + `images_failed` counter + `gdrive_sync.image_oversized` log (AC-3c oracle line 94). MED-3: dry-run `images_written` counter no longer inflates — counter is not incremented in `_resolve_one_image` when `dry_run=True` (was an upper-bound lie; without downloading we have no basis to count successes). LOW: `except Exception` in `_resolve_one_image` narrowed to `except OSError` (covers `socket.timeout` + `ssl.SSLError` subclasses; unexpected exceptions propagate). LOW: `test_decompression_bomb_treated_as_undecodable` added in `test_images.py` — temporarily lowers `PIL.Image.MAX_IMAGE_PIXELS` to 1, asserts `ImageUndecodableError` raised (security spec L122). LOW: `cleanup_orphans` gains optional `lore_root` parameter; when supplied, asserts `sidecar_dir.resolve().is_relative_to(lore_root)` before touching filesystem (AC-18 defense-in-depth); caller in `sync.py` now passes `lore_root=ctx.lore_root`. LOW: AC-11 sidecar-preservation-on-archive test gap deferred — code path exists at `sync.py:275` (`images=entry.images`), follow-up ticket ING-014-bis planned. **LOC budget override**: +742 src LOC vs ≤ 300 cap — two-stage pipeline + 3 new modules + GHA extension form a single cohesive slice (AC-3 ↔ AC-9 md5 post-compression invariant cannot be split); accepted per humain arbitrage consistent with ING-011 precedent (accepted at +727 LOC).

### Fixed
- **ING-010 MED-1** (`scripts/gdrive_export/paths.py`): `resolve_local_path` now raises `ValueError` when both the plain candidate and the id-suffixed candidate are already in `taken_paths`, instead of silently returning a colliding path.
- **ING-010 MED-2** (`scripts/gdrive_export/state.py`): `save_state` now writes to a sibling `.tmp` file and uses `os.replace()` for an atomic rename — protects against state corruption on process crash mid-write (POSIX + Windows).
- **ING-010 MED-3** (`scripts/gdrive_export/frontmatter_merge.py`): `merge_frontmatter` now `copy.deepcopy`s each default value from `defaults_user` before inserting into the merged dict, preventing the module-level `USER_MANAGED_DEFAULTS["tags"]` list from being shared by reference across calls.
- **ING-010 fresh-eyes HIGH** (`scripts/gdrive_export/slugify.py`): `slugify` now re-strips leading/trailing `-` after the 80-char cap, preventing a trailing hyphen when the cap lands mid-hyphen-run. Fixes AC-3 idempotence invariant.
- **ING-010 fresh-eyes MED-1** (`scripts/tests/conftest.py`): AC-14 import-firewall regex strengthened to catch bare `import X`, aliased `import X as Y`, and `from X import ...` forms for `requests`, `httpx`, `urllib`/`urllib3`, `aiohttp`, and existing Drive API packages. Uses `re.MULTILINE`. New `tests/test_import_firewall.py` covers all bypass patterns (21 cases).

### Notes
- **ING-010 LOC budget**: PR totals ~1 058 LOC vs the ≤ 300 rule. Overrun is test-heavy (unit + property + git-integration suites across 6 modules). No production logic exceeds budget; test density is justified by TDD on filesystem/atomicity edge cases and property-based coverage of AC-3 invariants.

## [0.1.0] - TBD

Initial release.
