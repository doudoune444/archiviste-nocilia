# Review — ING-011

## Verdict
REQUEST_CHANGES

## Findings

| File:line | Severity | Pattern | Evidence | Suggested fix |
|---|---|---|---|---|
| scripts/gdrive_export/sync.py:293-296 | HIGH | swallowed error / missed errors count | `except (DriveApiError, FrontmatterMergeError, OSError, ValueError) as exc:` then `if mime not in (_SHEET_MIME,): counts.errors += 1`. A workbook-level failure (e.g. `get_spreadsheet_tabs` raising `DriveApiError` on 429/5xx) is logged but `counts.errors` stays 0, so `__main__.py` exits 0 (AC-19). Combined with `counts.total` never incremented for the failed workbook, summary is silently incorrect. Failure modes spec L52 says "Quota Sheets / Slides API → mêmes règles qu'ING-010 (log error, file suivant, exit 1)". | Remove the `_SHEET_MIME` exclusion. Errors at the workbook level must increment `counts.errors` (and ideally `counts.total += 1`). Add an integration test where `get_spreadsheet_tabs` raises `DriveApiError(429)` → assert `summary.errors == 1`. |
| scripts/gdrive_export/sync.py:90-95 | MED | dead allocation / silent collision risk | For `_SHEET_MIME`, the outer loop computes `local_path = resolve_local_path(components, slugify(name, file_id), ".md", taken_paths, ...)` (= `<sheet_slug>.md`) and reserves it in `taken_paths`. That path is never written. Worse: if another file (gdoc / png at same depth with same slug) is processed in the same run, the gsheet's phantom `<sheet_slug>.md` reservation forces the legitimate file to take a `-fileid` suffix, causing a spurious rename + diverging from ING-013 path rules. | Either skip `resolve_local_path` for gsheet (compute a placeholder not added to `taken_paths`) or compute and reserve all real tab paths up front. The tab paths are also built independently in `_process_gsheet:375` without going through `resolve_local_path`, so they bypass the collision suffix mechanism entirely. |
| scripts/gdrive_export/sync.py:351-427 | MED | function >40 LOC | `_process_gsheet` body is ~76 lines (excluding signature/docstring), violating `clean-code.md` "≤ 40 lines body". Cyclomatic complexity also high (3 nested branches: rename detection, cap check, unchanged check inside the loop). | Extract `_render_tab_or_error(tab, ...)`, `_apply_tab_state(...)`. |
| scripts/gdrive_export/sync.py:244-258 | MED | parameters > 4 | `_process_file` has 12 parameters (after the new `taken_paths`, `lore_root`, `state` additions). `clean-code.md` says "≤ 4 parameters. Beyond → struct / dataclass." | Group into a `FileContext` dataclass (file_id, mime, name, components, local_path, existing) and a `SyncContext` (drive_client, new_state, counts, dry_run, state, taken_paths, lore_root, drive_md5). |
| scripts/gdrive_export/sync.py:164-176 | MED | parameters > 4 | `_handle_archived_tab` has 10 parameters. Same rule. | Pass through the same `SyncContext` dataclass. |
| scripts/gdrive_export/sync.py:451-503 | LOW | function slightly >40 LOC | `_process_gslide` body ~52 lines. Less severe than `_process_gsheet` but still over the cap. | Extract `_apply_gslide_state` helper. |
| scripts/gdrive_export/sync.py:273-275 | LOW | `assert` for control-flow | `_process_file` uses `assert taken_paths is not None` / `assert lore_root is not None` / `assert state is not None` to narrow types. `python -O` strips them. Defensive but flagged: these "Optional" parameters are mandatory for gsheet but optional in the signature, which is a code smell — make them required, drop the asserts. | Make the params non-Optional and remove defaults; or carry them via a `SyncContext` (see above). |
| scripts/gdrive_export/drive_client.py:155-156 | LOW | silently-ignored failure mode | `verify_extra_scopes` comment "Other codes: ignore for probe purposes". A 500 from the probe makes scope verification silently pass; the real Sheets/Slides calls will then fail later with less actionable messages. | Re-raise / log non-403, non-404 responses so the boot probe is informative; or document why 5xx at boot is preferred over fail-fast. |
| scripts/gdrive_export/drive_client.py:219 | LOW | broad except (Exception) | `_is_insufficient_scope` does `except Exception: return True`. Justified as "safer default" per plan, but bare `Exception` triggers `BLE001` style smell and could mask programmer errors (e.g. AttributeError on refactor). | Catch specific exceptions: `(json.JSONDecodeError, KeyError, AttributeError, UnicodeDecodeError)`. |
| Diff size | MED | vertical-slice ≤ 300 LOC | Production diff (excluding tests/specs/plans/CHANGELOG/runbook) ≈ 727 LOC (`drive_client +105`, `gsheet_renderer +112`, `gslide_renderer +121`, `slugify +15`, `sync +303`, `auth +5`, `__main__ +3`, plus deletions). Tests: +851 LOC. Total +1634 LOC. `vertical-slice.md` says "≤ 300 LOC diff … Beyond → split ticket." Two renderers + sync dispatch overhaul is arguably one cohesive vertical slice (cannot ship gsheet without dispatch wire-up), but the ticket could have been split into ING-011a (gsheet) + ING-011b (gslide). | Acknowledge override (single cohesive sync extension), or split if a follow-up cleanup is on the roadmap. Document the override in the PR description. |
| scripts/gdrive_export/gslide_renderer.py:69 | LOW | concat without separator | `extract_speaker_notes` does `"".join(parts)` over text from multiple notes shapes. If notes span multiple shapes (rare), the join produces no boundary, possibly running words together inside the `> **Notes**:` blockquote. Also, if notes contain `\n`, the blockquote breaks (subsequent lines won't be quoted). | Join with `\n\n`; for AC-11 blockquote correctness, prefix each note line with `> ` after `**Notes**:`. AC-11 doesn't explicitly forbid multi-line, but the current render risks broken Markdown. |
| scripts/gdrive_export/gsheet_renderer.py:44-56 | LOW | set-as-dict | `resolve_tab_collisions` uses `seen: dict[str, bool] = {}` where a `set[str]` is the canonical Python idiom. Cosmetic. | `seen: set[str] = set()` + `seen.add(base_slug)`. |
| scripts/gdrive_export/gsheet_renderer.py:50 | LOW | redundant slug fallback | `slugify(title, str(gid))` — the fallback id is the gid stringified, but `gid` is also used to suffix collisions on line 55. If both tabs have empty/punctuation-only titles, both fall back to `file-<gid[:8]>` (different gids → different fallbacks, no collision). Works, but the fallback semantic is "file-..." which is odd in a tab context. | Consider `slugify(title, f"tab-{gid}")` or accept that tab fallback labels begin with `file-` (pure cosmetic). |
| scripts/gdrive_export/sync.py:401 | LOW | drive_path delimiter inconsistency | `drive_path = f"{drive_path_prefix}/{tab_title}"` concatenates raw `tab_title` (may contain `/`, spaces, em-dashes) into the `drive_path` frontmatter field. Other paths use slash-joined folder + name. Not a security risk (frontmatter only), but a tab named `a/b` produces a misleading drive_path. | Either escape `/` in `tab_title` or document that drive_path is informative only. |
| scripts/tests/test_sync_integration.py:840-846 | LOW | private attribute access in test | Test does `client._sheets_service = MagicMock()` after `DriveClient.__new__(DriveClient)`. Same in `test_drive_client.py::TestSheetsApi::_build_sheets_client`. Brittle to refactor. | Pass an injected service via constructor or factory; or use a test fixture that monkeypatches `build`. Tolerable for unit isolation. |

## Spec coverage

- AC-1: ✓ test `tests/test_sync_integration.py::TestGsheetSync::test_two_tabs_create_two_files` + `TestGslideSync::test_one_file_per_presentation` + `test_drive_client.py::TestSheetsApi::test_get_spreadsheet_tabs_returns_properties`
- AC-2: ✓ tests `tests/test_gsheet_renderer.py::TestRenderTabMarkdown::test_header_row_and_separator/test_pipe_escaped_in_cells/test_newline_in_cell_becomes_br/test_empty_sheet_body`
- AC-3: ✓ tests `test_slugify.py::TestUnicodeDashNormalization::test_em_dash_normalized/...` + `test_sync_integration.py::TestGsheetSync::test_tab_path_uses_double_dash_separator`
- AC-4: ✓ tests `test_gsheet_renderer.py::TestResolveTabCollisions::test_collision_second_gets_gid_suffix/test_order_by_index`
- AC-5: ✓ tests `test_gsheet_renderer.py::TestBuildTabSourceId::*` + `test_sync_integration.py::TestGsheetSync::test_source_id_format`
- AC-6: ✓ tests `test_gsheet_renderer.py::TestFrontmatterTitle::test_title_format` + `test_sync_integration.py::TestGsheetSync::test_frontmatter_title_has_em_dash`
- AC-7: ✓ test `test_sync_integration.py::TestTabArchival::test_tab_archived_when_removed` (per-tab removed-from-workbook archive). ✗ **No test for whole-workbook deletion archive each tab** (parent file_id absent from drive_ids branch in `_handle_archived_tab`). Code path exists at sync.py:178-181 but is uncovered.
- AC-8: ✓ tests `test_gsheet_renderer.py::TestContentSignature::test_sha256_stable/test_different_content_different_hash`
- AC-9: ✓ tests `test_gslide_renderer.py::TestExtractSlideText::test_top_to_bottom_order/test_left_to_right_order` + `TestRenderPresentationMarkdown::test_three_slides_numbered` + `test_sync_integration.py::TestGslideSync::test_slide_headings_numbered`
- AC-10: ✓ test `test_sync_integration.py::TestGslideSync::test_one_file_per_presentation`
- AC-11: ✓ tests `test_gslide_renderer.py::TestExtractSpeakerNotes::*` + `test_sync_integration.py::TestGslideSync::test_speaker_notes_rendered`
- AC-12: ✓ tests `test_gsheet_renderer.py::TestRenderTabMarkdown::test_nfkc_normalize_applied/test_zero_width_char_stripped`
- AC-13: ✓ test `test_sync_integration.py::TestGsheetSync::test_tab_over_1mib_skipped_other_exported`
- AC-14: ✓ test `test_sync_integration.py::TestMimeRegression::test_gdoc_and_png_unchanged` + existing 78 ING-010 + ING-013 tests still green (206 total)
- AC-15: ✓ tests `test_drive_client.py::TestScopeProbe::test_403_insufficient_scope_exits/test_404_probe_success` + `test_sync_integration.py::TestScopeFailFast::test_scope_missing_exits_with_message`. **Defensive fallback for unexpected error shape verified**: `_is_insufficient_scope` returns `True` on parse failure (drive_client.py:219), documented in plan as "fail-fast is the safer default". No test exercises the fallback explicitly — recommend a test where Google returns 403 with unparseable body → still fails fast.
- AC-16: ✓ tests `test_gslide_renderer.py::TestRenderPresentationMarkdown::test_hidden_slide_skipped_renumbered` + `test_sync_integration.py::TestGslideSync::test_hidden_slide_skipped_renumbered` (asserts `slide_index_original=2`, 1-based)
- AC-17: ✓ tests `test_gsheet_renderer.py::test_html_comment_first_line` + `test_gslide_renderer.py::test_html_comment_first_line/test_html_comment_no_tab_field` + `test_sync_integration.py::TestGsheetSync::test_html_comment_first_line_body` + `TestGslideSync::test_html_comment_first_line_body`. Byte-exact match `<!-- gdrive: file=... -->\n\n` verified via `body.startswith(...)` + `body.split("\n")[0]`.

## Property invariants

- `specs/properties.md` lists no invariant targeting gsheet/gslide. Slugify property tests (`test_property_idempotence/length/alphabet`) extended via Unicode-dash matrix tests but property tests pass unchanged with extended pipeline — non-breaking confirmed (206/206 pytest pass including 600 hypothesis examples).

## Security

- No secrets detected in diff (auth uses env vars only, no hardcoded SA JSON, no test fixture leaks).
- SQL: N/A (no DB queries in scripts/).
- Path traversal: tab path = `lore_root.joinpath(*[slugify(c, file_id) for c in components]) / tab_filename` where `tab_filename = f"{sheet_slug}--{tab_slug}.md"` and `sheet_slug`/`tab_slug` are slugified (alphabet `^[a-z0-9-]+$|^file-[0-9a-f]{8}$`). No user-controlled `..` or `/` can escape `lore_root`. ✓
- SSRF: Drive/Sheets/Slides API calls take only `file_id` (alphanumeric Drive ID listed by the SA-authorized folder traversal). No user-supplied URLs reach `httplib2.Http`. ✓
- Trust boundaries: SA credentials sourced from env (`GDRIVE_SA_KEY_JSON` or `GOOGLE_APPLICATION_CREDENTIALS`), never from CLI args. ✓
- Scopes: `drive.readonly` + `spreadsheets.readonly` + `presentations.readonly` — least-privilege, all read-only. ✓
- Embedding poisoning hooks: cell content + slide text + speaker notes pass through `normalize_body` (NFKC + C0 strip). `gsheet_renderer._escape_cell` additionally strips Cf-category (zero-width) chars in cell context. ✓ Slide text in `gslide_renderer` only goes through `normalize_body` — zero-width chars in slide content survive (NFKC does not remove them). Note for AC-12 strictness: spec says "Tout texte extrait gsheet/gslide passe par la même normalisation NFKC + strip control chars qu'ING-010 AC-4". Zero-width characters are NOT C0 controls; they are Cf format chars. AC-12 wording does not mandate Cf strip for slides — but the inconsistency between renderers (gsheet strips Cf, gslide does not) is worth a follow-up. **LOW severity finding, not blocking.**
- Output sanitization: HTML comment line `<!-- gdrive: file=<file_id> tab=<gid> -->` — `file_id` is from Drive (alphanumeric); `gid` is an int. No injection risk. ✓
- Logging: no PII / secrets logged. `_log_file` emits `source_id`, `local_path`, `status` only. ✓
- Forbidden patterns scan over diff: no `unwrap`, no `verify=False`, no unsafe deserialization, no `eval`, no `subprocess shell=True`, no `format!` SQL, no `String::from_utf8_unchecked`. ✓
- One `# type: ignore[no-untyped-call]` in `auth.py:84` from ING-013 (upstream google-auth gap), justified comment. Unchanged in this diff.

## Out-of-scope changes

- `specs/acceptance/ING-011.md` — modified +11/-10 in this PR (commit `bd6961f docs(spec): ING-011 update AC refs post ING-010/013 split`). Spec files are "humain-only" per `CLAUDE.md`. The diff is renumbering AC references from old ING-010 monolith to current ING-010/013 split. Assumed pre-approved by human (separate commit, scoped `docs(spec):`). **Confirm with author that this spec edit was explicitly approved.**
- `specs/plans/ING-011.md` — new file, +90 LOC, expected per workflow. ✓
- `docs/runbook.md` — section onboarding scopes additionnels, in plan's "Files to touch". ✓
- `CHANGELOG.md` — `[Unreleased]` entry. ✓
- No files touched outside the plan's `Files to touch` list. ✓

## Lints

- `uv run ruff check .` → All checks passed
- `uv run mypy gdrive_export/ tests/` → Success: no issues found in 28 source files
- `uv run pytest` → 206 passed in 4.00s (including 600 hypothesis examples on slugify properties)

## Summary

The implementation hits all 17 ACs with explicit test coverage, lints clean, mypy strict passes, and no security forbidden patterns are present. Two architectural issues warrant fixing before merge:

1. **HIGH** — `sync.py:294` silently drops `counts.errors += 1` for gsheet workbook-listing failures, violating the documented exit-1 contract.
2. **MED** — Dead `local_path` allocation for gsheet in the outer loop (`sync.py:92-95`) reserves a phantom path that can collide with legitimate gdoc/png files.

Several clean-code violations (parameter counts, function length) should be addressed via a `SyncContext` dataclass refactor — these are not new (gdoc dispatch already had them) but ING-011 grew the surface enough that the smell is now blocking maintainability. Vertical-slice cap (300 LOC) overrun is acknowledged given the coherent two-renderer + dispatch scope; document the override in the PR.

---

# Round 2 — re-review after fix commit 197f6d2

## Verdict
APPROVE

## Prior findings — fix verification

| Prior finding | Status | Evidence |
|---|---|---|
| HIGH-1 — gsheet workbook-listing error not counted | FIXED | sync.py:441-449 `_process_gsheet` wraps `get_spreadsheet_tabs` in try/except (DriveApiError, OSError); on failure increments both counts.total and counts.errors and returns. Integration tests TestGsheetWorkbookLevelError::test_429_on_get_spreadsheet_tabs_increments_errors + test_5xx_on_get_spreadsheet_tabs_increments_errors assert summary.errors == 1. Exit-1 chain covered by test_main.py::test_exit_1_on_errors (mocks errors=1 → sys.exit(1)). Two-half verification of the contract is acceptable. |
| MED-1 — phantom local_path reservation for gsheet | FIXED | sync.py:121-123 skips resolve_local_path for _SHEET_MIME and continues. Tab paths allocated per-tab inside _process_gsheet:461-464 and added to ctx.taken_paths only when real. Comment at L118-120 documents the MED-1 rationale. |
| MED-2 — _process_gsheet > 40 LOC | FIXED | Split into _fetch_tab_content (sync.py:346, 21 lines body), _apply_tab_state (sync.py:368, ~43 lines body — borderline, see round-2 finding L2-2), _archive_removed_tabs (defined but unused, see new finding L2-1), and slim _process_gsheet (sync.py:429, ~45 lines body — borderline but down from 76). |
| MED-3 — _process_file 12 params | FIXED | SyncContext dataclass at sync.py:59-73 collapses 7 shared params (drive_client, new_state, counts, dry_run, state, taken_paths, lore_root). _process_file now 8 params; _handle_archived_tab 8 params. Both still slightly above the ≤4 rule, but the residual params are per-file identifiers — the structural improvement is real and clean-code.md "extract when third occurrence appears" is honored. |
| MED-4 — _handle_archived_tab 10 params | FIXED | See above; reduced to 8 via SyncContext. |
| LOW-1 — _process_gslide > 40 LOC | FIXED | Split into _process_gslide (sync.py:514, 23 lines body) + _apply_gslide_state (sync.py:540, 36 lines body). Both under the cap. |
| LOW-2 — assert taken_paths is not None control-flow | FIXED | All three Optional params are now non-Optional inside SyncContext (required fields). Asserts removed from sync.py. |
| LOW-3 — verify_extra_scopes swallows non-403/404 | FIXED | drive_client.py:178-181 now re-raises non-403/non-404 HttpError as DriveApiError so boot fail-fast is informative. |
| LOW-4 — _is_insufficient_scope broad except Exception | FIXED | drive_client.py:244 now catches (json.JSONDecodeError, KeyError, AttributeError, UnicodeDecodeError) — specific exceptions matching every documented parse failure mode. |
| LOW-5 — gslide multi-shape notes concat without separator | FIXED | gslide_renderer.py:73-83 joins multi-textRun with \n, multi-shape with \n. render_presentation_markdown:131-135 prefixes every continuation line with "> " so blockquote stays valid for multi-line notes. Unit test test_multi_shape_notes_blockquote_valid asserts every non-empty notes line starts with ">". |
| LOW-6 — resolve_tab_collisions uses dict-as-set | FIXED | gsheet_renderer.py:45 now uses seen: set[str] = set() + seen.add(base_slug). |
| LOW-7 — private attribute access in tests | FIXED | DriveClient.from_services factory at drive_client.py:52-67 exposes a clean test seam. Tests test_drive_client.py + test_sync_integration.py::TestScopeFailFast now use DriveClient.from_services(drive, sheets, slides) instead of __new__ + private write. |
| LOW-8 — gslide Cf-strip inconsistency vs gsheet | FIXED | gslide_renderer._normalize_slide_text at L15-22 strips Cf category in addition to normalize_body, mirroring the cell pipeline in gsheet_renderer._escape_cell. Applied uniformly in extract_slide_text and extract_speaker_notes. |
| LOW-9 — drive_path delimiter ambiguity for tabs | FIXED | _apply_tab_state at sync.py:385 escapes "/" in tab_title to "\/" before concat into drive_path. |
| Coverage gap — AC-7 whole-workbook deletion | FIXED | TestWholeWorkbookDeletion::test_workbook_deleted_archives_all_tabs (test_sync_integration.py:920-953) runs sync with workbook in run-1 (2 tabs), removes the workbook entirely in run-2, asserts both tab .md files have archived: true, archived_at set, and summary.archived == 2. Exercises the real branch at sync.py:200-203 (composite key with parent file_id absent from drive_ids). Not a tautology mock — uses the production _handle_archived → _handle_archived_tab path. |

## Round 2 findings (fresh-eyes pass)

| File:line | Severity | Pattern | Evidence | Suggested fix |
|---|---|---|---|---|
| scripts/gdrive_export/sync.py:414-426 | LOW | dead code | _archive_removed_tabs(file_id, current_gids, ctx) is defined but never called anywhere in sync.py or tests (verified by grep). Tab-removal archival is already handled in _handle_archived → _handle_archived_tab (sync.py:189-225) before _process_gsheet runs. The new helper is orphaned, likely a leftover from the planned split that never got wired in. | Delete the function (13 LOC). Optional follow-up: add vulture or similar to CI. |
| scripts/gdrive_export/sync.py:368-411 | LOW | function slightly > 40 LOC | _apply_tab_state body ~43 lines. Borderline over the cap; readable but flagged for consistency with the rule. The two near-duplicate ctx.new_state[source_id] = StateEntry(...) blocks (dry_run vs non-dry_run paths) appear in _apply_tab_state, _apply_gslide_state and _process_gdoc — three occurrences, DRY threshold met. | Extract a _build_state_entry(local_path, sig, body_hash, now) helper used by all three writers. |
| scripts/gdrive_export/sync.py:464 | LOW | path reserved even when fetch fails | ctx.taken_paths.add(local_path) runs BEFORE _fetch_tab_content is called. If the tab fetch errors (429/5xx), the tab's local_path remains reserved in taken_paths, so a later file in the same run wanting that exact slug would get a -<fileid> suffix despite the tab not being written. Symmetric to the MED-1 phantom-reservation issue, much smaller blast radius (only triggers on transient tab-level API error AND a same-slug collision in the same run). | Move ctx.taken_paths.add(local_path) to after _fetch_tab_content returns non-None. |
| scripts/gdrive_export/sync.py:210-218 + sync.py:442 | LOW | duplicate get_spreadsheet_tabs call per workbook | _handle_archived_tab lazily fetches the tab list for a workbook to detect tab-level removals; _process_gsheet then fetches the same list again on the same run. Two Sheets API calls per gsheet when at least one stale composite key exists in state. Pre-existing pattern (not introduced by the refactor) but the SyncContext refactor made it more visible. | Memoize the tab list in SyncContext (e.g. ctx.tab_lists: dict[str, list[...]]), or have _process_gsheet read from the already-computed current_tab_gids. Pre-existing; OK to defer. |
| scripts/gdrive_export/drive_client.py:230-246 | LOW | doc/behavior asymmetry | _is_insufficient_scope docstring says "On parse failure, returns True (fail-fast is the safer default)" — but the narrowed exception list at L244 only catches the 4 listed types. A ValueError from int() cast or a TypeError from .get() on a non-dict would now propagate, killing the boot probe with a stack trace rather than failing safely. Probability low (Google JSON shape is well-known) but the documented invariant says "any parse failure ⇒ fail-fast True". | Either broaden via (json.JSONDecodeError, AttributeError, KeyError, TypeError, ValueError, UnicodeDecodeError) or rewrite docstring to list the exact catch set (cheaper fix). |
| scripts/gdrive_export/sync.py:215-218 | LOW | swallowed error → state retained without counts.errors | When _handle_archived_tab fails to fetch tabs (DriveApiError), it sets ctx.new_state[state_key] = entry and returns. This is "conservative" (keep old state on transient error) which is correct, BUT it doesn't increment counts.errors. The workbook is later processed in the main loop where _process_gsheet's own get_spreadsheet_tabs call WILL increment errors (HIGH-1 fix). Net behavior OK; flagged as design-coupling: the two error paths must stay symmetric, which is fragile. | Add a comment at L213-218 noting "errors are counted in _process_gsheet; here we conservatively retain state without double-counting". |

## Regressions hunted, none found

- _handle_archived flow for non-gsheet composite keys: still archives via _archive_entry. Verified by TestArchive::test_gdoc_archived_on_deletion green.
- _process_gdoc and _process_png unchanged in behavior (only param shape via ctx). Verified by TestFirstRun + TestRename + TestSecondRunUnchanged green.
- gslide rendering: _normalize_slide_text Cf-strip applied to BOTH extract_slide_text AND extract_speaker_notes — verified no test asserted Cf preservation (grep across tests/ found only gsheet Cf-strip assertions).
- DriveClient.__init__ real-credentials path unchanged; from_services is purely additive (uses object.__new__ to bypass __init__ in tests only).
- 206 prior + 6 new = 212 pass.

## Spec coverage delta

- AC-7 whole-workbook-deletion branch: now covered by TestWholeWorkbookDeletion::test_workbook_deleted_archives_all_tabs. Coverage gap from round 1 closed.
- All other AC mappings unchanged from round 1.

## Lints

- uv run ruff check . → All checks passed
- uv run mypy gdrive_export/ tests/ → Success: no issues found in 28 source files
- uv run pytest → **212 passed in 4.00s** (was 206; +6 new: 2 for HIGH-1, 1 for AC-7 whole-workbook, 2 for multi-textRun + multi-shape notes, 1 for blockquote validity)

## Diff size

- Production diff (excluding tests/specs/plans/CHANGELOG/runbook): 835 insertions / 98 deletions = +737 net (was 727 at round 1).
- Tests diff: ~1063 LOC.
- Round-2 fix commit added ~191 prod LOC + ~196 test LOC over the round-1 baseline. The vertical-slice override is already documented; the post-review refactor was prescribed by review so the additional LOC is justified.

## Security

- No regression introduced. New DriveClient.from_services factory uses object.__new__ to bypass __init__ for tests only — cls-bound and type-annotated, no exposure beyond pre-built services. No secrets leaked, no new external URL paths, scopes still least-privilege read-only.
- Cf-strip extension on gslide closes an embedding-poisoning gap (LOW-8 from round 1) — both renderers normalize identically now.
- Tab drive_path "/" escape (LOW-9) is informative-only (frontmatter field, not used for filesystem path) — closes minor downstream-confusion risk for tabs with "/" in their title.

## Summary

All HIGH and MED findings from round 1 are genuinely fixed (not papered over): the HIGH-1 contract is tested at both the counts.errors == 1 boundary and the sys.exit(1) boundary; the phantom path reservation is removed and gsheet tab paths are now allocated only when written; SyncContext collapses parameter lists meaningfully; both _process_gsheet and _process_gslide are split into testable units. Coverage gap (AC-7 whole-workbook deletion) is closed with a real branch-exercising test.

Round 2 findings are all LOW: one bona-fide dead-code function (_archive_removed_tabs, never called), one minor symmetric phantom-path bug at tab-fetch error, two pre-existing duplications that the refactor surfaced but did not introduce, two docstring/comment polish items. None block merge.

Verdict: **APPROVE**. Ship it.
