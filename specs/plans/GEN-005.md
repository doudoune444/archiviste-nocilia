# Plan — GEN-005 Mode 4 mystère + ACL post-retrieval filtering

## Pre-flight

(a) Files / dirs read: `specs/acceptance/GEN-005.md`, `specs/acceptance/SEC-001.md`, `specs/acceptance/GEN-003.md`, `docs/vision.md`, `specs/plans/GEN-001.md`, `specs/plans/GEN-002.md`, `specs/plans/RET-001.md`, `specs/properties.md`, `migrations/0002_schema.sql`, `workers/src/archiviste_workers/retrieve/{search,schemas}.py`, `workers/src/archiviste_workers/generate/{router,models,prompt}.py`, `workers/src/archiviste_workers/services/retrieve_client.py`, listing `workers/src/archiviste_workers/services/`, listing `workers/tests/`.

(b) 3 key hypotheses:
1. Timing-constant is achieved structurally: mystery branch performs same external calls as canon (1× LLM invoke, 2× `ConversationClient.append_message`, 1× `query_log` INSERT), no early-return, no static reply, no ACL cache. Test harness uses stubbed LLM with identical `asyncio.sleep`; hard-fail CI on p95 delta > 200 ms (spec D-3).
2. `access_tier` source = `documents.access_tier` (migration 0002:23-24), already populated by ING-001 from frontmatter, already JOIN-ed in retrieve SQL. Plan adds `d.access_tier` to SELECT, propagates through `RetrievedChunk` → `Chunk` → `filter_chunks_by_tier`. No new migration.
3. SEC-001 vocab mismatch (`member`/`author` vs workers `members`/`author_only`) is out-of-scope (D-6). Workers keep current vocab; gateway/SEC-001 maps at boundary.

(c) Zones of uncertainty:
- `specs/openapi/gateway-to-workers.yml` add `access_tier` field on `/v1/retrieve` response — humain-only file. Spec AC-2 + Non-goal §38 sanction the field add. Flagged in Risks.
- `Chunk` model gains required field `access_tier` — every existing test constructing `Chunk(...)` literals breaks. Audit needed in `test_generate_*.py`.
- RET-001 SQL filter removal touches `test_retrieve_integration.py` ACL assertions; will be flipped to assert all-tiers-returned.

## Goal

Move ACL filtering from retrieve SQL to a pure post-retrieval module in workers; add Mode 4 mystery branch in `generate/router.py` triggered when all top-K chunks are ACL-blocked; preserve canon timing structurally; trace `mode='mystery'` in `query_log`.

## Acceptance criteria recap

See `specs/acceptance/GEN-005.md` AC-1..AC-20 (verbatim, non recopié pour respecter la limite ≤ 100 lignes).

## Files to touch

### New
- `workers/src/archiviste_workers/services/acl.py` — pure module: `FilterResult` dataclass `(visible, blocked_count)`, `ALLOWED_ACCESS_TIERS_BY_USER_TIER` constant (AC-3 mapping byte-for-byte), `filter_chunks_by_tier(chunks, user_tier) -> FilterResult` with fail-closed on unknown chunk tier (logs `acl_unknown_tier` with `chunk_id` only). No imports of `fastapi`/`httpx`/`asyncpg` (AC-20). ~40 LOC.
- `workers/tests/test_acl_filter.py` — unit, no I/O: 9-cell permission matrix (3 user_tier × 3 access_tier) + 3 unknown-tier fail-closed cases + grep static assertion (AC-3, AC-20). ~50 LOC.
- `workers/tests/test_mode4_mystery.py` — integration: `mystery_response_shape` (AC-1), `all_blocked_triggers_mystery` (AC-4), `partial_block_triggers_canon` + `partial_block_canon_no_leak` (AC-5/19), `empty_retrieve_not_mystery` (AC-6), `query_log_inserted_with_mystery_mode` (AC-12), conversation persistence success + ING-003 failure (AC-11), prompt-injection propagation (AC-13), AC-9/10 nominal usage assertions, AC-16/17 LLM timeout/upstream, AC-18 log shape with `blocked_count`. ~120 LOC.
- `workers/tests/test_mode4_timing.py` — `@pytest.mark.timing` 50 canon + 50 mystery stub round-trips, stubs LLM `await asyncio.sleep(SLEEP_FIXED)`, asserts `|p95(mystery) - p95(canon)| ≤ 200ms` hard-fail per D-3. ~60 LOC.

### Modified
- `workers/src/archiviste_workers/retrieve/search.py` — drop `WHERE d.access_tier = ANY($2::text[])` clause, drop `$2` param, add `d.access_tier AS access_tier` in SELECT, function signature keeps `allowed_tiers` param ignored with inline `# D-2: ignored post GEN-005, filtering moved to services/acl.py`. ~10 LOC.
- `workers/src/archiviste_workers/retrieve/schemas.py` — add `access_tier: Literal["public","members","author_only"]` on `RetrievedChunk`. ~3 LOC.
- `workers/src/archiviste_workers/services/retrieve_client.py` — `Chunk.model_validate` now propagates `access_tier` (auto via Pydantic if field added in models). ~0 LOC change (validation auto-propagates).
- `workers/src/archiviste_workers/generate/models.py` — extend `Mode = Literal["canon", "off_topic", "lore_gap", "mystery"]`, add `access_tier: Literal["public","members","author_only"]` to `Chunk`. ~3 LOC.
- `workers/src/archiviste_workers/generate/prompt.py` — add `MYSTERY_SYSTEM_PROMPT` constant figée byte-for-byte (AC-7 instructions a-f), add `build_mystery_messages(query, suspected_injection) -> list[BaseMessage]` returning `[SystemMessage, HumanMessage]` with no chunks (AC-8). ~30 LOC.
- `workers/src/archiviste_workers/generate/router.py` — after retrieve, call `filter_chunks_by_tier(chunks, parsed.user_tier)`; if `intent='in_domain'` (assume from GEN-003 — fallback to always `in_domain` if GEN-003 not merged yet) and `result.visible == [] and result.blocked_count >= 1` → mystery branch (call `build_mystery_messages` + LLM, citations=[], `mode='mystery'`); else canon with `result.visible` chunks; log INFO adds `blocked_count` when `mode='mystery'` OR (`mode='canon'` AND `blocked_count > 0`). ~40 LOC delta.
- `workers/tests/test_retrieve_integration.py` — flip ACL assertions: `/v1/retrieve` returns all tiers regardless of `user_tier`, assert `access_tier` field present; remove SQL-side ACL test. ~15 LOC delta.
- `workers/tests/test_retrieve_properties.py` — adjust if it depended on tier filter (INV-9/INV-10 unchanged). ~5 LOC delta.
- `workers/tests/test_generate_prompt.py` — add `mystery_system_prompt_byte_for_byte` + `build_mystery_messages_no_chunks` (AC-7, AC-8). ~30 LOC.
- `workers/tests/test_generate_router.py` — fixture `Chunk` constructors gain `access_tier="public"` default. ~5 LOC delta.
- `CHANGELOG.md` — entrée `[Unreleased] / Added` : `**GEN-005** : Mode 4 mystère + ACL post-retrieval filtering (services/acl.py, MYSTERY_SYSTEM_PROMPT, retrieve no longer filters SQL).`

### Humain-only — flagged for explicit approval
- `specs/openapi/gateway-to-workers.yml` — add `access_tier` field on `RetrievedChunk` schema (sanctioned by AC-2 + Non-goal §38). **No SQL migration, no `golden_qa.jsonl` edit, no `properties.md` edit.**

### Explicitly NOT touched
- `migrations/*.sql` — schema unchanged (AC + Non-goal explicit).
- `specs/properties.md` — INV-3/INV-9/INV-10 still hold under new retrieve behaviour.
- `specs/golden_qa.jsonl` — D-7.
- `gateway/**` — D-6, no Rust change.

## Test strategy

- **Unit pur** (`test_acl_filter.py`): 9-cell matrix + 3 fail-closed cases (AC-3, AC-20) + grep static `import` check.
- **Unit prompt** (`test_generate_prompt.py`): byte-for-byte `MYSTERY_SYSTEM_PROMPT` equality + regex on AC-7 sub-instructions a-f + `build_mystery_messages` shape + absence of `<chunk`/`<retrieved_chunks` (AC-8).
- **Integration** (`test_mode4_mystery.py`): TestClient + ASGI in-process, retrieve stubbed via `httpx.MockTransport`, LLM stubbed via `respx` or `Runnable.invoke` patch, real Postgres for `query_log` + ING-003 mock. Covers AC-1/4/5/6/9/10/11/12/13/16/17/18/19.
- **Timing-property** (`test_mode4_timing.py`): 50+50 stub samples with calibrated `asyncio.sleep` LLM, `time.perf_counter()` p95, hard-fail at delta > 200 ms (D-3), warning at > 100 ms.
- **Integration RET-001 drift** (`test_retrieve_integration.py`): assert all 3 tiers returned + `access_tier` field present.
- **Contract**: schemathesis re-run on `/v1/retrieve` after OpenAPI yml update.
- **Eval**: none (D-7).

## Implementation steps (ordered, TDD)

1. Write **failing** `test_acl_filter.py` (9-cell matrix + fail-closed). Red.
2. Create `services/acl.py` (`FilterResult`, mapping constant, `filter_chunks_by_tier`). Green AC-3, AC-20.
3. Write **failing** `test_generate_prompt.py::mystery_system_prompt_byte_for_byte`. Red.
4. Add `MYSTERY_SYSTEM_PROMPT` + `build_mystery_messages` in `generate/prompt.py`. Green AC-7, AC-8.
5. Extend `Mode` literal + `Chunk.access_tier` in `generate/models.py` and `RetrievedChunk.access_tier` in `retrieve/schemas.py`. Fix `test_generate_router.py` fixtures.
6. Modify `retrieve/search.py`: drop SQL filter, add `d.access_tier` in SELECT, inline doc comment D-2. Update `test_retrieve_integration.py` assertions. Green.
7. Write **failing** `test_mode4_mystery.py` (AC-1, AC-4 first). Red.
8. Patch `generate/router.py`: branch routing after retrieve, propagate `blocked_count` in log, mystery LLM invoke + `query_log` + ING-003 persistence. Preserve canon path with `result.visible` chunks. Green AC-1/4/5/6/9/10/11/12/18/19.
9. Add AC-13 (injection propagation), AC-16/17 (timeout/upstream) sub-tests. Green.
10. Write `test_mode4_timing.py` with stub LLM `asyncio.sleep(0.150)` constant; assert delta ≤ 200 ms. Green or surface blocker.
11. Update `specs/openapi/gateway-to-workers.yml` (humain validates) + re-run `test_retrieve_contract.py` schemathesis.
12. `ruff check`, `mypy --strict`, `pytest workers/tests/test_acl_filter.py workers/tests/test_mode4_*.py workers/tests/test_generate_prompt.py workers/tests/test_retrieve_*.py` green.
13. CHANGELOG `[Unreleased]` entry.

## Risks / open questions

- **R1 (LOC)** — Spec estimate ~280 LOC prod (limite haute). Decomposition : `acl.py` ~40 + `router.py` delta ~40 + `prompt.py` ~30 + models/schemas/search.py ~25 ≈ **~135 LOC prod**. Tests ~230 LOC (hors comptage). OK ≤ 300.
- **R2 (`specs/openapi/gateway-to-workers.yml` humain-only)** — `access_tier` field add on `/v1/retrieve` response is required by AC-2. Spec sanctioned in Non-goal §38. Flag humain validation before commit.
- **R3 (Chunk model breaking field add)** — `Chunk.access_tier` becomes required (Pydantic v2 strict). Existing tests constructing `Chunk(source_path=..., ord=..., text=...)` literals break. Audit step 5 fixture update covers `test_generate_router.py`; double-check `test_generate_parser.py` and `test_generate_prompt.py` fixtures.
- **R4 (GEN-003 dependency on `intent`)** — Spec Pre-conditions list GEN-003 mergé. If GEN-003 not yet merged at plan time, `intent` field is absent from `router.py`; mystery branch assumes `intent='in_domain'` unconditionally. Plan assumes GEN-003 merged per spec; if not, surface as blocker (no workaround).
- **R5 (`asyncpg` SQL param change)** — Removing `$2` shifts `top_k` from `$3` to `$2`. Confirm `conn.fetch` param order updated. Test `test_retrieve_integration.py` catches.
- **R6 (timing test flakiness CI)** — D-3 mitigates with 200 ms threshold. If CI runner still flakes, mark `@pytest.mark.timing` and skip in CI default; track in OBS-001 V2 follow-up.

## Out of scope

- Mode 3 lore-gap (GEN-004), Mode 2 off-topic (GEN-003), classifier `intent` (GEN-003).
- SEC-001 user_tier vocab alignment (`member`/`author` vs `members`/`author_only`) — D-6.
- UI ACL author management (V2).
- Per-conversation/per-message ACL.
- Audit log table `acl_deny` events (OBS-001 V2).
- OTel counter `acl_blocked_total` (OBS-001 V2).
- ACL cache (D-5, side-channel).
- Adversarial "near-visible" chunk detection (V2).
- Per-tier mystery prompt tuning (D-7 single prompt).
- `specs/golden_qa.jsonl` mystery entries (EVAL-002 ticket).
- Gateway Rust changes (D-6).
- New migration (no schema delta).
- Statistical timing hardening >1000 samples (OBS-001 V2).
- Removing `allowed_tiers` param from `retrieve/search.py` signature (kept for backward-compat per D-2; cleanup ticket post-merge).
